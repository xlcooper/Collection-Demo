# 业务代码运行与验证指南

本文只维护当前 Demo 的正式运行入口、输入输出契约和验证方式。实验执行事实与审计结论以根目录 `PROGRESS.md` 为准。

## 1. 正式入口

```bash
python scripts/run_object_memory.py --input-dir <图片目录>
```

该入口串联：

```text
图片发现与哈希幂等
→ SAM3 自动点网格候选
→ 候选过滤和图像资产
→ Qwen 源图级批量判断
→ SQLite 对象记忆
→ 运行报告
```

历史分阶段验证脚本已从最终 Demo 结构中删除。

## 2. 当前固定输入与输出

```text
data/input/       固定输入集
data/memory/      当前完整对象记忆输出
```

`data/memory/` 已包含一次完整运行，使用默认配置继续运行时：

- 相同哈希的已完成图片会直接跳过；
- 新图片会写入现有对象记忆；
- 已有对象卡片和参考图会参与新图片的身份比较。

若要从零运行同一批输入，必须使用新的空记忆根。

## 3. 增量运行

```bash
cd /root/autodl-tmp/Collection-Demo
conda activate "$PWD/.conda/envs/object-memory-demo"

export HF_HOME="$PWD/weights/qwen"
export HF_HUB_CACHE="$HF_HOME/hub"

python scripts/run_object_memory.py \
  --input-dir data/input \
  --report environment/run_report.json
```

没有传入 `--memory-root` 时，程序使用 `config/default.yaml` 中的 `data/memory`。

## 4. 从空记忆运行

为输入—输出效果比较选择一个不存在的新目录：

```bash
python scripts/run_object_memory.py \
  --input-dir data/input \
  --memory-root data/memory_fresh \
  --validate-demo \
  --report environment/run_report_fresh.json
```

`--validate-demo` 只检查运行路径覆盖：

- 至少2个 `new`；
- 至少1个 `existing`；
- 至少1张完全重复图片被跳过；
- 至少1个候选被 filtered 或 ignored；
- 最终没有 pending 或 failed proposal。

这些条件不能判断对象身份是否正确，也不能代替输入真值和逐图对象 ID 复核。

## 5. 常用参数

| 参数 | 作用 |
|---|---|
| `--input-dir` | 必填，递归读取支持的图片 |
| `--config` | 配置文件，默认 `config/default.yaml` |
| `--memory-root` | 覆盖配置中的记忆根目录 |
| `--checkpoint` | 覆盖 SAM3 checkpoint 路径 |
| `--qwen-model` | 覆盖 Qwen 模型 ID |
| `--revision` | 指定 Qwen snapshot revision |
| `--allow-network` | 允许 Qwen 从网络解析或下载模型 |
| `--validate-demo` | 增加结构路径覆盖检查 |
| `--report` | 将运行报告额外写入指定路径 |

输入目录和记忆根目录不能相同，也不能互相包含。

## 6. 输出结构

每个记忆根都是一个不可拆分的完整单元：

```text
<memory_root>/
├─ memory.sqlite
├─ sources/
├─ proposals/
├─ objects/
├─ raw_responses/
└─ run_reports/
```

SQLite 与文件资产通过相对路径关联。清理或替换结果时必须处理整个记忆根，不能只删除数据库或单类图像。

外部 `--report` 是内部 `run_reports/` 报告的第二份副本，便于从仓库根目录快速检查。

## 7. 结果检查

一次运行至少检查：

1. 顶层运行状态、模型加载顺序和 external errors；
2. source、proposal、decision、object、observation 计数是否与 SQLite 一致；
3. 所有 source/crop/mask/overlay/raw-response 路径是否存在；
4. 重复图片是否在 SAM3 与 Qwen 前被跳过；
5. `new` 与 `existing` 是否依据真值指向正确具体实例；
6. accepted observations 是否包含背景、固定装置、阴影或无意义局部；
7. 单元测试是否有独立、可核对的执行摘要。

报告的 `status=passed` 只代表程序设定的结构条件通过，不自动代表对象记忆效果通过。

## 8. 当前回归测试

```bash
python -m unittest discover -s tests -v
```

测试目录按当前功能组织，覆盖配置与存储、候选后处理、批量身份判断、记忆事务、自动点网格和端到端编排。真实模型推理与完整回归在服务器执行。
