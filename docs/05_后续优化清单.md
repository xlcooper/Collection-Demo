# 后续优化清单

> 本文档集中保存用户明确延后项、用户记录的后续想法、有证据但待定优先级的假设、已接受权衡和历史不处理项。它不是当前版本计划，也不用来判定当前实验是否通过。

## 管理规则

- 每项使用稳定 ID；普通版本升级不删除、重置或重新排号。
- 只记录用户已提出的后续想法、已明确延后的事项，或有实验证据但当前未排期的事项。
- 条目状态只在用户改变优先级、开始实施、实验证实或明确放弃时更新。
- 当前必须修复的阻断项仍写在 `AGENTS.md`；已发生事实和审计结论仍写在 `PROGRESS.md`。

## 用户明确延后

### FUT-MEM-001 观测信息增量筛选

- **状态**：暂缓；2026-08-02 用户确认当前处理没有问题。
- **记录依据**：用户先后提出“有效新视角不一定都要保存”的优化想法，并在 2026-08-02 明确当前暂不考虑。
- **当前行为**：候选有效且终态为 `new` 或 `existing` 时，直接保存 observation。
- **后续方向**：将“候选有效”、“属于已有对象”和“值得新增长期观测”拆开；只在新视角、外观、状态或身份证据有增量时追加长期观测。
- **权衡**：可减少重复和低信息视角，但也可能过早删掉后续身份判断所需的证据。

## 用户记录的后续想法

### FUT-MEM-002 观测资产去重

- **状态**：已记录为后续优化想法，未排期。
- **记录依据**：2026-07-31 用户提出优先保存差异化 crop、不在对象目录重复保存 overlay；v0.17.0 审计量化了实际重复空间。
- **当前行为**：每条 `new/existing` observation 都从 proposal 复制 `crop`、`mask` 和 `overlay`。
- **后续方向**：候选审计层只保留一份资产，observation 通过引用使用；长期记忆优先组织有差异的 crop。
- **证据与权衡**：已清理的v0.17.0历史记忆根中重复副本占40.17%，原始证据可从Git提交 `f413654` 恢复；去重能节省空间，但会增加引用、迁移和单对象导出复杂度。

## 有证据但待用户定优先级

### FUT-CARD-001 对象稳定属性与场景信息分离

- **状态**：第一步已实施，等待服务器实验验证；完整属性累计结构仍未排期。
- **记录依据**：v0.16.1 出现鼠标颜色属性污染；v0.17.0 对象卡出现桌面反光和旁边物体等场景关系；v0.17.4 再次把灰银色鼠标更新为粉色，水瓶描述混入桌面与对象ID，并让整杯观测进入后仍保留 `cup lid` 档案。
- **当前行为**：第二轮v3提示要求当次 crop 描述写入 observation，比较后的稳定信息更新对象卡；禁止把对象ID、桌面、附近物体、位置和上下文框颜色写入对象卡。当前只把临时标注的 description 单独持久化，其他结构化字段仍由最终对象卡承载。
- **后续方向**：如新实验仍有末次观测污染，再为稳定属性、当次状态和场景关系设计独立 schema，并对属性冲突做累计管理。
- **权衡**：能减少末次观测污染，但会增加 schema 和更新规则。

### FUT-VIS-001 overlay 对第二轮视觉判断的影响

- **状态**：彩色覆盖已移除，原色定位框方案等待服务器验证；完整消融仍未开展。
- **记录依据**：v0.16.1 的鼠标颜色污染已发生；v0.17.4 中两张原始 crop 仍是灰银色，而红色高亮 overlay 中鼠标呈粉色，Qwen连续两次把它判断为粉色。对应关系更强，但还没有 crop-only 对照实验，不能认定 overlay 是唯一原因。
- **当前行为**：`overlay.jpg` 保留原始场景颜色，只画候选定位框；crop 是外观属性依据，上下文图只辅助判断边界、遮挡、附着和位置。
- **后续方向**：比较 crop-only、crop+mask 与 crop+overlay 对颜色、材质和身份判断的影响。
- **权衡**：当前方案保留位置和附着证据并减少颜色干扰，但是否足够仍需真实结果证明。

## 当前接受的设计权衡

### FUT-BATCH-001 首轮4图批次权衡

- **状态**：保留当前4图设计，不自动拆单救援。
- **记录依据**：2026-07-31 用户明确要求保留4图批次设计和其 trade-off；v0.17.0 的6词错误和 v0.17.4 的旧 `and` 误判都证明了单字段错误会使同批4张图一起停止。两个已知硬限制现已分别修正。
- **当前行为**：每批是一个完整结构化响应；任一内嵌字段违反 schema，整批不会生成可部分使用的结果。
- **后续方向**：只在有可比实验时评估批次大小或协议隔离方式。
- **权衡**：大批次降低调用和重复上下文成本，但会放大单字段错误的影响范围。

### FUT-DISC-001 首轮目标选择的召回与精度

- **状态**：v4提示已实施第一轮优化，长期权衡保留，等待服务器验证。
- **记录依据**：2026-07-31 两阶段方案确立时用户要求保留相关 trade-off；v0.17.4 中 `img_0008` 漏列鼠标，`img_0006` 又把整杯缩成杯盖和吸管，说明首轮遗漏和粒度偏差会直接限制后续结果。
- **当前行为**：首轮 Qwen 选择值得机械臂深入观察的完整独立物体，并优先生成 `computer mouse`、`water bottle`、`drink cup` 这类短而稳定的类别词；其选择仍决定 SAM3 的搜索范围。
- **后续方向**：根据更广场景调整选择标准、概念宽窄和固定物/可拆部件边界。
- **权衡**：选择越严格，候选越干净，但首轮遗漏会成为后续不可恢复的召回上限；概念过窄会漏异形视角，过宽会增加多实例和重复候选。

### FUT-SAM-001 SAM3文字阈值的召回与误检

- **状态**：阈值已从0.5调整为0.4，等待服务器实验验证。
- **记录依据**：v0.17.4中进入SAM3的16条提示有5条返回0候选，且一个成功水瓶候选分数仅约0.531，说明0.5可能偏紧；阈值前的最高失败分数没有被官方接口保留，因此不能保证0.4一定找回这些物体。
- **当前行为**：SAM3只把分数高于0.4的区域交给项目脚本；报告记录阈值、每条提示的检测数和0候选提示。
- **后续方向**：用下一次相同输入实验比较0候选数、误检数、脚本删除数和第二轮成本，再决定是否保留0.4。
- **权衡**：较低阈值可能找回遮挡或弱匹配物体，也可能产生更多背景区域、重复候选和Qwen视觉token。

### DEC-CONF-001 上游概念只作检索假设

- **状态**：已实施，作为长期设计护栏保留。
- **记录依据**：2026-07-31 用户确认两阶段设计并要求保留其 trade-off；当前 README、业务指南和第二轮提示均已实现该原则。
- **当前行为**：第二轮必须先根据 crop 判断外观，并用原色上下文框判断边界和附着关系，再把首轮文本概念当作可错的来源信息，不能直接当成对象事实。
- **权衡**：可减少上游命名对候选确认的偏置，但要求第二轮重新进行视觉理解。

### FUT-PERF-001 两阶段 Qwen 驻留与上下文成本

- **状态**：正确性稳定后再考虑。
- **记录依据**：2026-07-31 两阶段方案的单卡顺序驻留权衡；v0.16.1、v0.17.0 与 v0.17.4 已有真实耗时证据。
- **当前行为**：Qwen 首轮观察后释放，SAM3 完成后再加载 Qwen 做第二轮判断。
- **后续方向**：在对象数和观测规模增长后，再量化模型重载、视觉 token 和对象卡参考图成本。
- **权衡**：顺序驻留能在单卡上控制显存，但会增加加载和推理时间。

## 后续验证覆盖

### FUT-VAL-001 扩充产品语义验证集

- **状态**：当前能力边界，本阶段核心闭环稳定后再扩充。
- **记录依据**：当前隐藏真值只含3个具体实例，没有覆盖同细分类别的不同实例、独立纯无效场景或规模压力。
- **后续方向**：分别增加同类异实例身份区分、无目标/纯无效场景和更大对象记忆规模的可审计数据。
- **权衡**：能防止从当前3实例过度外推，但应在现行召回和协议阻断项修正后再增加实验成本。

## 历史已接受限制

### HIST-GRID-001 旧点网格配件误建档

- **状态**：归档，不做针对性修复。
- **记录依据**：用户在历史单图 gate 审计后明确接受该单例限制。
- **内容**：历史单图 gate 中 `grid_point_000006` 配件被误建档。当前两阶段文本定向流程已不使用该点网格路径，因此只保留历史记录。
