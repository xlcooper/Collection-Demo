# AutoDL 服务器环境分析与模型决策

> **维护规则（强制）**：本文只描述最新服务器环境及已确认的项目路径。每次收到新环境报告后，本地 AI 必须更新报告时间、对应提交、状态、关键版本、路径、风险和下一步；旧数据不得继续以“当前结论”出现。环境事实以 `environment/server_env_report.json` 为准，模型是否可运行以真实冒烟测试为准。涉及环境、模型、缓存或路径的开发必须先读本文，不得套用框架默认值。

## 1. 当前分析依据

- 环境报告：`environment/server_env_report.json`
- 报告生成时间：2026-07-30 11:41:54（UTC+08:00）
- 报告对应代码：`29221fb`
- 报告回传提交：`59bd3a7`
- 自检结果：`warning`，0 个阻塞项、2 个警告项
- 模型报告：SAM3 `cup` 提示得到 1 个 proposal，推理 0.462 秒；Qwen 输出 98 token，推理 7.356 秒、13.32 token/s，模型全部位于 GPU 0

## 2. 结论

M0 环境与模型验证已经完成，现有环境可以继续用于当前 M3 开发，不需要重建 Conda 环境或更换模型。Qwen 必须使用项目数据盘中的缓存路径，不能回退到系统默认的 `~/.cache/huggingface`。

当前保留两个非阻塞风险：系统 `nvcc` 为 11.8，以及数据盘剩余 23.45 GiB。PyTorch 自带的 CUDA 12.8 运行时已经正常识别 RTX 4090，因此普通推理可继续；MVP 不编译可选 CUDA 扩展，也不再下载备用模型。

两个模型已通过统一入口顺序运行。Qwen 峰值显存约 10.2 GiB，SAM3 约 5.0 GiB，顺序加载策略成立。数据盘剩余 23.45 GiB，低于保守的 30 GiB 警戒线，但没有形成阻塞；第一阶段继续使用小规模数据并停止下载备用模型。

## 3. 当前环境事实

| 项目 | 最新结果 | 当前判断 |
|---|---|---|
| 操作系统 | Linux 5.15，x86_64 | 合适 |
| GPU | RTX 4090，24564 MiB | 采用单卡、顺序加载 |
| 驱动 | 580.105.08 | 可运行 PyTorch cu128 |
| Conda | 22.11.1，当前环境 `object-memory-demo` | 独立环境已就绪；无需因版本提示升级 base |
| Python | 3.12.13 | 满足 SAM3 要求 |
| PyTorch | 2.10.0+cu128，CUDA 可用 | GPU 基础链路通过 |
| torchvision | 0.25.0+cu128 | 与 PyTorch 基线一致 |
| SAM3 | 0.1.0 | 已安装且实际导入通过 |
| SAM3 checkpoint | `weights/sam3/sam3.pt`，3.21 GiB | `cup` 提示推理通过，1 个 proposal 和 mask |
| Transformers | 4.57.6 | 已安装 |
| qwen-vl-utils | 0.0.14 | 已安装 |
| Qwen 权重 | 已下载至 `weights/qwen/hub` | 本地 snapshot 加载与结构化输出已通过 |
| vLLM / SGLang | 未安装 | 符合 MVP 决策，不是缺失项 |
| 仓库数据盘 | 总计 50 GiB，剩余 23.45 GiB | 触发空间警告；第一阶段限制数据规模，不再下载备用模型 |
| 系统 nvcc | 11.8 | 仅限制源码编译扩展，不阻塞预编译推理 |

报告中的 128 个逻辑 CPU 和 1007.51 GiB 内存可能是容器可见的宿主机视图，而非实例配额，因此不参与模型选型。后续若 CPU/内存成为瓶颈，再增加 cgroup 限额采集。

### 3.1 当前确认路径

以下路径是当前服务器已经验证的项目事实。表内相对路径均以仓库根目录为基准；业务脚本不得自行替换成框架或系统默认目录。

| 用途 | 当前路径 |
|---|---|
| 仓库根目录 | `/root/autodl-tmp/Collection-Demo` |
| Conda 环境 | `/root/autodl-tmp/Collection-Demo/.conda/envs/object-memory-demo` |
| SAM3 checkpoint | `/root/autodl-tmp/Collection-Demo/weights/sam3/sam3.pt` |
| Qwen `HF_HOME` | `/root/autodl-tmp/Collection-Demo/weights/qwen` |
| Qwen `HF_HUB_CACHE` | `/root/autodl-tmp/Collection-Demo/weights/qwen/hub` |
| 测试数据 | `/root/autodl-tmp/Collection-Demo/data/smoke` |
| SQLite 与记忆资产根目录 | `/root/autodl-tmp/Collection-Demo/data/memory` |
| 临时运行产物 | `/root/autodl-tmp/Collection-Demo/runs` |
| Git 跟踪的验证报告 | `/root/autodl-tmp/Collection-Demo/environment` |

开发环境相关功能前，按 README → PROGRESS → 本文 → `docs/03_服务器环境与冒烟测试指南.md` 的顺序核查，并对照最新报告及已经通过的脚本。若仍无法确认，应把未知项明确写出或向用户确认，不能以“通常如此”为依据继续实现，也不能擅自重新安装或下载替代资产。

## 4. 模型部署决策

### 4.1 SAM3

- 使用官方 `facebook/sam3` 图像 checkpoint；
- 权重固定在服务器 `weights/sam3/sam3.pt`，不进入 Git；
- 源码固定到 `requirements-sam3.txt` 中记录的提交；
- 第一阶段只验证图像推理，不安装视频、训练或可选 CUDA 扩展；
- 通用提示 `object` 返回 0 proposals；M2 已验证并锁定显式类别提示列表，一张图只编码一次并逐类别推理。它只覆盖配置类别，不代表开放场景全量发现。

### 4.2 Qwen3-VL

- 首选 `Qwen/Qwen3-VL-8B-Instruct-FP8`；
- Hugging Face 缓存根目录为 `weights/qwen`；
- `HF_HUB_CACHE` 固定为 `weights/qwen/hub`，不是系统默认缓存目录；
- MVP 使用 Transformers 直接推理；
- 模型下载与推理解耦：冒烟和正式推理先把缓存引用解析为本地 snapshot 目录，再交给 Transformers；
- 只有 8B FP8 在真实加载或推理中失败时，才回退到 `Qwen/Qwen3-VL-4B-Instruct`；
- 不提前安装 vLLM、SGLang 或 Thinking 模型。

### 4.3 顺序运行

```text
SAM3：图像 → proposal / mask / crop / overlay → 保存并释放显存
Qwen：读取对象图像与显式记忆 → 新颖性判断 / 标注 → 保存并释放显存
```

RTX 4090 24 GiB 不承担两个模型同时驻留。这个限制应保留到真实显存数据证明可以安全调整为止。

## 5. 当前风险与处理

| 风险 | 状态 | 处理 |
|---|---|---|
| SAM3 通用提示 `object` 返回 0 proposals | 已接受的能力边界 | 使用 M2 已验证的显式类别提示列表，不宣称开放场景全量发现 |
| Qwen 身份判断把同一视图误判为新对象 | M3 提示 v2 待复验 | 环境和物理对象标注已通过；v2 明确多图角色、禁止比较 overlay，并保持单次组合推理 |
| Qwen 98 token 推理 7.356 秒 | 基线已建立 | 13.32 token/s 且全部在 GPU 0；M3 再用真实 crop 评估吞吐 |
| generation flags 警告 | 已接受 | 本次确定性生成与 JSON 均通过；若后续影响行为再清理生成配置 |
| 数据盘剩余 23.45 GiB | 持续监控 | 第一阶段使用小型数据，不下载备用模型；接近 15 GiB 前先评估或清理可恢复产物 |
| 系统 nvcc 11.8 | 已接受 | 不编译 `flash-attn-3`、`cc_torch` 等扩展 |
| CPU/内存报告可能为宿主机视图 | 未决但不阻塞 | 当前不据此扩大并发或 batch size |
| 业务脚本回退到系统默认模型缓存 | 已发现并修正运行步骤 | 所有 Qwen 入口必须显式继承项目 `HF_HOME` 与 `HF_HUB_CACHE`，开发前先查本文路径表 |

## 6. M0 结论与后续约束

- M0 验收完成：环境 0 阻塞，SAM3、Qwen 单模型和统一顺序入口均通过。
- 后续里程碑继续使用现有模型，不下载 4B 或其他备选权重。
- SQLite、测试数据和运行产物继续留在项目数据盘；数据盘接近 15 GiB 前必须先评估或清理可恢复运行产物。
- M2 已完成候选策略验证；M3 继续验证对象语义质量，但必须使用上文确认的 Qwen 缓存路径。

环境重建与模型冒烟复现命令以 `docs/03_服务器环境与冒烟测试指南.md` 为准；当前开发任务以 `PROGRESS.md` 为准。
