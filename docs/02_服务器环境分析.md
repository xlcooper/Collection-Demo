# AutoDL 服务器环境分析与模型决策

> **维护规则（强制）**：本文只描述最新服务器环境。每次收到新环境报告后，本地 AI 必须更新报告时间、对应提交、状态、关键版本、风险和下一步；旧数据不得继续以“当前结论”出现。环境事实以 `environment/server_env_report.json` 为准，模型是否可运行以真实冒烟测试为准。

## 1. 当前分析依据

- 环境报告：`environment/server_env_report.json`
- 报告生成时间：2026-07-29 19:33:31（UTC+08:00）
- 报告对应代码：`b55c7cd`
- 报告回传提交：`bee21d4`
- 自检结果：`warning`，0 个阻塞项、1 个警告项
- 额外已验证事实：SAM3 在补齐运行依赖后可实际导入；Qwen 模型权重已下载；SAM3 首次推理暴露官方处理器缺少 BF16 autocast 上下文的问题

## 2. 结论

服务器基础环境已经解除阻塞，可以进入模型冒烟阶段，不需要重建 Conda 环境。

当前只保留一个非阻塞风险：系统 `nvcc` 为 11.8，低于 SAM3 面向的 CUDA 12.6 工具链。PyTorch 自带的 CUDA 12.8 运行时已经正常识别 RTX 4090，因此普通推理可继续；MVP 不编译可选 CUDA 扩展。

下一道门槛不是环境安装，而是分别证明：

1. 加入显式 BF16 autocast 后，现有 `sam3.pt` 能对固定图像生成有效 proposal、mask、bbox 和 score；
2. Qwen3-VL-8B-Instruct-FP8 能在单张对象图像上稳定输出约定 JSON；
3. 两个模型顺序运行时，磁盘和显存均满足第一阶段要求。

## 3. 当前环境事实

| 项目 | 最新结果 | 当前判断 |
|---|---|---|
| 操作系统 | Linux 5.15，x86_64 | 合适 |
| GPU | RTX 4090，24564 MiB | 采用单卡、顺序加载 |
| 驱动 | 580.105.08 | 可运行 PyTorch cu128 |
| Conda | 22.11.1，当前环境 `object-memory-demo` | 独立环境已就绪；无需因版本提示升级 base |
| Python | 3.12.13 | 满足 SAM3 要求 |
| PyTorch | 2.10.0+cu128，CUDA 可用 | GPU 基础链路通过 |
| torchvision | 0.25.0+cu128 | 与 PyTorch 基线一致 |
| SAM3 | 0.1.0 | 已安装；增强版自检将验证真实导入 |
| SAM3 checkpoint | `weights/sam3/sam3.pt`，3.21 GiB | 文件存在；等待推理冒烟 |
| Transformers | 4.57.6 | 已安装 |
| qwen-vl-utils | 0.0.14 | 已安装 |
| Qwen 权重 | 已下载 | 等待 SAM3 通过后运行 Qwen 冒烟 |
| vLLM / SGLang | 未安装 | 符合 MVP 决策，不是缺失项 |
| 仓库数据盘 | 总计 50 GiB，剩余 33.33 GiB | 可继续下载和冒烟；下载后重新关注余量 |
| 系统 nvcc | 11.8 | 仅限制源码编译扩展，不阻塞预编译推理 |

报告中的 128 个逻辑 CPU 和 1007.51 GiB 内存可能是容器可见的宿主机视图，而非实例配额，因此不参与模型选型。后续若 CPU/内存成为瓶颈，再增加 cgroup 限额采集。

## 4. 模型部署决策

### 4.1 SAM3

- 使用官方 `facebook/sam3` 图像 checkpoint；
- 权重固定在服务器 `weights/sam3/sam3.pt`，不进入 Git；
- 源码固定到 `requirements-sam3.txt` 中记录的提交；
- 第一阶段只验证图像推理，不安装视频、训练或可选 CUDA 扩展；
- `object` 文本提示只用于打通链路，“全量发现物体”的提示策略仍需后续专门验证。

### 4.2 Qwen3-VL

- 首选 `Qwen/Qwen3-VL-8B-Instruct-FP8`；
- Hugging Face 缓存根目录为 `weights/qwen`；
- MVP 使用 Transformers 直接推理；
- 只有 8B FP8 在真实加载或推理中失败时，才回退到 `Qwen/Qwen3-VL-4B-Instruct`；
- 不提前安装 vLLM、SGLang 或 Thinking 模型。

### 4.3 顺序运行

```text
SAM3：图像 → proposal / mask / crop / overlay → 保存并释放显存
Qwen：读取对象图像与显式记忆 → 新颖性判断 / 标注 → 保存并释放显存
```

RTX 4090 24 GiB 不承担两个模型同时驻留。这个限制应保留到真实显存数据证明可以安全调整为止。

## 5. 当前风险与处理

| 风险 | 状态 | 处理 |
|---|---|---|
| SAM3 首次推理出现 BF16/FP32 不一致 | 已定位、待复测 | 冒烟脚本显式使用 CUDA BF16 autocast |
| Qwen 权重已下载但尚未推理 | 待验证 | SAM3 通过后单独运行 Qwen 冒烟 |
| 下载后数据盘余量下降 | 可控 | Qwen 下载和冒烟后重新生成环境报告 |
| 系统 nvcc 11.8 | 已接受 | 不编译 `flash-attn-3`、`cc_torch` 等扩展 |
| CPU/内存报告可能为宿主机视图 | 未决但不阻塞 | 当前不据此扩大并发或 batch size |

## 6. 进入 M1 的条件

只有以下项目全部满足后，才开始对象记忆流水线和数据库实现：

- 增强版环境自检无阻塞；
- SAM3 单图冒烟通过；
- Qwen3-VL 单图结构化输出冒烟通过；
- 两份报告记录加载时间、推理时间和显存峰值；
- 模型下载后的数据盘仍有足够空间保存首批 Demo 数据。

当前执行命令以 `docs/03_服务器环境与冒烟测试指南.md` 为准。
