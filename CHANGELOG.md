# CHANGELOG

> **维护规则（强制）**：CHANGELOG 由本地 AI 统一维护。用户可直接 push 服务器生成的报告；收到通知后，AI 必须先 pull、分析变化，再在后续维护提交中补记该进展。每个 AI push 都必须新增版本、北京时间和简短摘要，最新版本位于最上方。

## v0.20.3 — 2026-08-04 18:18:51 +08:00

- 在 README 顶部增加当前 Web 展示地址，与 GitHub 仓库共同作为对外导航；不在仓库中记录访问密码。
- 按用户最新安排将当前协作方向调整为服务器无卡维持已有实验展示、暂不重新实验；仅同步版本边界，不修改算法、依赖或实验产物。

## v0.20.2 — 2026-08-04 14:57:43 +08:00

- 拉取并审计服务器提交 `af67e84`：完整 `data/memory/` 含1次运行、14张唯一源图、15个候选与决定、3个对象和15条观测；SQLite完整性为 `ok`，数据库119条资产引用无缺失，121个记忆库文件全部由Git跟踪，内外运行报告SHA-256一致。
- 将 `PROGRESS.md` 对齐运行 `run_20260803T093048019080Z`：28个输入去重为14张，23条SAM查询得到15个候选，3个 `new` 与12个 `existing` 形成3个正确对象档案；结构检查通过，但只覆盖人工确认的15/24次物体出现，M5-B仍未通过。
- 明确实验版本边界：该报告实际使用首轮v4、第二轮v3和SAM3阈值0.4；当前首轮v5与多记忆库Web功能晚于此次运行，尚未由正式实验验证。同步管理摘要、服务器环境实测显存和版本号，不修改代码或服务器原始产物。

## v0.20.1 — 2026-08-03 21:47:52 +08:00

- 将 README 重写为开源项目展示入口：以问题、架构、简洁原理、Web 能力、快速开始、审计边界和文档导航组织内容，移除重复的“当前实现”、完整 workflow、模型环境表、数据树与项目树，并纳入学术 Poster 风格的端到端架构图。
- 收紧文档职责：`docs/04_业务代码运行与验证指南.md` 作为稳定算法流程与报告字段事实源，`data/README.md` 只维护数据契约，`PROGRESS.md` 只维护真实实验与审计结论；删除跨文件重复的实现表、历史清理说明和易过期的当前目录状态。
- 澄清 AutoDL 自定义服务边界：平台可将实例内的 `127.0.0.1:6006/6008` 映射为公网 HTTPS 地址，因此使用该入口时仍需设置 Basic 密码，无需为此改为更宽泛的 `0.0.0.0` 监听。本批次仅修改文档和图片资产，不改变算法、依赖或实验数据。

## v0.20.0 — 2026-08-03 21:04:20 +08:00

- 为 Web 实验台增加多记忆库管理：保留兼容默认库，支持以服务器生成的安全内部编号创建独立空白库、选择完整已有库继续追加观测，以及整库删除或清空；页面刷新或服务重启后恢复上次选择，失败、不完整和不可读的库只供复核，运行期间锁定所有记忆库变更。浏览器始终只提交库编号，运行状态固定记录本次库，图片与审计资产请求也携带库编号，避免跨库串读；全新 Demo 覆盖门槛只用于零历史 run 的空白库，已有库续写不重复套用首轮覆盖条件。
- 明确 Qwen 到 SAM3 的文本契约：页面分别展示中文审计名称、计划用于 SAM3 且执行时不改写的英文 `sam_text_prompt`，并依据 `prompt_detection_counts` 标出已完成并记录检测数的文字查询；中途失败时不把未记录条目误写成确定未发送。首轮提示升级为 `robot-scene-guidance-v5`，先判定稳定基础类别、允许至多一个有用可见修饰词，明确保留 `transparent water bottle` 并强化透明瓶与饮料杯的类别区分。
- 重构结果摘要与数量口径：按输入整理、目标与已完成查询、候选区域、对象决策和记忆写入五组展示，区分本次增量与库内累计；流程与数据检查状态、运行错误、人工复核清单、模型耗时和折叠技术检查各自分层，目标未形成已记录查询或候选未形成决定时显式提示差额，历史库报告也从 SQLite 恢复真实运行耗时。
- 补充多记忆库安全边界、失败运行的库内报告、删除失败回滚与临时隔离清理、提示词契约和静态页面回归覆盖，同步 README、服务器环境说明、数据契约、业务运行指南、管理摘要及版本号。本地按项目约定只完成 Python AST、JavaScript 语法、差异与安全模式静态检查，不运行单元测试、模型、Demo 或 Web 服务，等待服务器验证。

## v0.19.1 — 2026-08-03 18:22:54 +08:00

- 将 Web 首页重构为面向项目展示的 `Collection Demo`：以当前端到端对象记忆能力重写摘要，紧凑展示 Qwen3-VL、SAM3、RTX 4090 单卡顺序推理配置，并增加独立 GitHub 项目链接块。
- 精简顶部 PIPELINE 与输入区文案，移除面向内部目录的说明；为输入整理、目标规划、SAM3 分割和对象决策补充用途说明，将图片数、Qwen 规划目标、保留候选和候选决定改为带完整单位的分组统计，明确14张处理图与23个规划目标的关系。
- 修复轮询期间结果区反复重建导致的交互回弹：相同内容不再修改 DOM，SQLite 与摘要视图同样跳过无变化重绘；定位、裁剪和掩码选择按候选记忆，新运行时显式清空。
- 更新 Web 静态契约用例、管理摘要和版本号。本地按项目约定只进行静态检查，不运行单元测试、模型、Demo 或 Web 服务，等待服务器验证。

## v0.19.0 — 2026-08-03 15:59:17 +08:00

- 新增单用户服务器 Web 实验台：浏览、上传和删除 `data/input/` 图片，单进程启动正式端到端入口，运行期间锁定输入；非回环监听强制使用环境变量 Basic 认证，浏览器不能传入服务器路径、模型参数、SQL或任意命令。
- 为正式流水线加入可选的即时落盘 JSONL 进度事件，按输入登记、首轮 Qwen、SAM3、第二轮 Qwen 和报告的真实工作单元记录进度、耗时、中间结果与失败证据；进度写入失败会透明报错并尽力关闭 SQLite run 与未完成 source，默认 CLI 行为和报告 schema 6 保持不变。
- 完成无需构建的单页可视化：顶部模块流、实验进度条与墙钟计时、28文件/14唯一内容去重证据、首轮目标、SAM3 保留及过滤候选、Qwen 对象决定与原始响应，以及确定性结果摘要。
- 将 SQLite 记忆设计为“长期对象卡—跨视角观测时间线”和 `source → proposal → decision → object` 候选血缘两种互补视图；数据库只读打开，图片与审计 JSON 按路径白名单提供，结构通过与人工语义验收明确分开。
- 固定 FastAPI、Uvicorn 与 multipart 依赖，补充进度、Web 服务、安全路径、状态恢复和静态页面契约的回归用例，并同步 README、数据契约、服务器搭建与运行指南、管理摘要及版本号。本地按项目约定只完成 Python AST、JavaScript 语法、差异与安全模式静态检查，未运行单元测试、模型、Demo 或服务器 Web，等待服务器拉取验证。

## v0.18.0 — 2026-08-02 17:39:40 +08:00

- 按用户授权删除三次旧实验的 `data/memory/`、`data/memory_v017_01/`、`data/memory_v017_02/` 及三份外部运行报告，共移除288个已跟踪文件、约18.5 MB；固定 `data/input/` 完整保留，删除前快照可从 Git 提交 `f413654` 恢复。
- 从最近审计暴露的源头优化首轮发现：提示升级为 `robot-scene-guidance-v4`，优先完整物体和短而稳定的类别词；schema不再仅因 `and` 拒绝整批，仍拒绝 `or`，真正的多物体表达由场景提示约束。
- 将SAM3文字阈值从0.5调整为0.4，并在schema 6运行报告中记录实际阈值、每条提示的检测数和0候选提示；该调整可能增加误检和Qwen成本，等待服务器实验决定是否保留。
- 将候选上下文改为原始场景颜色加定位框，配置禁止重新启用mask着色；第二轮提示升级为 `guided-image-batch-memory-reasoning-v3`，以crop判断外观、以上下文判断附着和遮挡，并阻止完整物体与附着部件互相归并。
- 将当次临时描述写入 observation、稳定累计描述写入对象卡；同步更新回归覆盖、README、实验看板、数据契约、服务器环境说明、业务执行指南和长期优化清单。当前未产生新实验结果；本地按项目约定不运行单元测试、模型或Demo，等待服务器复验。

## v0.17.5 — 2026-08-02 16:29:01 +08:00

- 拉取并审计 v0.17.4 第三次真实服务器实验 `run_20260802T075402522428Z`：28个输入去重为14张唯一图，首轮 Qwen 4次调用列出23个目标；第1批因单杯描述 `plastic drink cup with straw and cartoon lid` 含 `and` 被误判为多物体表达，4张图和7个目标整体停止，其余16条指令进入 SAM3 并形成11个候选。
- 逐图核对实际输入、首轮目标、SAM3结果和最终记忆：第二轮 Qwen 对9张图调用9次，给出3个 `new`、8个 `existing`，最终形成3个对象ID和11条观测；人工确认应有24次物体出现，数据库覆盖11/24，其中杯子档案被建成 `cup lid`，灰银色鼠标被后续观测更新为粉色。
- 完成 SQLite、文件与报告审计：95个记忆库文件完整，数据库完整性和外键检查正常，缺失及孤儿资产均为0，内外报告逐字节一致。服务器提交未包含单元测试日志，因此不声明回归测试通过。
- 用大白话重写本次实验看板，加入14张图逐图结果、完整编号链、两次实验对比和耗时；同步 AGENTS、数据目录契约、服务器环境说明、业务指南、长期优化记录及版本号。本批次只做只读审计和文档维护，未修改业务代码，未在本地运行模型、Demo 或单元测试。

## v0.17.4 — 2026-08-02 14:57:35 +08:00

- 删除首轮 `sam_text_prompt` 的固定6词上限，不再用任意单词数拒绝可用的单物体描述；继续保留2–64字符、英文小写、单一具体物体、禁止 `and/or`、位置词和泛称等必要边界。
- 优化首轮 Qwen 提示：允许在64字符内加入有助于 SAM3 定位的多个可见属性，并将提示版本升级为 `robot-scene-guidance-v3`，使后续实验能与旧结果明确区分。
- 增加回归覆盖：本次实验中的两条7词原句和一条8词单物体描述应通过，超过64字符及原有多物体、位置、泛称等错误仍应拒绝。
- 同步 README、业务运行指南、AGENTS、PROGRESS 和版本号；v0.17.0 原始报告及记忆库保持不变。本地按项目约定未运行单元测试、模型或 Demo，等待服务器验证。

## v0.17.3 — 2026-08-02 14:14:06 +08:00

- 用大白话重写 `PROGRESS.md`：按28个输入、14张唯一图、23条首轮建议、18条 SAM3 指令、12个候选、3个对象和12条观测的真实数量链解释本次实验。
- 新增逐环节的输入、实际处理、输出与文件位置表，逐图列出14张唯一图的 `source_id`、`target_id` 和最终结果，并用一条真实链路说明候选如何进入对象记忆。
- 明确 SAM3 只加载1次、10张图各编码1次并执行18次文字查找；说明 `target_id` 不会直接保存到 `proposal_id`，以及第2批和 SAM3 阶段分别损失了什么。
- 压缩审计、里程碑、关键判断和实验历史，移除重复的原则性说明；同步管理摘要和版本号。本批次未修改业务算法，未在本地运行模型、Demo 或单元测试。

## v0.17.2 — 2026-08-02 13:19:42 +08:00

- 修正 `--validate-demo` 的结构验收：不再要求正式实验必须出现 filtered/ignored 候选；该分支改为非门槛的 `demo_observations` 记录，干净候选集可以正常通过结构检查。由于报告字段结构变化，运行报告 schema 升至5。
- 新增 Demo coverage 回归测试：覆盖零 filtered/ignored 仍通过、四个必需路径各自仍会阻断、分支观察不影响结果，以及流水线原始失败不会被 coverage 覆盖。
- 新建 `docs/05_后续优化清单.md`，用稳定 ID 分类保留用户明确延后项、用户记录的后续想法、当前接受权衡、有证据但待定优先级的研究假设、验证覆盖边界和历史不处理项；版本更新不再自动清除这些记录。
- 按用户最新决定，当前 `new/existing` 直接保存 observation 为接受行为，不再将低信息观测列为本阶段验收失败。同步修正 AGENTS、PROGRESS、服务器环境说明、业务验证指南和版本号；本地未运行单元测试、模型或 Demo。

## v0.17.1 — 2026-08-02 01:30:25 +08:00

- 拉取并审计 v0.17.0 第二次真实服务器实验 `run_20260801T161221190913Z`：28个输入正确去重为14张唯一图，首轮严格4批4次且无重试/拆单；`with` 已真实放行，但第二批因两个具体提示各有7词、超过本地6词上限而使4张图整体失败。
- 完成隐藏真值与逐图视觉审计：10张成功图的18个合法提示只产生12个 SAM3 候选，后处理过滤0个；12/12候选均为真实目标并正确聚合为水瓶、鼠标、饮料杯3个对象，无背景污染或身份错并/拆档，但最终视图覆盖仅12/24，鼠标仅3/10，一个重遮挡局部仍被长期保存。
- 完成报告、SQLite 与资产审计：数据库完整性、外键、哈希、关系和102个文件均闭环；外部/内部报告逐字节一致。12条 observation 重复复制36组 proposal 资产2,955,257字节，占记忆根40.17%。
- 记录报告语义问题：`images_analyzed=10` 只统计成功解析图，Qwen 实际看过14张；failed source 仍可让 `every_processed_source_has_scene_guidance=true`；没有 filtered/ignored 会使旧 `--validate-demo` coverage 门槛失败，即使候选本身更纯净。
- 确认服务器附件中的单元测试不是有效回归结果：命令运行在 `(base)` Python 3.10，7个模块因缺依赖或源码路径导入失败。业务指南改为先激活项目 Conda 环境，并使用显式 `PYTHONPATH="$PWD/src"` 执行测试。
- 重写 `PROGRESS.md` 当前实验与审计，更新 AGENTS 管理摘要、服务器环境说明、数据根契约和版本号；完整保留 `data/memory/` 与 `data/memory_v017_01/` 两次失败证据。本批次未修改业务算法，也未在本地运行模型、Demo 或单元测试。

## v0.17.0 — 2026-07-31 22:00:21 +08:00

- 按用户明确决定确立“实验故障透明、减少过度防御性设计”准则：实验失败作为定位证据直接暴露，未经真实事实和用户授权不预设自动重试、拆单救援、静默规范化、降级或回退；事务完整性、审计写盘和显式人工复核继续保留。
- 从源头修正 Qwen→SAM3 文本契约：`with` 可描述同一完整物体的可见特征，例如 `water bottle with handle`，不再被误报为泛化词；首轮提示升级为 `robot-scene-guidance-v2`，同时强调不得用 `with` 连接不同对象，并在附属部件最显眼时仍命名完整对象。
- 删除 `max_error_attempts` 配置、首轮批次重试与逐源图救援、第二轮候选判断重试；每个首轮批次和每张第二轮源图只调用一次 Qwen，推理、格式或协议错误均保存原始结果并直接失败。配置 schema 升至3。
- 运行报告 schema 升至4：删除 rescue、attempt 和 accepted/attempt-list 字段，改为单值 `raw_response`、`errors` 和实际 `qwen_calls`；首轮与第二轮每个逻辑范围只保存一份 `response.json`，并显式记录无自动恢复的 Qwen 错误策略。
- 将回归测试改为验证 `with` 合法短语原样通过、两轮非法响应各只调用一次并直接失败、单份原始响应可审计，以及配置中不再存在自动重试项；同步 README、业务指南、AGENTS、PROGRESS 和版本号。
- 保留 v0.16.1 失败实验的 `data/memory/`、外部报告和历史审计原样不动；本批次未产生新实验结果。本地按项目约定不运行单元测试、模型或 Demo，只完成静态检查，等待服务器验证。

## v0.16.2 — 2026-07-31 21:06:57 +08:00

- 拉取并审计服务器正式实验 `run_20260731T110624049759Z`：28个输入正确去重为14张唯一图，但只有3张完成、11张在首轮 Qwen→SAM3 文本 schema 处失败；最终形成6个候选、6条观测和2个对象，外部报告为 `failed`。
- 定位主故障为协议而非 SAM3：具体短语中的 `with` 被误报为“too generic”，一个非法提示使整个4图批次作废；4个主批次全部失败后拆成14张单图救援，首轮实际调用33次。15次无反馈重试逐字复现原响应，额外消耗352.486秒、38,810 input token和5,420 output token。
- 完成隐藏真值与逐图视觉审计：进入 SAM3 的6个候选均为水瓶或鼠标，背景污染0/6且两对象在受限子集内未错并/拆档；但饮料杯完全漏档，白/灰鼠标卡片被末次响应错误覆盖成粉色，低信息遮挡视角仍被长期保存。
- 完成 SQLite 与资产审计：完整性和外键检查通过，53个唯一数据库资产路径及36份原始响应引用均存在；18份 observation 图像与 proposal 逐字节重复1,239,065字节，占记忆根31.33%。外部与内部报告逐字节一致。
- 重写当前实验看板并同步管理摘要、README、数据契约、服务器环境说明和运行指南；明确当前失败记忆根应整体保留，新 run 不能接管其中11个未完成哈希，修复后必须使用独立空记忆根复验。
- 本批次只进行服务器产物的只读分析、文档和版本维护；未修改业务代码、配置、依赖或 SQLite schema，未在本地运行模型、Demo 或单元测试。服务器提交未携带现行单元测试日志。

## v0.16.1 — 2026-07-31 18:44:43 +08:00

- 按用户明确授权清理旧实验记录：删除 `data/memory/` 中423个文件（约27.6 MB），包括 SQLite、14张规范源图副本、72组候选资产、59条对象观测资产、14份 Qwen 原始响应和内部运行报告；同时删除外部 `environment/demo_run_report.json`。
- 完整保留 `data/input/` 的28个固定输入文件；默认记忆根现在未初始化，下一次正式运行会创建全新数据库和资产，不再受旧哈希或对象档案影响。
- 将 `PROGRESS.md` 从旧实验的完整当前实录压缩为历史摘要，保留28→14→3584→72→59→8链路、主要审计失败和 Git 恢复点；同步更新 AGENTS、README、数据契约和运行指南，不再把已删除结果描述为当前输出。
- 本批次只涉及旧产物清理、文档和版本同步；未运行本地模型、Demo 或单元测试，完成路径、输入保留、Git 删除范围、Markdown 与版本一致性检查。

## v0.16.0 — 2026-07-31 18:35:46 +08:00

- 将正式业务链升级为两个逻辑 Qwen 阶段：SHA-256 幂等门后默认每4张新图进行一次机器人场景观察，输出受约束的独立物体目标和 SAM3 英文文本概念；SAM3 后仍按源图完成候选有效性、结构化标注、`new/existing/ignored/uncertain` 与递增对象记忆判断。
- 新增首轮专业提示、Pydantic schema、精确 `source_id` 覆盖、概念词法约束、有限重试和批次失败后的单图救援；不静默回退点网格。第二轮明确上游概念只是可错的检索假设，并在 crop/overlay 之后才展示该元数据，降低确认偏差。
- 将 SAM3 适配器改为同图只编码一次、逐文本概念调用高层 processor 并保留 prompt 来源；文本阈值从历史点网格0.88改为0.5起始值。脚本继续执行面积与高 IoU 过滤，同概念才做包含过滤，并先为每个有检测的概念保留最高分 mask，再按全局分数补足上限。
- 运行报告升级为 schema v3，分别记录两轮 Qwen 的调用、token、耗时、加载和原始响应；运行异常也计入实际请求次数，全部尝试响应均可导航，原始响应写盘失败会阻止结果继续。SAM3 统一读取哈希后保存的规范 source，避免输入文件中途变化造成阶段不一致。
- 更新 README、数据契约、服务器环境说明、运行指南、AGENTS 与实验看板；完整保留 v0.15.2 的3584→72→59→8失败实验，不把尚未服务器运行的 v0.16.0 代码改写成实验结果，并记录召回上限、概念宽窄、4图批次、双次 Qwen 驻留、固定物排除和确认偏差等 trade-off。
- 本地仅完成 Python 编译检查、差异格式检查和静态一致性审查；按仓库约定未运行单元测试、真实模型或 Demo，等待服务器在新空记忆根执行验证。

## v0.15.2 — 2026-07-31 16:45:02 +08:00

- 记录尚未实现的记忆存储优化判断：候选有效且归入已有对象，不代表必须新增长期 observation；应进一步判断新视角、外观、状态或身份信息增量。
- 明确建议的资产边界：对象记忆优先保留差异化 `crop`，`overlay` 在 proposal 审计层保留一份并由观测引用，避免在对象目录重复复制；当前实现仍保持每条 `new/existing` 复制 `crop/mask/overlay`，等待后续实验验证。

## v0.15.1 — 2026-07-31 16:22:33 +08:00

- 将 `PROGRESS.md` 重构为“实验看板与审计分析”：按28个输入、14个唯一源图、3584个原始候选、3512个脚本过滤、72组候选、59条接纳观测和8个对象的真实链路解释本次实验，再分别给出机器一致性审计、产品真值审计、问题定位、里程碑、当前关键判断和实验历史。
- 补齐运行 ID、时间、配置摘要、模型与提示版本、逐图候选/卡片/决定、候选与观测资产、对象级污染和性能数据，并明确输入场景、候选组、接纳观测及对象档案之间的数量边界。
- 调整管理分工：`PROGRESS.md` 只维护已发生实验、审计与实验史，不再维护未来计划；`AGENTS.md` 维护当前协作方向，`CHANGELOG.md` 维护已提交版本，并同步 README、数据契约和运行指南中的职责引用。
- 本地只执行报告、SQLite、资产计数、历史证据路径、Markdown 差异和文档一致性检查；按仓库约定不运行模型、Demo 或单元测试。

## v0.15.0 — 2026-07-31 15:40:31 +08:00

- 按用户决策完成最终 Demo 仓库收尾：删除历史 gate、smoke、M1–M5 分阶段验证脚本与报告、整个 `runs/`、重复暂存数据、候选预览包和所有 `.ipynb_checkpoints/`；Git 历史继续保留可追溯记录。
- 将固定输入迁入 `data/input/`，将当前完整端到端输出迁入默认 `data/memory/`，将外部结果报告改为 `environment/demo_run_report.json`；数据库和报告内容保持原样，未修改当前实验结果。
- 保留直接覆盖现行实现的单元测试，并从 M1–M5 阶段文件名改为配置存储、候选处理、身份判断、记忆事务和端到端编排等功能名称。
- 从当前配置和 SAM3 适配器移除只为旧 M2 复现保留的显式类别提示分支，最终业务链只保留无外部类别词的自动点网格策略；提示版本改为稳定的功能命名。
- 精简 README：删除内容导航、项目一览、长篇项目简介、独立第一阶段目标与边界、核心模块和文档导航；将一句话愿景放在开头摘要后，将系统架构移到当前实现之前，并依据最终保留框架重写数据、运行和项目结构。
- 将环境与业务指南收敛为当前搭建、正式入口和输入输出验证说明；删除所有历史里程碑命令与失效路径。
- 正式入口去除阶段命名：新运行使用 `--validate-demo`、`demo_coverage` 和 `object_memory_demo_batch`；已保留的原始报告不回写。
- 本地按仓库约定只执行结构、引用、Git 和数据库只读检查，不运行模型或单元测试；真实回归仍由服务器执行。

## v0.14.7 — 2026-07-31 15:15:06 +08:00

- 拉取服务器提交 `2cda2e0`，完整纳入715个 `data/` 与 `runs/` 文件及 `environment/m5_end_to_end_report.json`；本地只读核对报告、SQLite、隐藏真值、对象观测和过程资产，没有删除或重跑模型。
- 确认端到端链路的结构检查、14对重复图片哈希跳过、数据库完整性和全部资产引用通过，但产品语义验收失败：3个真实目标最终形成8个对象，出现跨实例错误合并、同实例拆档、鼠标档案污染和5个纯背景/固定装置对象。
- 固化本轮规模与性能基线：14张唯一图片、3584个候选、72个进入 Qwen、59个观测、14次 Qwen 调用；总耗时约25分48秒，其中 Qwen 推理约24分11秒。
- 完成 `data/`、`runs/` 实际目录盘点并形成保留/清理分类；当前不删除任何产物，失败验收库继续作为证据，待用户确认后再清理 `.ipynb_checkpoints`、重复 `data/temp`、旧 gate 和其他历史遗迹。
- 修正验收文档对 `--validate-phase-one` 的解释：顶层 `passed` 仅证明路径覆盖和结构闭环，不能代替逐图真值与对象身份审计；本次报告也不包含可证明单元测试已执行的证据。

## v0.14.6 — 2026-07-31 14:47:34 +08:00

- 按用户明确决策撤销对 `data/`、`runs/` 及 SQLite 文件的 Git 忽略：当前 Demo 仅二十余张图片，先完整同步输入、数据库、中间处理图、原始响应和阶段验证产物，再由本地审计清洗。
- 新增 `runs/README.md` 作为验证产物入口；服务器快照拉取后再依据真实目录补齐来源、依赖和去留结论，不凭名称猜测。
- 更新 README、AGENTS、PROGRESS 及服务器运行指南，使数据版本管理和回传命令与新决策一致；模型权重、缓存、Conda 环境和根目录隐藏真值继续忽略。
- 当前提交只开放跟踪边界，本地仍未获得服务器 `data/` 与 `runs/` 快照，尚不能解释或删除其中历史遗迹；下一步由服务器提交两目录后再拉取分析。

## v0.14.5 — 2026-07-31 14:38:21 +08:00

- 新增可由 Git 同步到服务器的 `data/README.md`，逐项解释 `memory`、`smoke`、M5 输入、历史 gate 记忆库、当前验收库和未知 `temp` 的用途、证据边界与保留策略。
- 说明每个记忆库中的 `memory.sqlite`、`sources`、`proposals`、`objects`、`raw_responses` 和 `run_reports`，并明确数据库与资产必须按完整记忆库管理。
- 将 Git 边界调整为只跟踪 `data/README.md`，继续忽略真实输入、SQLite、图像资产和内部运行产物；精选的机器可读验收报告仍通过 `environment/*.json` 管理。
- 根 README 增加服务器数据工作区、命名模式和目录说明链接；业务验证指南同步引用该契约，避免在服务器上仅凭目录名推测用途。
- 服务器截图只能证明 `m5_input` 和 `m5_validation_memory_01` 已生成，尚无端到端报告，故 M5-B 继续保持进行中。本批次不移动、删除或提交任何运行数据。

## v0.14.4 — 2026-07-31 14:29:23 +08:00

- 重写 README 的信息架构与全文，使其回归专业项目介绍职责：围绕项目背景、愿景、第一阶段边界、当前实现、系统架构、核心 workflow、模型与实验环境、数据存储、运行入口和目录导航展开。
- 从 README 移除当前版本、里程碑状态、进展摘要、下一动作、AI 强制规则、文档维护流程、Git/push 和版本规则，避免对外介绍与项目管理混杂。
- 扩充 `AGENTS.md`，集中承接阅读顺序、事实源、当前管理摘要、文档职责矩阵、长期开发准则、本地/服务器分工、脚本与依赖、Git 协作、版本规则和维护检查单。
- 明确 README 面向展示与新人，AGENTS 面向 AI 协作与项目管理，PROGRESS 是当前执行状态的唯一详细事实源，CHANGELOG 只记录已提交历史。
- 本批次只重构文档职责和项目介绍，不修改业务代码、模型配置、依赖或 SQLite schema。

## v0.14.3 — 2026-07-31 14:11:09 +08:00

- 将本地 `temp/` 加入 Git 忽略，并把28张验收图片重命名为无语义的 `img_0001.jpg`～`img_0028.jpg`，文件名不暴露对象、视角或重复关系。
- 逐图建立本地隐藏真值：14个唯一 SHA-256、14张逐字节副本，以及同一水瓶、同一鼠标、同一饮料杯3个具体实例；饮料杯吸管明确作为配件而非独立对象。
- 验证每个哈希组恰好包含一张基准图和一张副本，真值表28行完整覆盖；图片与 `temp/ground_truth.md` 均保持本地忽略，不进入 Git 或模型输入。
- 将下一动作更新为只上传 `temp/img_*.jpg` 后执行完整 M5-B；同时记录当前集合不覆盖“同一细分类别的两个不同实例”和“独立纯无效场景”，不得扩大验收结论。
- 本批次不修改业务代码、模型配置、依赖或 SQLite schema。

## v0.14.2 — 2026-07-31 13:56:00 +08:00

- 按用户决定将 `grid_point_000006` 的配件误建档保留为第一阶段已知非阻塞限制：继续记录此类假阳性，不调整提示、SAM 阈值、模型调用或部署方案。
- 取消单图 gate v2 修复与复验计划；首次批量 gate 以“结构通过、6/7语义结果和限制已记录”结束，历史报告保持不变。
- 将 M5-B 下一动作切换为10～20张多视角完整验收，重点验证真实对象卡片下的 existing 归并、同类不同实例分离、哈希跳过和失败闭环。
- 同步 README、PROGRESS、服务器风险说明和运行指南；下一份服务器证据直接使用 `environment/m5_end_to_end_report.json`。
- 本批次只更新项目决策和进展管理文档，不修改业务代码、依赖或 SQLite schema。

## v0.14.1 — 2026-07-31 13:48:03 +08:00

- 拉取并审计服务器单图批量 gate：7个候选只调用1次 Qwen，批量 schema、完整覆盖、顺序驻留与持久化结构通过，无 pending/failed。
- 拒绝用报告顶层 `status=passed` 代替产品验收：逐候选精确结果仅6/7，`grid_point_000006` 被当作“插入杯盖中的吸管”错误新建对象，M5-B 继续保持进行中。
- 固化本轮性能证据：9712 input token、1675 output token、109.945秒、峰值显存13530.58 MiB；报告为空记忆，尚未覆盖真实对象卡片下的 existing 归并，且未携带单元测试结果。
- 新增轻量 `AGENTS.md` 作为仓库 AI 协作入口，并同步 README、PROGRESS、环境风险和 M5 运行指南；下一轮使用新记忆目录与 `m5_image_batch_gate_report_v2.json`，不覆盖历史报告。
- 本批次只更新项目管理和验证职责文档，不修改模型逻辑、依赖或 SQLite schema。

## v0.14.0 — 2026-07-31 13:22:32 +08:00

- 将 Qwen 调用单位从“逐候选两阶段”改为“每张源图一次批量判断”：同图全部保留候选、全部 active 对象卡片及其最近参考图共同进入一次输入。
- 移除脚本端类别/属性/描述相关度计算和 Top-8 shortlist；脚本只整理输入、校验整批候选覆盖及 existing 对象 ID 范围，不代替 Qwen 做语义筛选。
- 新批量 schema 强制逐候选输出有效性、临时结构化标注、最终决策和比较后最终标注；ignored 不得携带标注，valid 必须同时保留临时与最终标注。
- `new` 使用最终标注建档；`existing` 在追加观测的同一事务中用最终累计标注更新对象卡片。SQLite schema 和依赖均未变化。
- 报告 schema 升至 v2，改为按源图记录候选数、全部卡片 ID、参考图数、批量调用次数及原始整批响应；输出上限提高到4096 token，以容纳多候选 JSON。
- 更新 M3/M5 确定性测试、真实验证脚本、README、PROGRESS 和运行指南；本地按项目约定未运行测试，等待服务器执行单图7候选批量 gate。

## v0.13.0 — 2026-07-31 12:44:25 +08:00

- 按新的对象记忆策略重构 Qwen 主流程：第一阶段只判断候选有效性并生成临时标注，不接触对象卡片；无效候选直接 `ignored`。
- 对有效候选先用一次 SQLite 查询读取全部卡片文字，按类别、材质、颜色、形状和描述排序；只为默认前8张 shortlist 查询参考图路径并交给第二阶段 Qwen 做实例身份确认。
- 空记忆中的有效候选直接 `new`；有记忆时第二阶段只允许 `new/existing/uncertain`，禁止把“与旧卡片不同”再次解释为 `ignored`。
- 报告新增候选分析、是否查询记忆、全部卡片数、shortlist 分数、身份确认结果及两阶段 Qwen 调用计数；原始响应明确标记 analysis/identity 阶段。
- 用两阶段 schema、提示、检索排序及编排回归测试替换旧的全库固定分批接口；依赖和 SQLite schema 均未变化，等待服务器真实同图 gate 验证。

## v0.12.1 — 2026-07-31 11:01:55 +08:00

- 验收 Qwen gate v2：新记忆目录正常处理图片、无哈希跳过；mask 隔离 crop 修复有效，原先误建档的阴影 `grid_point_000100` 已正确返回 `ignored`。
- gate v2 仍未通过最终语义门槛：Qwen 已认出 `grid_point_000067` 是饮料杯，却以“不是已有鼠标”为由返回 `ignored`，把对象卡片不匹配错误当成候选无效。
- 提示升级至 v4，强制候选有效性独立于对象卡片判断；完整物体与所有已有卡片均不匹配时必须为 `new`，不能为 `ignored`。继续保持每候选/卡片批次单次调用。
- 运行指南解释 `memory_02`、`memory_03` 只是自动创建的隔离实验记忆目录编号，并给出 gate v3 的新目录与报告命令。

## v0.12.0 — 2026-07-31 10:45:44 +08:00

- 验收 Qwen gate v1：结构状态通过、3 new/4 ignored 且无错误，但逐候选审查发现一个真实物体被忽略，同时一个阴影被误建成“白色红盖饮料容器”，因此语义门槛未通过。
- 定位原因为候选 `crop.png` 仍是 mask 外接矩形原图，阴影候选的矩形中包含邻近杯子；仅看 new/ignored 总数会掩盖这种语义交换错误。
- 将候选 crop 改为 mask 隔离图：只保留 mask 内原始像素，外部填充可配置的中性灰；不增加图片或 Qwen 调用，overlay 继续提供位置上下文。
- Qwen 提示升级至 v3，明确只判断隔离 crop 的保留像素，并将阴影、反射、纹理和无效局部判为 ignored；新增像素与提示回归测试及同图 gate v2 命令。

## v0.11.1 — 2026-07-30 19:56:37 +08:00

- 扩展 M5 运行报告的逐候选审计信息：记录原始候选编号、来源、分数、bbox、面积比例及 crop/mask/overlay 路径。
- 将 Qwen 的理由码、简短理由和结构化标注直接写入逐图决策；失败项保持相同字段结构，便于仅凭 Git 报告检查阴影是否被 `ignored`。
- 增加 M5 编排回归断言，并同步 Qwen 单场景接管的审查说明；用户无需再次逐个口述候选文件。

## v0.11.0 — 2026-07-30 19:52:24 +08:00

- 验收 M5-A v2：全部自动检查通过，256 个原始候选最终保留 7 个，3/3 个真实物体仍存在；15 个大区域和2个包含式局部被过滤，候选推理 7.183 秒、峰值显存 5390.59 MiB。
- 根据用户视觉确认关闭 M5-A，并把阶段推进到 M5-B；少量剩余阴影先交给 Qwen 的原有单次有效性判断，不增加第二次调用。
- 增加单场景 Qwen 接管门槛：先验证3个真实物体建档、阴影/局部 `ignored` 和无 pending/failed，再投入10～20张多视角最终批量验收。
- 将更进一步的阴影前置过滤记为非阻塞方向：先收集多场景 `ignored` 比例与耗时证据，避免按单张图片调整阈值。

## v0.10.3 — 2026-07-30 19:46:06 +08:00

- 定位服务器三项 M5 回归失败为测试夹具冲突：原假候选 mask 占整图 64%，被新设的 50% 大区域规则正确过滤，因而没有进入 Qwen。
- 将 M5 编排测试的假候选调整为占图 25% 的普通对象区域，并让重复候选分数高于置信度门槛，使测试继续验证 Qwen 编排和 mask 去重，而不回退生产过滤规则。

## v0.10.2 — 2026-07-30 19:41:10 +08:00

- 验收首次 M5-A 自动候选报告：16×16 点网格产生 256 个原始候选并保留 12 个，3/3 个真实物体均被覆盖；人工确认其余为 3 个阴影、2 个物体局部和 4 个桌面候选。
- 将默认最大 mask 面积比例从 0.8 收紧至 0.5，并新增较低分 mask 被更完整候选覆盖至少 90% 时的局部过滤，分别针对大面积桌面和物体局部噪声。
- 保持 Qwen 每候选单次组合判断，不用贴合单张图片的置信度切点过拟合；无法安全由几何规则排除的阴影继续由 Qwen 判为 `ignored`。
- 新增包含 mask 的确定性测试，并把当前状态、风险和同图 M5-A v2 复验命令同步到职责文档。

## v0.10.1 — 2026-07-30 19:15:12 +08:00

- 明确 `scripts/run_object_memory.py` 是第一阶段端到端实验总入口，`verify_m*.py` 仅承担里程碑验证和问题定位。
- 将 M2 显式类别提示标为历史验收接口，并把当前无类别提示主流程统一指向 M5 总入口，避免旧描述被误认为现行方案。
- 固化文档随真实实现同步的维护边界：直接替换当前文件中的过时描述，不回写初始规划书、历史报告和 CHANGELOG，也不重复堆叠多份现行说明。

## v0.10.0 — 2026-07-30 19:04:42 +08:00

- 将 M5 主流程从外部类别提示改为无类别提示的自动点网格候选：项目脚本自动铺点，SAM3 批量生成 mask，用户不再传入 `cup`、`mug` 等参数。
- 增加候选质量、最大面积和每图数量限制，并让 Qwen 明确区分“自动候选来源”与“类别标签”，自主过滤背景、物体局部、粘连区域和无意义碎片。
- 保留历史 M2 文本提示复现能力，同时让现有 SAM3 验证脚本可在不传 `--prompt` 时独立校准自动候选，不加载 Qwen 或写入正式记忆库。
- 将 M5 拆分为 M5-A 自动候选服务器校准和 M5-B 完整批量验收；更新无提示 workflow、数据准备、服务器命令、能力边界和回传规则。

## v0.9.0 — 2026-07-30 18:33:26 +08:00

- 验收服务器 M4 报告：新建、归并、哈希幂等、filtered/ignored/uncertain/failed、重试、事务回滚、对象卡片和运行摘要十项检查全部通过。
- 实现 M5 单脚本真实批量入口：递归读取图片、按哈希跳过重复、一次加载 SAM3 处理全批次、释放后加载 Qwen，并持续写入 SQLite。
- 每个候选使用当时最新的对象卡片；正常 uncertain 保持 pending，不立即重复调用，只有推理或结构解析异常才执行默认最多两次的有限重试。
- 新增逐图决策、原始响应、token、耗时、显存、核心表计数与阶段覆盖报告，以及不加载真实模型的 M5 编排回归测试。
- 固化 10～20 张 M5 验收图片的组成、独立验收记忆库、显式运行命令和同物体/不同实例对象 ID 复核标准。

## v0.8.0 — 2026-07-30 18:13:38 +08:00

- 验收服务器 M3 提示 v2 报告：空记忆正确新建并标注白色陶瓷咖啡杯，同一视图以 1.0 置信度匹配回正确对象 ID，全部检查通过。
- 实现 M4 SQLite 记忆闭环：按 SHA-256 跳过已完成图片，事务化写入 new/existing/ignored/uncertain，并记录 filtered 与 failed 候选。
- 新增 pending proposal 的递增 attempt 重试、非法 existing 引用回滚、对象观测资产复制清理、SQLite 对象卡片查询和 run 汇总。
- 新增确定性 M4 单元测试与显式服务器入口 `python scripts/verify_m4.py`；不加载模型、不修改正式记忆库，真实批量编排留到 M5。

## v0.7.4 — 2026-07-30 17:40:23 +08:00

- 将 M3 提示升级至 v2，保持每个候选、每个对象卡片批次一次 Qwen 推理，并在同一 JSON 中输出最终决策与当前标注。
- 明确 IMAGE_A 候选 crop、REFERENCE_IMAGE 对象参考图和 IMAGE_Z 场景 overlay 的角色与顺序；禁止使用 overlay 的着色判断身份差异。
- 强制单次调用内部按候选有效性、实例身份、当前标注的顺序处理；同一图像或相同显著细节必须作为强实例证据，证据不足时使用 uncertain 而非臆造差异。
- 在业务指南中固化 M3 输入输出接口、卡片分批汇总和 M4 对象/观测/决策记录 workflow，并补充图片映射与单次调用测试。

## v0.7.3 — 2026-07-30 17:17:51 +08:00

- 删除环境运行指南中重复的服务器状态和路径清单，明确 `docs/02_服务器环境分析.md` 是路径事实的唯一维护位置。
- `docs/03_服务器环境与冒烟测试指南.md` 只保留可执行操作并引用 `docs/02`；命令里的路径不再被视为第二份事实源。

## v0.7.2 — 2026-07-30 17:07:05 +08:00

- 在服务器环境职责文档中集中固化仓库、Conda、SAM3、Qwen 缓存、数据、SQLite、运行产物和报告的当前确认路径。
- 新增长期准则“事实优先，禁止臆测”：环境相关开发必须先读职责文档、最新报告和已验证脚本，禁止用框架默认值或经验假设替代项目事实。
- 明确信息冲突或证据不足时应先核查、标注不确定性并在必要时向用户确认，不得擅自安装、下载或继续假设性开发。
- 同步检查服务器并纳入 M3 第二次报告：缓存路径已修正且新对象标注通过，但同一视图对象卡片仍被误判为新对象，当前问题更新为身份提示或多图映射质量。

## v0.7.1 — 2026-07-30 17:01:38 +08:00

- 检查 M3 首次服务器报告：验证在 Qwen 加载前失败，未产生语义、身份判断、耗时或显存结果。
- 定位原因为新终端未指向项目已有的 `weights/qwen/hub`，脚本误查系统默认 Hugging Face 缓存；并非模型或测试数据缺失。
- 修正 M3 运行指南，显式设置现有项目模型缓存路径；无需重新安装、下载或添加数据。

## v0.7.0 — 2026-07-30 16:52:45 +08:00

- 验收服务器回传的 M2 报告：2 个跨提示候选正确去重为 1 个，源图关联和 crop/mask/overlay 十项检查全部通过。
- 实现 M3 Qwen 对象判断流水线：使用候选 crop、overlay、SAM 类别提示和分批对象卡片，输出受约束的身份决策与可见事实标注。
- 新增 schema、理由码和对象 ID 范围校验，以及低置信/批次冲突保守降级；实际数据库写入继续留在 M4。
- 新增显式入口 `python scripts/verify_m3.py`，复用 M2 资产验证空记忆新建和同一视图回匹配，无需重复安装项目。

## v0.6.1 — 2026-07-30 16:44:16 +08:00

- 固化后续显式脚本入口范式：面向用户统一使用 `python scripts/<脚本名>.py [参数]`。
- 明确 Git 只负责同步代码，依赖安装仅在首次配置或依赖变化时单独说明；现有 M1/M2 入口不返工。

## v0.6.0 — 2026-07-30 16:17:58 +08:00

- 实现 M2 显式提示列表 SAM3 适配器：复用单图特征、逐类别推理并输出 SDK 无关的 CPU 候选。
- 新增置信度/面积过滤、跨提示 mask IoU 去重、过滤原因和 crop/mask/overlay 原子写入。
- 将 proposal 审计字段加入 SQLite schema v2，提供 v1→v2 迁移、确定性单元测试和真实 GPU 验证报告入口。
- 明确当前候选策略只覆盖配置类别，不把它描述为开放场景穷举；M2 等待服务器报告后验收。

## v0.5.1 — 2026-07-30 16:06:05 +08:00

- 验收服务器回传的 M1 数据骨架报告：六项检查全部通过，六张核心表为空且可查询。
- 完成 M1 并把当前关卡推进到 M2 SAM3 流水线，明确适配器、后处理、图像资产和候选策略的进入条件。

## v0.5.0 — 2026-07-30 15:57:52 +08:00

- 实现 M1 数据骨架：集中配置、六类 Pydantic 数据模型、SQLite 六张核心表与安全相对路径规则。
- 新增可重复执行的 `object-memory init/status` CLI、标准库回归测试和服务器 M1 验证报告入口。
- 增加业务代码运行与验证指南；M1 保持进行中，等待 AutoDL 报告后验收。

## v0.4.2 — 2026-07-30 14:34:34 +08:00

- 固化第一阶段的通俗摘要目标、能力边界和完成定义。
- 在 README 补充从图片去重到对象记忆写入与运行摘要的端到端规划 workflow。

## v0.4.1 — 2026-07-30 13:16:09 +08:00

- 为进度看板增加单一最后更新时间，便于新对话判断状态新鲜度。
- 将服务器指南职责收窄为环境重建与模型冒烟复现，避免与 PROGRESS 的任务管理职责重叠。

## v0.4.0 — 2026-07-30 11:45:34 +08:00

- 确认双模型统一顺序入口和最新环境自检通过，完成 M0 并进入 M1 数据骨架。
- 固化 Qwen 13.32 token/s、GPU 0 全量放置和数据盘剩余 23.45 GiB 等实测事实。
- 将磁盘空间控制、全量候选和对象语义风险分别路由到 M1、M2、M3。

## v0.3.6 — 2026-07-30 11:36:39 +08:00

- 固化 SAM3 冒烟的 proposal、score、耗时和显存证据。
- 为 Qwen 报告补充 token 数、生成速率和设备放置，以分析单图推理耗时。

## v0.3.5 — 2026-07-30 11:28:46 +08:00

- 确认 Qwen3-VL-8B FP8 本地推理与结构化 JSON 冒烟通过，推进到双模型顺序验证。
- 记录整图标注的对象语义风险，并让统一冒烟入口自动刷新环境报告和公共运行配置。

## v0.3.4 — 2026-07-30 11:15:55 +08:00

- 记录 Qwen 在 `local_files_only` 模式下仍未进入推理的最新证据。
- 将 Hugging Face 仓库 ID 显式解析为本地 snapshot 目录，彻底绕开推理阶段的仓库 API 检查。

## v0.3.3 — 2026-07-30 10:43:12 +08:00

- 将 PROGRESS 明确为唯一执行进度源，重构 README 为摘要、准则和职责导航入口。
- 按初始规划重新对齐 M1–M5 主线，并将已明确通过的 SAM3 冒烟标记为完成。
- Qwen 冒烟默认只读已下载的本地缓存，避免网络检查阻塞模型加载。

## v0.3.2 — 2026-07-29 20:42:47 +08:00

- 新增第一阶段持续进度看板，记录里程碑、证据、完成条件和下一动作。
- 记录 SAM3 具体类别提示已运行通过，并将下一关更新为 Qwen3-VL 冒烟。
- 移除误导性的 SAM3 `object` 默认提示，要求显式提供图像中存在的具体类别。

## v0.3.1 — 2026-07-29 20:36:40 +08:00

- 修复 SAM3 冒烟推理的 BF16/FP32 dtype 不一致，并同步 Qwen 下载与冒烟进度。

## v0.3.0 — 2026-07-29 19:45:33 +08:00

- 确认服务器基础环境无阻塞，并进入 SAM3/Qwen3-VL 模型冒烟阶段。
- 更新当前环境事实、执行入口和文档持续维护规则。
- 增强自检以验证 SAM3 真实导入并记录关键运行依赖。

## v0.2.4 — 2026-07-29 19:29:59 +08:00

- 补齐 SAM3 图像导入所需依赖，并固定兼容的 setuptools 与 OpenMP 线程配置。

## v0.2.3 — 2026-07-29 19:26:16 +08:00

- 固定 SAM3 所需的 setuptools 兼容范围，并关闭其独立安装步骤的构建隔离。

## v0.2.2 — 2026-07-29 18:28:18 +08:00

- 将环境配置拆分为 Conda、普通 Python 依赖和 SAM3 三个独立步骤，并统一 Qwen 缓存目录命名。

## v0.2.1 — 2026-07-29 17:53:11 +08:00

- SAM3 改用固定提交的官方源码包安装，并增加 pip 超时与重试容错。

## v0.2.0 — 2026-07-29 16:46:16 +08:00

- 新增可复现的服务器 Conda 环境声明和安装流程。
- 新增 SAM3 与 Qwen3-VL 独立冒烟测试及报告。
- 新增服务器执行指南并更新项目导航。

## v0.1.2 — 2026-07-29 15:49:50 +08:00

- 记录服务器存储清理结果和 SAM3 权重位置。
- 将下一步收敛为项目 Conda 环境创建与模型冒烟测试。

## v0.1.1 — 2026-07-29 14:49:59 +08:00

- 分析服务器环境报告并确定模型部署策略。
- 精简 README 环境摘要并增加详细分析导航。
- 增强环境自检的 CUDA、磁盘和目录告警。

## v0.1.0 — 2026-07-29 14:16:19 +08:00

- 建立第一阶段项目基线。
- 新增项目入口、长期开发准则和版本记录规则。
- 新增服务器环境自检脚本与模型选型基线。
- 将项目背景和设计规划调整为仅本地保留。
