# AGENTS.md

本文件是仓库内 AI 协作入口，只规定阅读和维护顺序，不复制项目状态。

1. 先读 `README.md`，再读 `PROGRESS.md`；当前阶段、完成判断和下一动作只以 `PROGRESS.md` 为准。
2. 再按任务读取对应职责文件、最新服务器报告和实现代码；不得从历史规划或 CHANGELOG 推测当前进度。
3. 本地只做审查、修改、提交和 push；深度学习脚本、测试与 Demo 只在 AutoDL 服务器运行。
4. 用户说明服务器已 push 时，先确认工作区安全并 pull，再依据拉取到的报告分析。
5. 代码、服务器结果或阶段状态变化时，同步维护 `PROGRESS.md`、README 当前摘要、相关职责文件和 `CHANGELOG.md`；不改写历史报告。
6. 每个完成的本地 AI 修改批次按 README 的版本与 Git 规则提交并 push；失败时明确报告。
