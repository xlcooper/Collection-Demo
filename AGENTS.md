# AGENTS.md

> 仓库内 AI 协作、项目管理和开发规范入口。本文不承担对外项目介绍；面向首次了解项目的读者请使用 `README.md`。

## 1. 必读顺序与事实源

每个新对话和每次继续开发必须按以下顺序建立上下文：

1. 阅读 `AGENTS.md`，确认协作规则、文档职责和当前管理摘要。
2. 阅读 `README.md`，理解项目背景、目标、架构、实验环境和已实现 workflow。
3. 阅读 `PROGRESS.md`，了解当前实验状态、最近正式实验的真实流程与审计结论、里程碑和仍生效的关键判断。
4. 涉及“当前不做、以后优化”的判断时阅读 `docs/05_后续优化清单.md`。
5. 按任务读取对应职责文档、最新服务器报告、配置和实现代码。

事实优先级：

- 当前实验状态、正式实验的运行事实、结果、审计结论和里程碑：以 `PROGRESS.md` 为详细事实源。
- 当前管理方向和下一协作动作：以本文件第2节管理摘要及用户最新明确决策为准。
- 用户明确延后项、记录的后续想法、待定研究假设、已接受权衡和历史不处理项：以 `docs/05_后续优化清单.md` 为唯一集中清单。
- 当前服务器环境和路径：以 `environment/server_env_report.json` 及 `docs/02_服务器环境分析.md` 为准。
- 当前固定输入以 `data/input/` 为准；仓库当前没有对象记忆根或 Demo 运行报告。v0.16.1、v0.17.0 和 v0.17.4 的旧产物已按用户授权删除，历史摘要见 `PROGRESS.md`，删除前完整快照可从 Git 提交 `f413654` 恢复。
- 可复现依赖：以 `environment.yml`、`requirements.txt` 和 `requirements-sam3.txt` 为准。
- 已提交版本历史：以 `CHANGELOG.md` 为准。
- 历史规划和旧报告只证明当时事实，不得据此推测当前状态。

如本文件对实验事实或验收结论的摘要与 `PROGRESS.md` 冲突，必须以 `PROGRESS.md` 为准并在同一维护批次修正；协作方向以用户最新明确决策为最高依据。

## 2. 当前项目管理摘要

- **版本**：v0.20.1
- **当前关卡**：M5-B——端到端对象记忆验收
- **当前状态**：最近一次真实实验仍是 v0.17.4，M5-B 未通过：28个输入去重为14张唯一图，首轮 Qwen 提出23个目标，但旧 `and` 规则使第1批4张图和7个目标停止；其余16条提示得到11个候选，最终保存3个对象ID和11条观测，只覆盖人工确认的24次物体出现中的11次，并有杯盖粒度和鼠标颜色错误。旧产物现已清理。v0.18.0 的源头修正仍未在服务器验证；v0.20.0 在现有 Web 实验台上新增多记忆库的新建、续写、复核和整库删除，分别展示计划用于 SAM3 的英文原文与已完成查询的检测数，并将首轮提示升级为基础类别优先且允许单个有用修饰词的 v5，同时重构结果摘要；这些代码同样尚未在服务器运行测试或新实验。v0.20.1 只重组 README 与文档职责、加入架构图并澄清 AutoDL 自定义服务访问边界，不改变算法或实验结论
- **当前协作方向**：服务器拉取 v0.20.1 后，若尚未安装 v0.19.0 新增的 Web 依赖则只更新一次 `requirements.txt`，随后执行全部单元测试并启动 Web 实验台；从页面新建一份空白记忆库，使用保留的 `data/input/` 完成全新实验，生成完整记忆根与 `environment/run_report.json` 后整体提交。收到结果后既核对记忆库新建、选择、续写/锁定/删除边界、Web 进度、英文 SAM3 原文、摘要和 SQLite 可视化闭环，也分别审计首轮目标、SAM3每条提示的召回、脚本候选、完整物体粒度、颜色判断和对象归并；在新证据前不得宣称提示优化或 Web 服务器运行已经验证有效
- **长期优化与权衡清单**：统一维护在 `docs/05_后续优化清单.md`。其中观测信息增量筛选已被用户明确延后；当前 `new/existing` 直接保存 observation 是接受行为，不作为本阶段验收失败项

详细实验流程、证据、验收门槛和数据覆盖边界必须查看 `PROGRESS.md`；未来动作不得从实验历史中推测，应以本摘要和用户最新明确决策为准。

## 3. 文档职责

| 文件 / 路径 | 唯一职责 | 维护要求 |
|---|---|---|
| `README.md` | 对外项目展示和新人入口：项目价值、简洁架构与原理、可见能力、能力边界、最小快速开始和文档导航 | 不复制详细实现、模型字段、数据树、当前实验数字或项目管理内容；不写 AI 指令、当前版本、里程碑、下一动作、Git 协作或维护流程 |
| `data/README.md` | 固定 Demo 输入与运行时记忆输出契约 | 数据布局或运行方式变化时更新；不记录实验完成结论 |
| `AGENTS.md` | AI 协作规范、项目管理入口、开发准则、当前管理摘要与协作方向、版本及 Git 规则 | 管理决策、协作方向或当前摘要变化时同步更新 |
| `PROGRESS.md` | 当前实验状态、正式实验执行实录、审计分析、里程碑、关键判断和实验历史 | 新实验或新审计证据到达时刷新当前实验并沉淀被替换实验；原始证据清理后保留压缩摘要和恢复边界；不记录未来计划或下一动作 |
| `docs/05_后续优化清单.md` | 用户明确延后项、记录的后续想法、待定假设、已接受权衡和历史不处理项 | 使用稳定 ID 持久保留；不因版本升级自动删除或重置，只在用户决策或实证状态变化时更新 |
| `CHANGELOG.md` | 已提交版本的历史时间线 | 只追加新版本，不承担当前计划 |
| `docs/02_服务器环境分析.md` | 最新服务器事实、路径、模型部署判断和环境风险 | 新环境报告回传后更新 |
| `docs/03_服务器环境搭建指南.md` | 当前服务器环境、依赖和模型准备方式 | 只维护可执行环境操作 |
| `docs/04_业务代码运行与验证指南.md` | 稳定算法流程、正式入口、参数、报告字段和验证方式 | 实现、入口或验证方式变化时更新；数据目录结构只概述并链接 `data/README.md`，不记录当前实验状态 |
| `environment/server_env_report.json`、新生成的 `environment/*_report.json` | 当前服务器与 Demo 机器证据 | 原始报告不回写；已清理的历史报告只从 Git 历史恢复 |
| `docs/00_项目纲要.md`、`docs/01_第一阶段项目设计规划书.md` | 原始背景、边界和初始设计依据 | 本地忽略，不作为当前状态源 |

每类信息只在对应职责文件中详细维护。其他文件可以链接或做稳定概述，但不得复制会频繁变化的状态表。

## 4. 长期开发准则

1. **实验与审计权威**：真实运行流程、实验结果、完成判断、里程碑和审计结论只在 `PROGRESS.md` 详细维护；当前管理方向和下一协作动作由本文件管理摘要维护。
2. **MVP 与奥卡姆剃刀**：优先完成满足当前目标的最小方案，避免额外扩展、过早抽象和过度设计。
3. **聚焦第一阶段**：当前只开发自动化对象记忆 Demo；提示词可以面向未来机械臂自主采集筛选值得观察的物体，但不实施主动采集、视角规划、机械臂控制、认知完整度或世界模型。
4. **Friendly 代码**：模块、脚本和逻辑必须职责清楚、命名直接、流程可读；优先短函数、显式类型、结构化模型输出和可操作错误信息。
5. **事实优先，禁止臆测**：环境、依赖、模型、缓存和路径相关设计必须先查对应职责文档、最新报告和已验证脚本。信息冲突或证据不足时先核查并明确不确定性。
6. **实验故障透明，减少防御性设计**：实验阶段的失败本身是定位问题的证据。未经真实实验事实证明必要且用户明确授权，不预设自动重试、拆单救援、静默修补或规范化、降级、回退等路径来追求表面成功；保存原始响应并直接暴露失败，优先修复已经确认的源头原因。事务完整性、审计写盘和显式人工复核不属于自动容错，继续保留。
7. **按事件维护**：新实验或新审计证据更新 `PROGRESS.md`；管理方向变化更新 `AGENTS.md`；稳定接口或数据契约变化更新对应职责文件；已提交版本变化追加 `CHANGELOG.md`。没有新实验时不得机械改写实验结论，也不回写历史报告或初始规划。
8. **延后项不丢失**：用户明确“先不考虑”或“记录为后续优化”时，必须在 `docs/05_后续优化清单.md` 新增或更新稳定条目，不能只写入当前版本摘要或 CHANGELOG。
9. **保留能力边界**：当前候选由 Qwen 首轮生成的文本概念驱动 SAM3；使用者无需给类别词，但首轮漏掉的物体不会进入 SAM3，文本分数也不同于历史点网格质量分。不得描述为已经证明可穷举任意场景所有物体。
10. **保守身份判断**：同类别、颜色或材质不能单独证明两个观测属于同一具体实例；证据不足时使用 `uncertain`。
11. **结构化与可审计**：模型输出必须通过 schema、候选覆盖和对象 ID 范围校验；原始响应、运行报告和持久化记录应可追踪。
12. **不扩大验收结论**：只依据真实数据覆盖和服务器证据声明能力；未包含的同类不同实例、纯无效场景或规模压力不得推断为已通过。

## 5. 本地与服务器分工

### 本地

- 用于人与 AI 协作、阅读、设计、修改、审查、版本管理和 push。
- 不安装深度学习环境，不运行模型脚本、单元测试或 Demo。
- 可执行只读检查、文档一致性检查、Git 检查和本地测试数据整理。

### AutoDL 服务器

- 运行全部单元测试、环境检查、真实模型推理和端到端 Demo。
- 使用项目内 Conda 环境，不长期使用 `base`。
- Qwen 必须显式使用项目数据盘中的 `HF_HOME` 和 `HF_HUB_CACHE`。
- 依赖没有变化时不得重复安装；模型权重和缓存不进入 Git。当前固定输入以及新运行生成的完整记忆输出和对应报告进入 Git，便于跨机器比较输入—输出效果。

项目运行环境统一由 Conda 管理。环境配置按可独立重跑的步骤拆分，不使用单个脚本串联全量安装。

## 6. 脚本与依赖规则

- 面向用户的运行入口统一采用 `python scripts/<脚本名>.py [参数]`。
- 第一阶段端到端总入口是 `python scripts/run_object_memory.py`。
- Web 实验台入口是 `python scripts/run_object_memory_web.py`；它必须单 worker、单实验运行，并通过正式端到端入口启动独立子进程，不得复制模型流程或开放任意命令。
- `scripts/check_server_env.py` 只检查环境，不替代正式入口。
- Git 同步代码、配置、文档、当前机器报告、`data/input/`，以及新运行生成的完整独立记忆根；依赖安装只在首次配置或依赖变化时单独执行。
- 不新增依赖 `pip install -e .` 才能调用的控制台快捷命令；现有历史入口不为此返工。
- 不提前引入 vLLM、SGLang、备用模型或可选 CUDA 扩展，除非真实证据证明当前 MVP 方案失败。

## 7. Git 与服务器协作

1. 用户说明服务器已 push 时，本地 AI 必须先检查工作区，再 pull。
2. 只依据拉取到的报告和代码分析服务器结果；不得根据口述补造指标。
3. 用户不负责维护 `CHANGELOG.md`；服务器提交范围按当前任务执行，新的效果比较应包含输入变更、完整新记忆根和对应报告。
4. 本地 AI 完成分析后，按信息职责更新文件：实验事实与审计进入 `PROGRESS.md`，协作方向进入 `AGENTS.md`，稳定运行或数据契约进入对应指南。
5. 每个完成的本地 AI 修改批次都必须提交并 push；push 失败时明确报告。
6. 当前阶段可提交 `data/input/`、独立命名且完整的新实验记忆根及对应机器报告，便于并排审计；不恢复历史 gate、smoke、`runs/` 或分阶段验证产物，仍不提交模型权重、缓存、项目内 Conda 环境和根目录 `temp/` 中的隐藏真值表。

## 8. 版本规则

- 每个本地 AI push 都在 `CHANGELOG.md` 顶部新增语义化版本、北京时间时间戳和简短摘要。
- 默认递增补丁版本；阶段目标或兼容性明显变化时递增次版本或主版本。
- `pyproject.toml`、本文件管理摘要和 `PROGRESS.md` 的当前版本必须一致。
- 同一对话中的关联修改作为一个版本批次。
- CHANGELOG 只追加历史；过时的当前描述应在职责文件中直接替换，不能靠追加说明保留多个“当前版本”。

## 9. 每轮维护检查单

开始前：

- 工作区是否干净，是否存在用户未提交改动。
- 是否已按顺序阅读 AGENTS、README、PROGRESS 和任务职责文件。
- 用户是否说明服务器已 push；若是，是否已先 pull。
- 实验结论是否有报告、数据库、资产或隐藏真值审计支持；协作方向是否来自用户明确决策。

完成前：

- 是否只修改了任务授权范围内的内容。
- `AGENTS.md` 的实验摘要是否与 `PROGRESS.md` 一致，且协作方向没有被误写成实验事实。
- 若没有新实验或新审计证据，是否避免机械改写 `PROGRESS.md`。
- 若用户明确延后某项优化，是否已在 `docs/05_后续优化清单.md` 持久记录。
- README 是否保持对外介绍职责，没有混入项目管理内容。
- CHANGELOG 和版本号是否更新。
- 是否完成适当验证，并明确哪些测试只能在服务器运行。
- 是否提交并 push，且工作区恢复干净。
