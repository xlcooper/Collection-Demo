# 实验看板与审计分析

> 本文用大白话记录当前实验处于什么状态、最近一次真实实验每一步发生了什么，以及审计后能得出什么结论。后续优化清单见 `docs/05_后续优化清单.md`。

- **当前版本**：v0.20.1
- **最后更新**：2026-08-03
- **当前关卡**：M5-B——端到端对象记忆验收
- **当前实验状态**：等待服务器验证多记忆库 Web 实验台并用新建空白库重新实验

## 1. 现在仓库里有什么

- `data/input/` 保留28个输入文件，其中14张内容唯一，另外14个是完全相同的副本。
- 之前三次实验的 `data/memory/`、`data/memory_v017_01/`、`data/memory_v017_02/` 和三份外部报告已经按用户授权删除。
- 下一次 Web 运行可创建全新的 `data/memory_<内部编号>/`（或选择兼容默认 `data/memory/`），并刷新 `environment/run_report.json`；当前两类记忆根都不存在。
- 删除前的完整历史结果仍可从 Git 提交 `f413654` 恢复；下文保留的是历史审计摘要，不代表这些文件现在还在仓库中。
- v0.18.0 已修改代码和提示词，但还没有服务器测试或新实验报告，因此不能把这些修改写成已经有效。
- v0.19.0 新增 Web 实验台、阶段进度事件、输入管理和 SQLite 记忆可视化代码；当前同样只有本地静态审查，没有服务器测试、Web 实际运行或新实验报告，因此不改变最近一次实验失败结论。
- v0.19.1 优化 Web 的项目介绍、模型与单卡推理配置展示、阶段用途说明和计数单位，并避免无变化轮询反复重建结果区域；该版本仍未在服务器验证，不改变最近一次实验失败结论。
- v0.20.0 新增 Web 多记忆库选择、新建、完整库续写、失败库只读复核和整库删除；目标规划页明确区分中文审计文本、计划用于 SAM3 的英文 `sam_text_prompt` 与已完成查询的检测数，首轮提示升级为 `robot-scene-guidance-v5`，允许 `transparent water bottle` 这类单个有用修饰词并强化瓶/杯基础类别判断；结果摘要改为本次增量、库内累计、运行错误和人工复核分层展示。该版本仍未在服务器验证，不改变最近一次实验失败结论。
- v0.20.1 只重组 README 与文档职责、加入架构图并澄清 AutoDL 自定义服务访问边界，没有修改模型、流程或实验数据，因此不改变任何实验结论。

## 2. 如何阅读实验记录

稳定算法流程、编号含义、运行参数和报告字段统一由[业务代码运行与验证指南](docs/04_业务代码运行与验证指南.md)维护；数据目录契约见 [`data/README.md`](data/README.md)。本文只记录真实实验发生了什么及其审计结论，不再复制实现说明。

后文常用链路为 `run → source → target / sam_text_prompt → proposal → decision → object / observation`：一次运行处理去重后的源图，首轮目标提示驱动候选生成，候选经过对象决策后才可能形成长期对象或观测。

## 3. 最近一次真实实验发生了什么

最近一次真实实验是 v0.17.4，运行编号为 `run_20260802T075402522428Z`。原始文件已删除，但可从 Git 提交 `f413654` 恢复。

```text
28个输入文件
→ 去掉14个完全相同的副本，剩14张唯一图
→ Qwen第一次按4+4+4+2分成4批，共调用4次，提出23个目标
→ 第1批因一条单杯描述含“and”而整批停止，7个目标没有进入SAM3
→ 其余10张图留下16条SAM3文字提示
→ SAM3执行16次文字查找：11次找到区域，5次返回0候选
→ 脚本检查11个区域，删除0个，正式保留11个候选
→ 9张图有候选，所以Qwen第二次按图调用9次，处理全部11个候选
→ 得到3个new和8个existing
→ 最终形成3个对象ID和11条观测
```

这里的“16次文字查找”不是加载 SAM3 16次。同一张图只编码一次，但图中每个目标提示都要分别向 SAM3 查询一次。

人工逐图确认的参照是：水瓶出现10次、鼠标出现10次、饮料杯出现4次，共24次物体出现。它们是3个具体物体在多张图里的24次出现，不是24个不同物体。

| 对照项 | 历史实验结果 | 人工确认 |
|---|---:|---:|
| 水瓶观测 | 6 | 10 |
| 鼠标观测 | 3 | 10 |
| 饮料杯相关观测 | 2 | 4 |
| 合计 | 11 | 24 |

这次实验没有通过 M5-B，主要问题是：

1. `img_0004.jpg` 的 `target_001` 使用 `plastic drink cup with straw and cartoon lid`。它描述的是一个杯子及其部件，但旧程序见到 `and` 就拒绝，连带第1批4张图全部停止。
2. 进入 SAM3 的16条提示中，5条没有返回区域。旧阈值0.5偏紧，而且 `white water bottle with handle`、`wireless computer mouse` 等描述加入了不稳定或当前视角不可靠的属性。
3. `img_0006.jpg` 只把附着在杯子上的杯盖和吸管当成对象；后来整杯观测仍被归入 `cup lid` 档案，说明“完整物体”和“附着部件”的粒度没有守住。
4. 鼠标的原始 crop 是灰银色，但带红色高亮的旧 overlay 与“粉色”误判同时出现；水瓶档案还混入了桌面和对象ID等场景信息。
5. SQLite、文件引用和内外报告在删除前审计完整，问题来自候选召回和语义判断，不是存储损坏。

## 4. 最近实验之后的代码变化

v0.18.0 至 v0.20.0 已针对最近实验暴露的目标短语、SAM3 阈值、候选上下文、部件粒度、对象卡语义、报告可读性和 Web 实验管理进行修改，但尚无新的服务器报告证明效果。已提交变化见 [CHANGELOG.md](CHANGELOG.md)，当前实现与验证口径见[业务代码运行与验证指南](docs/04_业务代码运行与验证指南.md)；在新实验前，本节不重复维护实现清单，也不据此改变 v0.17.4 的失败结论。

## 5. 里程碑

| 里程碑 | 状态 | 结论 |
|---|---|---|
| M0 环境与模型 | 完成 | SAM3 和 Qwen 已在服务器真实运行 |
| M1 数据库与文件骨架 | 完成 | 对象、候选、观测和文件可以对应保存 |
| M2 SAM3 候选接口 | 完成但有限 | 可以按文字寻找区域，不同视角仍可能漏检 |
| M3 Qwen 对象判断 | 完成但有限 | 可以创建和归并对象，语义稳定性仍需复验 |
| M4 记忆写入闭环 | 完成 | SQLite、图片和报告可以完整对应 |
| M5-A 候选到记忆 | 完成但有限 | 批量候选可以完成判断和写入 |
| M5-B 完整实验效果 | 未通过 | 最近真实实验只覆盖11/24，并有部件粒度和颜色问题 |

## 6. 当前关键判断

1. 最近一次真实实验仍然失败；v0.18.0 只是待验证的修正，不能提前宣布效果提升。
2. 首轮 Qwen 决定 SAM3 会看什么；首轮漏掉的物体，后续环节无法补回。
3. SAM3 阈值降到0.4是召回与误检之间的实验性取舍，下一份报告必须分别核对提示数、0候选数、脚本删除数和最终观测数。
4. 第二轮必须先确认候选是不是完整独立物体，再做对象身份比较；相同类别、颜色或材质不能单独证明是同一实例。
5. 当前继续保存所有有效的 `new/existing` 观测；“只保留有新信息的视角”仍是后续优化，不是本轮验收项。
6. 现有人工参照只覆盖3个具体实例，不能据此声称已经通过同类不同实例、纯无效场景或规模压力测试。

## 7. 实验历史

| 版本与日期 | 实验 | 结果 |
|---|---|---|
| v0.15.2，2026-07-31 | SAM3 点网格基线 | 14张图产生3584个原始候选，脚本保留72个，Qwen接纳59条观测并形成8个档案；人工确认实际只有3个对象，且有大量背景和固定件污染。历史恢复点为 `3151334` 或更早提交 |
| v0.16.1，2026-07-31 | 第一次两阶段实验 | 14张唯一图中只有3张走完流程，产生6个候选和2个对象，饮料杯缺失 |
| v0.17.0，2026-08-02 | 第二次两阶段实验 | 第2批因旧6词限制停止；10张图完成，产生12个候选和3个对象ID，覆盖12/24 |
| v0.17.4，2026-08-02 | 第三次两阶段实验 | 第1批因旧 `and` 规则停止；10张图完成，产生11个候选和3个对象ID，覆盖11/24，并出现杯盖粒度和鼠标颜色错误 |
| v0.18.0，2026-08-02 | 清理与源头优化 | 删除三次旧记忆和报告，保留固定输入；完成首轮、SAM3阈值、候选上下文和对象卡语义修改，等待全新实验 |

v0.16.1、v0.17.0 和 v0.17.4 的完整删除前快照可从 Git 提交 `f413654` 恢复。
