# 实验看板与审计分析

> 本文用大白话记录当前实验处于什么状态、最近一次真实实验每一步发生了什么，以及审计后能得出什么结论。后续优化清单见 `docs/05_后续优化清单.md`。

- **当前版本**：v0.20.4
- **最后更新**：2026-08-07
- **当前关卡**：M5-B——端到端对象记忆验收
- **当前实验状态**：最新实验结构检查通过，但语义覆盖15/24，M5-B仍未通过；当前首轮v5和多记忆库功能仍待正式实验验证

## 1. 现在仓库里有什么

- `data/input/` 保留28个输入文件，其中14张内容唯一，另外14个是完全相同的副本。
- `data/memory/` 保存最新完整记忆库：1次运行、14张唯一源图、15个候选与决定、3个对象和15条观测；数据库、源图、候选、对象观测、原始回答和内部报告共121个文件，全部进入Git。
- `environment/run_report.json` 与记忆库内部 `run_reports/run_20260803T093048019080Z.json` 的SHA-256一致；SQLite完整性检查为 `ok`，数据库中的119条资产引用均存在且已跟踪。
- v0.16.1、v0.17.0和v0.17.4的旧产物仍只从Git提交 `f413654` 恢复；当前记忆根只属于最新实验。
- 最新报告实际记录首轮 `robot-scene-guidance-v4`、第二轮 `guided-image-batch-memory-reasoning-v3` 和SAM3阈值0.4。当前代码的首轮v5及v0.20.0多记忆库功能晚于这次运行，不能用该报告宣称已经验证。
- v0.20.1只重组展示与文档；v0.20.2只同步最新实验事实和审计；v0.20.3只增加公开 Web 展示导航，不修改算法或实验产物。
- v0.20.4只新增已确认但尚未实施的 `DEMO优化方案.md` 并同步长期优化记录，不修改算法、依赖或实验产物；DINOv3、单次Qwen、视觉指纹和新对象摘要均未验证。

## 2. 如何阅读实验记录

稳定算法流程、编号含义、运行参数和报告字段统一由[业务代码运行与验证指南](docs/04_业务代码运行与验证指南.md)维护；数据目录契约见 [`data/README.md`](data/README.md)。本文只记录真实实验发生了什么及其审计结论，不再复制实现说明。

后文常用链路为 `run → source → target / sam_text_prompt → proposal → decision → object / observation`：一次运行处理去重后的源图，首轮目标提示驱动候选生成，候选经过对象决策后才可能形成长期对象或观测。

## 3. 最近一次真实实验发生了什么

最近一次真实实验运行编号为 `run_20260803T093048019080Z`，使用首轮提示v4、第二轮提示v3和SAM3阈值0.4。运行从2026-08-03 17:30:48至17:40:44（北京时间），完整产物位于 `data/memory/`，外部报告为 `environment/run_report.json`。

```text
28个输入文件
→ 去掉14个完全相同的副本，剩14张唯一图
→ Qwen第一次按4+4+4+2分成4批，共调用4次，提出23个目标
→ 23个目标全部进入SAM3文字查询，没有批次失败
→ 15次查询找到区域，8次返回0候选，共产生15个阈值以上区域
→ 脚本删除0个，正式保留15个候选
→ 11张图有候选，所以Qwen第二次按图调用11次，处理全部15个候选
→ 得到3个new和12个existing，没有ignored、uncertain或失败候选
→ 最终形成3个对象ID和15条观测
```

这里的“23次文字查找”不是加载SAM3 23次。同一张图只编码一次，但图中每个目标提示都要分别向SAM3查询一次。

人工逐图确认的参照是：水瓶出现10次、鼠标出现10次、饮料杯出现4次，共24次物体出现。它们是3个具体物体在多张图里的24次出现，不是24个不同物体。

| 对照项 | 最新实验结果 | 人工确认 |
|---|---:|---:|
| 水瓶观测 | 6 | 10 |
| 鼠标观测 | 7 | 10 |
| 饮料杯相关观测 | 2 | 4 |
| 合计 | 15 | 24 |

报告状态为 `passed`，表示输入登记、顺序驻留、候选覆盖、写盘和Demo结构检查全部通过；它不等于语义效果通过。M5-B仍未通过，审计结论是：

1. v4仍把 `img_0001.jpg`、`img_0003.jpg`和`img_0005.jpg`中的透明水瓶写成 `drink cup`，三条查询均返回0；`img_0012.jpg`使用 `water bottle with handle`也返回0。这正是当前v5新增瓶/杯基础类别约束和单修饰词规则要解决的问题，但本轮尚未使用v5。
2. 其余0候选包括5条 `drink cup`中的上述3条误分类及两个真实杯视角，以及2条 `computer mouse`；SAM3召回从旧实验11/24提高到15/24，但仍漏掉9次真实物体出现。
3. 15个保留候选全部被第二轮接受并正确归入3个具体对象。对象卡和抽查资产显示：饮料杯以带盖带吸管的完整杯建档，鼠标记录为银灰色，水瓶卡不再混入桌面、对象ID或上下文框颜色；旧实验的部件粒度和颜色污染在本轮证据中未复现。
4. SQLite `integrity_check`为 `ok`；15个proposal、15个decision和15个observation完整对应，内外报告哈希一致，问题仍在上游目标用词与SAM3召回，不在存储。

## 4. 最近实验之后的代码变化

本轮证明v4已消除整批停止，阈值0.4与第二轮v3能够形成结构完整且对象粒度正确的15条记忆；但v4仍存在瓶/杯用词错误。当前代码随后升级到首轮v5，并新增多记忆库管理与Web摘要重构，这些变化没有包含在本轮运行中。已提交变化见 [CHANGELOG.md](CHANGELOG.md)，稳定实现说明见[业务代码运行与验证指南](docs/04_业务代码运行与验证指南.md)。

## 5. 里程碑

| 里程碑 | 状态 | 结论 |
|---|---|---|
| M0 环境与模型 | 完成 | SAM3 和 Qwen 已在服务器真实运行 |
| M1 数据库与文件骨架 | 完成 | 对象、候选、观测和文件可以对应保存 |
| M2 SAM3 候选接口 | 完成但有限 | 可以按文字寻找区域，不同视角仍可能漏检 |
| M3 Qwen 对象判断 | 完成但有限 | 可以创建和归并对象，语义稳定性仍需复验 |
| M4 记忆写入闭环 | 完成 | SQLite、图片和报告可以完整对应 |
| M5-A 候选到记忆 | 完成但有限 | 批量候选可以完成判断和写入 |
| M5-B 完整实验效果 | 未通过 | 最新实验结构通过且对象卡正确，但只覆盖15/24；当前首轮v5尚未验证 |

## 6. 当前关键判断

1. 最新运行的程序结构和数据完整性通过，但语义覆盖只有15/24，因此M5-B仍然失败；不能把报告的 `passed` 解释为对象记忆效果通过。
2. 首轮 Qwen 决定 SAM3 会看什么；首轮漏掉的物体，后续环节无法补回。
3. SAM3阈值0.4本轮产生15个阈值上候选、脚本删除0个且第二轮全部接受；召回有所提高，但8条提示仍为0候选，当前证据支持继续审计提示质量而不是仅看总候选数。
4. 第二轮必须先确认候选是不是完整独立物体，再做对象身份比较；相同类别、颜色或材质不能单独证明是同一实例。
5. 当前继续保存所有有效的 `new/existing` 观测；“只保留有新信息的视角”仍是后续优化，不是本轮验收项。
6. 现有人工参照只覆盖3个具体实例，不能据此声称已经通过同类不同实例、纯无效场景或规模压力测试。

## 7. 实验历史

| 版本与日期 | 实验 | 结果 |
|---|---|---|
| v0.15.2，2026-07-31 | SAM3 点网格基线 | 14张图产生3584个原始候选，脚本保留72个，Qwen接纳59条观测并形成8个档案；人工确认实际只有3个对象，且有大量背景和固定件污染。历史恢复点为 `3151334` 或更早提交 |
| v0.16.1，2026-07-31 | 第一次两阶段实验 | 14张唯一图中只有3张走完流程，产生6个候选和2个对象，饮料杯缺失 |
| v0.17.0，2026-08-02 | 第二次两阶段实验 | 第2批因旧6词限制停止；10张图完成，产生12个候选和3个对象ID，覆盖12/24 |
| v0.17.4，2026-08-02 | 第三次两阶段实验 | 第1批因旧 `and` 规则停止；10张图完成，产生11个候选和3个对象ID，覆盖11/24，并出现杯盖粒度和鼠标颜色错误 |
| v0.18.0，2026-08-02 | 清理与源头优化 | 删除三次旧记忆和报告，保留固定输入；完成首轮v4、SAM3阈值、候选上下文和对象卡语义修改 |
| v0.18.0配置，2026-08-03 | 第四次两阶段实验 | 14张唯一图全部完成，23条提示产生15个候选，形成3个正确对象档案和15条观测；结构通过但只覆盖15/24，运行编号 `run_20260803T093048019080Z` |

v0.16.1、v0.17.0 和 v0.17.4 的完整删除前快照可从 Git 提交 `f413654` 恢复。
