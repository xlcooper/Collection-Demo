# 实验看板与审计分析

> 本文档只记录真实完成的实验：每一步收到什么、实际做了什么、输出了什么，以及最后如何评价。当前开发方向见 `AGENTS.md`，暂不处理的优化见 `docs/05_后续优化清单.md`。

- **当前版本**：v0.17.5
- **最后更新**：2026-08-02
- **当前关卡**：M5-B——端到端对象记忆验收

## 1. 本次结论与数量变化

本次实验仍未通过最终验收。6词限制已经删除，但第1批又因一条只描述单个杯子的英文含有 `and` 而整体停止。数据库最终保存3个对象档案和11条观测；人工逐图确认应有24次物体出现，数据库覆盖11/24，其中1条还只分出了杯盖和吸管。

运行编号：`run_20260802T075402522428Z`

```text
28个输入文件
→ 跳过14个内容完全相同的副本，剩14张唯一图片
→ Qwen第一次分4批看图，共写出23条找物体建议
→ 第1批的7条建议全部没有进入SAM3，剩16条
→ SAM3在10张图中按16条文字指令找物体，找到11个区域
→ 脚本检查后仍保留11个候选
→ 9张图至少有1个候选，所以Qwen第二次调用9次并处理11个候选
→ 最终形成3个对象档案和11条观测记录
```

人工逐图确认的结果是：水瓶出现10次、鼠标出现10次、饮料杯出现4次，共24次。这里的24不是24个不同物体，而是3个物体在多张图片中合计出现24次。

## 2. 名词和编号说明

| 名称 | 大白话解释 |
|---|---|
| `run_id` | 一次完整实验的编号 |
| `source_id` | 一张去重后原图的编号 |
| `target_id` | 第一次Qwen在单张图中临时列出的“第几个物体”；每张图都从 `target_001` 重新开始 |
| `sam_text_prompt` | 真正交给SAM3的英文找物体指令，例如 `wireless computer mouse` |
| `raw_candidate_id` | SAM3找到的临时区域编号 |
| `proposal_id` | 脚本检查并正式保存后的候选编号 |
| `object_id` | 现实中某个具体物体的长期档案编号 |
| `observation_id` | 某个对象在一张图片中的一次观测记录编号 |

本次运行产物中仍没有直接保存 `target_id → proposal_id` 的对应字段。SAM3实际收到的是英文提示词，不是 `target_id`；需要通过相同的 `source_id`、提示词和顺序还原关系。`raw_candidate_id` 中的 `text_000` 表示第一条提示，`text_001` 表示第二条提示。

## 3. 本次实验逐步实录

本次记忆库根目录是 `data/memory_v017_02/`。下表中的文件位置都从这个目录开始计算，只有最后一份外部报告位于仓库的 `environment/`。

| 环节 | 输入 | 本次实际发生的事 | 输出和文件位置 |
|---|---|---|---|
| 1. 读取与去重 | `data/input/` 中28个文件 | 按图片内容确认14个文件是另外14张图的完全相同副本。副本停止处理，14张唯一图继续 | 14个 `source_id`；处理用原图保存在 `sources/` |
| 2. Qwen第一次看图 | 14张唯一图 | 使用 `robot-scene-guidance-v3`，按 `4+4+4+2` 分成4批，共调用4次。原始回答列出23个物体。第1批中有一句 `plastic drink cup with straw and cartoon lid`，程序看到 `and` 就认为它连接了多个物体，因此第1批4张图、7条建议全部停止；其余3批留下16条指令 | 4份原始回答：`raw_responses/<run_id>/scene_batch_0001/response.json` 至 `scene_batch_0004/response.json` |
| 3. SAM3按文字找物体 | 10张图、16条英文指令 | SAM3模型加载1次，每张图先转换1次内部特征，再执行16次文字查找。11次找到区域，5次没找到：3次鼠标、1次水瓶、1次饮料杯 | 11个临时候选；5次“没找到”表示SAM3没有产生候选，不是脚本删除 |
| 4. 脚本检查候选 | 11个SAM3临时候选 | 检查区域大小、重复、包含关系和每张图的数量上限。本次11个全部通过，删除0个 | 11个 `proposal_id`，保存在 `proposals/<run_id>/<proposal_id>/`；每个有 `crop.png`、`mask.png` 和 `overlay.jpg` |
| 5. Qwen第二次看候选 | 11组候选图和当时已有的对象档案 | 只有9张图至少有1个候选，因此按图调用9次。Qwen给11个候选分别判断有效性和对象身份 | 3个 `new`、8个 `existing`，没有丢弃；回答位于 `raw_responses/<run_id>/<source_id>/response.json` |
| 6. 写入对象记忆 | 11个处理结果 | 创建3个对象ID并保存11次观测：水瓶6、鼠标3、杯子相关2 | 对应关系写入 `memory.sqlite`；图片位于 `objects/<object_id>/observations/<observation_id>/` |
| 7. 生成实验报告 | 整次运行的计数、错误和文件引用 | 程序状态为 `completed_with_errors`，外部验收报告为 `failed` | `run_reports/<run_id>.json`；`environment/run_report_v017_02.json` |

第1批触发停止的具体字段：

- 原图：`img_0004.jpg`
- `source_id`：`src_38f13969e802dc30e46f45461efef2ac`
- `target_id`：`target_001`
- 英文指令：`plastic drink cup with straw and cartoon lid`

这句话只描述一个饮料杯以及它的吸管和杯盖，并没有给出两个备选物体。当前程序对所有 `and` 一律拒绝，因此发生了误判。

## 4. 14张唯一图逐图结果

| 原图 | `source_id` | 批次 | 人工确认图中物体 | Qwen第一次、SAM3和最终记忆结果 |
|---|---|---:|---|---|
| `img_0001.jpg` | `src_c5a2b20d9299071e3915298423480189` | 1 | 鼠标、水瓶 | `target_001`鼠标、`target_002`水瓶；第1批整体停止，均未进SAM3 |
| `img_0003.jpg` | `src_4cf691844b8baeee414cff6a9d6e441e` | 1 | 鼠标、水瓶 | `target_001`水瓶、`target_002`鼠标；第1批整体停止 |
| `img_0004.jpg` | `src_38f13969e802dc30e46f45461efef2ac` | 1 | 饮料杯 | `target_001`饮料杯的英文含 `and`，触发第1批停止 |
| `img_0005.jpg` | `src_76482d84fc76d1c7e2061993bfa67a79` | 1 | 鼠标、水瓶 | `target_001`水瓶、`target_002`鼠标；第1批整体停止 |
| `img_0006.jpg` | `src_ebd9a7980e4eebecde06d3ab3317d51c` | 2 | 饮料杯 | 只写成“杯盖和吸管”；SAM3只找到杯盖和吸管；新建 `cup lid` 档案 |
| `img_0008.jpg` | `src_11b171cb7b25e04e37560c8a76630baf` | 2 | 鼠标、水瓶 | 只列水瓶，鼠标被漏掉；水瓶找到并新建档案 |
| `img_0009.jpg` | `src_cd11b8f53eee5bf00b87e637aa6c481f` | 2 | 鼠标、水瓶 | 水瓶和鼠标都找到；水瓶归入已有档案，鼠标新建档案 |
| `img_0011.jpg` | `src_01577d5285c9315307e807b77e802c81` | 2 | 饮料杯 | 饮料杯指令成功进入SAM3，但SAM3返回0候选，因此没有第二次Qwen回答 |
| `img_0012.jpg` | `src_3cea6b6ee098e81455d929f6f2ca3aa2` | 3 | 鼠标、水瓶 | 鼠标找到并归入已有档案；水瓶返回0候选 |
| `img_0013.jpg` | `src_f37cbe405bc095713b4c5096c9b3bafa` | 3 | 饮料杯 | SAM3找到整杯，却被归入已有的 `cup lid` 档案 |
| `img_0015.jpg` | `src_54ac1b5d01f7f5f4123518653092ee1b` | 3 | 鼠标、水瓶 | 两者都找到并归入已有档案 |
| `img_0017.jpg` | `src_3c29806ddef097e7d2dff530f49d18e4` | 3 | 鼠标、水瓶 | 水瓶找到；鼠标返回0候选 |
| `img_0019.jpg` | `src_5cf926f0d4c1c65b71d162b3e47ae947` | 4 | 鼠标、水瓶 | 水瓶找到；鼠标返回0候选 |
| `img_0021.jpg` | `src_46cdc95f1f72f513c8dfe5228aa5e0f6` | 4 | 鼠标、水瓶 | 水瓶找到；鼠标返回0候选 |

## 5. 一条真实编号链路

以 `img_0009.jpg` 中的鼠标为例：

```text
原图 img_0009.jpg
→ source_id: src_cd11b8f53eee5bf00b87e637aa6c481f
→ target_id: target_002
→ 交给SAM3的指令: wireless computer mouse
→ SAM3临时候选: text_001_candidate_000
→ 正式候选: prop_4dc708b7507947e8b46ef8fa75738c40
→ Qwen第二次结果: new
→ 鼠标档案: obj_8e86ee07358d49a4963895892507216a
→ 本次鼠标观测: obs_358ebfab093a4590ba512361dcae0fc3
```

对应文件依次位于：

- 第一次Qwen：`raw_responses/<run_id>/scene_batch_0002/response.json`
- 候选图片：`proposals/<run_id>/prop_4dc708b7507947e8b46ef8fa75738c40/`
- 第二次Qwen：`raw_responses/<run_id>/src_cd11b8f53eee5bf00b87e637aa6c481f/response.json`
- 最终观测：`objects/obj_8e86ee07358d49a4963895892507216a/observations/obs_358ebfab093a4590ba512361dcae0fc3/`

## 6. 最终记忆与审计结论

| 人工确认的真实对象 | `object_id` | 数据库观测 | 人工确认应有 | 档案结果 |
|---|---|---:|---:|---|
| 水瓶 | `obj_5fe3821514bd43019c733ea93bd0b1a8` | 6 | 10 | 身份归并正确；描述混入桌面和对象ID |
| 鼠标 | `obj_8e86ee07358d49a4963895892507216a` | 3 | 10 | 身份归并正确；实际灰银色，最终档案写成粉色 |
| 饮料杯 | `obj_0bf2fc229126451b9363a444a02a37c3` | 2 | 4 | 档案被建成 `cup lid`；一条只含杯盖，一条是整杯 |
| **合计** | **3个对象ID** | **11** | **24** | **对象ID数量正确不代表档案语义正确** |

- 11个候选都落在水瓶、鼠标或杯子相关区域，没有桌面、阴影或固定件候选。
- `img_0006` 的候选只包含附着在杯子上的杯盖和吸管，不是完整饮料杯；如果只计算完整对象档案，本次覆盖为10/24。
- `img_0012` 的鼠标被水瓶遮挡，SAM3只分出了可见的右半部分；这仍是鼠标区域，但信息不完整。
- 鼠标第一次建档时被写成银色；后续两次观测的原始裁图仍是灰银色，但带红色高亮的 `overlay` 中鼠标呈粉色，Qwen随后把档案更新为粉色。两者存在明显对应关系，但尚未做只看crop的对照实验，因此不能只凭本轮确定唯一原因。
- 杯子第一次以“杯盖”建档，后来整杯观测虽然被判断为同一实例，最终档案仍保留 `cup lid`，属于部件和整体粒度错误。
- SQLite完整性正常，外键异常0；95个记忆库文件完整，缺失和孤儿文件均为0。内外报告完全一致。
- 提交中没有单元测试日志，因此现有文件不能证明服务器回归测试是否通过。

### 与上次实验对比

| 指标 | `memory_v017_01` | `memory_v017_02` |
|---|---:|---:|
| Qwen第一次原始目标 | 23 | 23 |
| 程序采用的SAM3指令 | 18 | 16 |
| 整体停止的批次 | 第2批 | 第1批 |
| SAM3找到 / 没找到 | 12 / 6 | 11 / 5 |
| 脚本删除 | 0 | 0 |
| Qwen第二次调用 | 10 | 9 |
| `new / existing` | 3 / 9 | 3 / 8 |
| 对象ID / 观测 | 3 / 12 | 3 / 11 |
| 数据库覆盖 | 12/24 | 11/24 |

6词限制的修正确实让原第2批通过，但Qwen这次在第1批生成了含 `and` 的新表达，触发另一条硬限制。完成图数仍是10张，数据库覆盖反而少1条。

## 7. 运行耗时

| 环节 | 实际处理量 | 模型推理时间 |
|---|---:|---:|
| Qwen第一次看图 | 4次批量调用 | 210.733秒 |
| SAM3 | 10张图、16次文字查找 | 1.404秒 |
| Qwen第二次判断 | 9次调用、处理11个候选 | 295.733秒 |
| **整次运行** | 14张唯一图 | **528.864秒，约8分49秒** |

两轮Qwen合计调用13次，推理用时506.466秒，输入54443 tokens、输出7574 tokens。Qwen峰值显存11700.63 MiB，SAM3峰值显存5099.30 MiB。比上次少用约26秒，主要因为少了一次第二轮Qwen调用，不代表效果提升。

## 8. 里程碑

| 里程碑 | 状态 | 大白话结论 |
|---|---|---|
| M0 环境与模型 | ✅ | SAM3和Qwen都能在服务器真实运行 |
| M1 数据库与文件骨架 | ✅ | 对象、候选、观测和文件可以完整保存 |
| M2 SAM3候选接口 | ✅（有限） | 可以按英文名称找区域，但不同视角下仍会漏检 |
| M3 Qwen对象判断 | ✅（有限） | 可以创建和归并对象，但对象名称和颜色仍会被错误更新 |
| M4 记忆写入闭环 | ✅ | 数据库、图片和运行报告可以完整对应 |
| M5-A 候选到记忆 | ✅（有限） | 批量候选可以完成判断和写入 |
| M5-B 完整实验效果 | ❌ | 数据库只覆盖11/24，并存在杯盖粒度和鼠标颜色错误 |

## 9. 当前关键判断

1. 本次确实使用 `robot-scene-guidance-v3`；6词限制已经解除，历史第2批可以进入SAM3。
2. 当前对 `and` 的一律拒绝又误伤了单个杯子的属性描述，是本次4张图失败的直接原因。
3. 对已经进入SAM3的提示，水瓶找到6/7、鼠标3/6、饮料杯2/3；脚本没有删除任何候选。
4. 数据库中的3个对象ID不能直接理解为3个正确档案：杯子被记成杯盖，鼠标被记成粉色。
5. 红色overlay与粉色误判再次同时出现，证据比上次更强，但是否删除overlay应通过对照实验确认。
6. 当前继续保存所有有效的 `new/existing` 观测；视角信息增量不是当前验收项。
7. `data/memory/`、`data/memory_v017_01/` 和 `data/memory_v017_02/` 都是已结束实验的证据；新的对比实验使用新的空记忆库。

## 10. 历史记录

| 版本与日期 | 实验 | 结果与证据 |
|---|---|---|
| v0.15.2，2026-07-31 | SAM3点网格基线 | 14张图产生3584个原始候选，脚本留下72个，Qwen接纳59个观测并形成8个档案；人工确认实际只有3个对象，且有30条背景或固定件观测。原始文件已清理，需要从Git提交 `3151334` 或更早版本恢复 |
| v0.16.1，2026-07-31 | 第一次两阶段实验 | 14张唯一图中只有3张走完流程，产生6个候选和2个对象，饮料杯缺失；证据在 `data/memory/` 和 `environment/demo_run_report.json` |
| v0.17.0，2026-08-02 | 第二次两阶段实验 | 第2批因6词限制停止；10张图完成，产生12个候选和3个对象ID，数据库覆盖12/24；证据在 `data/memory_v017_01/` 和 `environment/run_report_v017_01.json` |
| v0.17.4，2026-08-02 | 第三次两阶段实验 | 第1批因 `and` 规则停止；10张图完成，产生11个候选和3个对象ID，数据库覆盖11/24，并出现杯盖粒度和鼠标颜色错误；证据在 `data/memory_v017_02/` 和 `environment/run_report_v017_02.json` |
