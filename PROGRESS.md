# 第一阶段进度看板

> **维护规则（强制）**：本文件是项目当前执行状态的唯一事实源。每个新对话按 AGENTS → README 的入口顺序后必须读取本文；每次收到服务器结果、推进里程碑或改变实现决策后，本地 AI 必须更新状态、证据、风险、计划和下一动作。用户明确确认可以判定功能完成；报告用于固化指标和技术事实，二者冲突时以最新报告为准。过时状态必须替换，不以追加说明掩盖。

**最后更新**：2026-07-31 15:15:06 +08:00

## 管理方式

- 初始里程碑来自本地 `docs/00_项目纲要.md`、`docs/01_第一阶段项目设计规划书.md`，并结合最新服务器环境形成。
- 纲要负责目标和边界，规划书负责初始架构参考；它们不是日常状态源，也不要求每轮重复读取。
- 本文件沿初始主线推进，并依据真实开发结果调整顺序、门槛和风险；调整只修改本文件和受影响的职责文件，不把过程信息回填堆积到规划书。
- CHANGELOG 记录已经提交的历史版本；环境重建与模型冒烟复现命令由 `docs/03_服务器环境与冒烟测试指南.md` 维护。

## 当前快照

- **版本**：v0.14.7
- **当前关卡**：M5-B——Qwen 接管与第一阶段端到端验收
- **端到端总入口**：`python scripts/run_object_memory.py`；`scripts/verify_m*.py` 仅用于里程碑验证和问题定位
- **第一阶段目标**：给系统一批不断增加的场景图片，它能自动发现物体、生成标签、建立对象档案，并把同一具体物体的不同观测持续归并到同一档案中；暂不负责主动选择拍摄视角、控制机器人或判断物体信息是否已经完整
- **里程碑计数**：11个关卡中10个已关闭，仅 M5-B 进行中；该计数不代表工作量百分比，第一阶段产品仍未验收
- **最近完成**：拉取服务器提交 `2cda2e0`，核对端到端报告、SQLite 六张表、14对哈希重复图片、全部引用资产和隐藏真值，并完成 `data/`、`runs/` 715个文件的用途与清理分类
- **当前问题**：端到端报告虽为 `status=passed`，但它只验证路径覆盖和结构闭环。真值只有水瓶、鼠标、饮料杯3个目标实例，结果却形成8个对象；水瓶与饮料杯发生跨实例合并，水瓶又被拆成两个 ID，鼠标档案混入固定装置，并额外建立5个纯背景/固定装置对象，因此 M5-B 产品验收未通过
- **下一动作**：保留失败验收库和报告；由用户先确认无争议清理项及语义修复优先级。清理与修复分开提交，修复后用 `data/m5_validation_memory_02/` 重跑，不复用 `_01`
- **M5-B 当前门槛**：结构层仍要求 new/existing/哈希跳过/filtered或ignored 覆盖且无 pending/failed；产品层必须以隐藏真值逐图核对目标有效性和具体实例 ID，不能用顶层 `passed` 或计数替代。单图 `grid_point_000006` 配件误建档仍为已接受限制，但本轮身份错并、拆档和大规模背景污染不在该豁免内

状态说明：✅ 已完成；🟡 正在进行；⬜ 尚未开始；⛔ 被外部条件阻塞。

### 最新 M5-B 端到端审计

依据：`environment/m5_end_to_end_report.json`、`data/m5_validation_memory_01/memory.sqlite`、完整图像资产与本地隐藏真值。外部报告和记忆库内部报告 SHA-256 完全一致，运行时间为 2026-07-31 14:17:42 至 14:43:30（UTC+08:00）。

| 维度 | 服务器结果 | 验收判断 |
|---|---|---|
| 输入与哈希幂等 | 28个文件由14个唯一 SHA-256 及14个逐字节副本组成；14个副本全部跳过，只有14个 source 进入处理 | ✅ 哈希门通过 |
| 候选与决策闭环 | 3584个 SAM 候选中3512个 filtered、72个进入 Qwen；最终8 new、51 existing、13 ignored、0 pending/failed | ✅ 结构闭环通过 |
| 数据库与资产 | SQLite `integrity_check=ok`、无外键违规；报告计数与1 run、14 sources、3584 proposals、8 objects、59 observations、72 decisions完全一致，所有 source/crop/mask/overlay/raw-response 引用存在 | ✅ 持久化通过 |
| 真值目标 | 真值只有同一水瓶、同一鼠标、同一饮料杯3个目标实例，预期3个对象，实际8个 | ❌ 对象数失败 |
| 实例身份 | 水瓶被拆成两个 ID，其中一个又吸收饮料杯观测；鼠标档案混入多个左右固定装置观测 | ❌ 归并与分离失败 |
| 候选有效性 | 对59个 accepted observations 逐图核对，29个属于目标/目标部件，30个属于背景或固定装置；另有5个对象完全由泡沫板、线缆或固定装置组成 | ❌ 背景抑制失败 |
| 发现覆盖 | 目标在各唯一源图中均至少有一个候选被接纳，但接纳后的有效性和身份归并错误严重 | 🟡 只有覆盖，不构成产品通过 |
| 单元测试 | 端到端报告、内部报告、原始响应和提交内容均无单元测试执行摘要 | ⬜ 不能确认已运行或通过 |
| 性能 | 总耗时1547.87秒（约25分48秒）；SAM推理64.47秒，Qwen 14次推理1450.92秒，输入137122 token、输出22923 token，Qwen峰值显存14512.97 MiB | 🟡 可运行，但Qwen占绝大多数耗时 |

顶层 `status=passed` 和 `phase_one_coverage=true` 只表示脚本设定的数量与终态条件被覆盖；当前实现并不知道隐藏真值，也不验证“new 是否是正确的新实例”或“existing 是否指向正确实例”，因此不能据此关闭 M5-B。

### 历史单图 M5-B gate 审计

依据：`environment/m5_image_batch_gate_report.json`，生成于 2026-07-31 13:41:34（UTC+08:00）。

| 维度 | 服务器结果 | 验收判断 |
|---|---|---|
| 结构与持久化 | 顶层 `status=passed`，6项通用 checks 全真，无 pending/failed | ✅ 结构通过 |
| 单图批量策略 | 7个候选、1次 Qwen、0张对象卡片、0张参考图 | ✅ 单图单调用通过 |
| 候选语义 | 3个真实物体均为 `new`；4个应忽略项中3个正确，`000006` 错误为“吸管”并 `new` | 🟡 已记录为非阻塞限制，不优化 |
| 记忆身份比较 | 空记忆库，`existing=0` | ⬜ 本报告未覆盖 |
| 单元测试 | 报告中没有测试执行摘要，服务器提交只包含 gate JSON | ⬜ 不能据此确认 |
| Qwen 成本 | 9712 input tokens、1675 output tokens、109.945秒、峰值13530.58 MiB | 🟡 单调用成立，但延迟高于旧逐候选 gate 总耗时 |

## 里程碑

| 里程碑 | 状态 | 最小交付物 / 完成条件 | 当前证据 | 下一动作 |
|---|---|---|---|---|
| P0 项目基线 | ✅ | 第一阶段范围、开发准则、Git 与环境流程固化 | `README.md`、环境声明和执行指南 | 持续维护，不扩展阶段范围 |
| M0-A 服务器环境 | ✅ | Conda、CUDA、SAM3 及 Qwen 依赖可用，0 个阻塞项 | 最新报告：0 blocker、2 warnings；数据盘剩余 23.45 GiB | M1 控制数据规模，不下载备用模型 |
| M0-B SAM3 单模型冒烟 | ✅ | 真实加载 checkpoint，并输出至少一个 proposal 和 mask | `cup` 得到 1 个 proposal，score 0.8867；加载 10.196 秒、推理 0.462 秒、峰值显存 5087.16 MiB | 单提示链路完成；全量策略留到 M2 |
| M0-C Qwen 单模型冒烟 | ✅ | 8B FP8 加载成功，输出可解析的约定 JSON，并记录耗时/显存 | 98 token，13.32 token/s；加载 3.518 秒、推理 7.356 秒、模型全部在 GPU 0、峰值显存 10439.92 MiB | 结构链路完成；语义质量留到 M3 验证 |
| M0-D 双模型顺序运行 | ✅ | 同一环境中先后完成 SAM3、Qwen，且无显存或磁盘阻塞 | 统一入口生成三份通过/无阻塞报告，回传提交 `59bd3a7` | M0 关闭，进入 M1 |
| M1 数据骨架 | ✅ | 配置、统一数据类型、SQLite schema、文件路径和最小 CLI | M1 报告 `passed`：6 项检查全真，六张表计数均为 0 | M1 关闭，进入 M2 |
| M2 SAM3 流水线 | ✅ | 候选策略、mask 去重、crop/mask/overlay 和源图关联 | M2 报告 `passed`：2 个原始候选跨提示去重为 1 个保留候选，10 项检查全真 | M2 关闭，进入 M3 |
| M3 MLLM 流水线 | ✅ | 对象卡片、身份判断、自动标注和 JSON 校验 | v2 服务器报告 `passed`：空记忆为 `new`；同一视图为 `existing` 且 ID 正确；全部检查通过 | M3 关闭，进入 M4 |
| M4 记忆闭环 | ✅ | 新建/归并、幂等、pending/failed、事务写入和运行摘要 | 服务器报告 `passed`，10 项检查全真；4 runs、3 sources、6 proposals、1 object、3 observations、5 decisions | M4 关闭，进入 M5 |
| M5-A 无提示候选校准 | ✅ | 不提供物体类别，由 SAM3 自动提出足够可用的场景候选 | v2：256 个原始候选保留 7 个，3/3 真实物体均保留；桌面和局部明显减少，全部检查通过 | 阶段关闭；阴影优化转为非阻塞后续项 |
| M5-B 第一阶段验收 | 🟡 | 单命令、无人确认地完成发现—判断—标注—存储—归并，并可追踪失败 | 完整批次结构和哈希门通过，但真值审计失败：3个目标形成8个对象，存在错并、拆档和5个纯无效对象 | 保留失败证据；先确认清理与修复优先级，再用全新记忆库复验 |

## 当前关键判断

1. M0 已完成：统一入口顺序运行两个模型并完成环境自检，三份报告均已进入 Git。
2. SAM3 的 BF16 链路已跑通：`cup` 得到 1 个 proposal 和 mask，score 0.8867；推理约 0.46 秒、峰值显存约 5.0 GiB。
3. Qwen3-VL-8B-Instruct-FP8 全部位于 GPU 0；98 个输出 token 用时 7.356 秒，即 13.32 token/s。此前约 10 秒属于正常波动，不是 CPU offload。
4. Qwen 整图曾输出 `coffee/latte`，没有严格聚焦物理容器；M3 已改为输入 SAM crop 与 overlay 并明确只标注被分割的物理对象。M5 自动候选不再附带类别提示，类别由 Qwen 仅依据 crop 像素生成。
5. 通用文本提示 `object` 返回 0 proposals，不能代表“全量发现物体”；此前 M2 用显式类别列表完成了接口验证，但该策略不满足当前“无需外界类别提示”的产品要求，不能继续作为主流程候选策略。
6. 数据盘剩余 23.45 GiB，足够进行小规模 M1，但不再下载备用模型；每个里程碑继续监控空间。
7. nvcc 11.8 与 generation flags 均为已接受的非阻塞警告。
8. M1 已通过服务器验证：默认配置、六类数据模型、重复初始化、六张核心表、空库查询和 CLI 状态均通过；报告生成于 2026-07-30 16:03:59（UTC+08:00）。
9. M1 只建立持久化地基；M2 已完成候选与图像资产，M3 只做 Qwen 判断而不写数据库，实际新建/归并和事务写入仍属于 M4。
10. M2 报告已通过：`cup`、`mug` 各得到 1 个候选，IoU 去重后保留 `cup` 候选；模型加载 9.243 秒、全部提示推理 0.515 秒、峰值显存 5098.53 MiB。
11. 历史 M3 验收曾按固定批次遍历对象卡片；v0.13.0 短暂改为脚本文字 shortlist，v0.14.0 已按当前方案移除相关度计算，改为每张源图把全部 active 卡片和参考图直接交给 Qwen。
12. M3 提示 v2 最终报告生成于 2026-07-30 17:52:37（UTC+08:00）：全部检查通过，模型仍完全位于 GPU 0；加载 3.562 秒、两次推理共 20.09 秒、峰值显存 10667.93 MiB。
13. M3 空记忆输出 `new` 和完整的白色陶瓷咖啡杯标注；同一 crop 对象卡片输出 `existing`、置信度 1.0，并正确返回 `obj_m3_same_view_reference`。旧提示 v1 的同视图误判问题已由 v2 修复。
14. 历史 M3 提示 v2 曾在同一调用中混合有效性、身份和标注；v0.13.0 曾拆成两个调用。v0.14.0 根据当前产品决策恢复单次批量调用，但用固定输出字段强制先给出有效性和临时标注，再做记忆比较；对象卡片仍可能影响模型判断，必须由真实 gate 验证而不能宣称已隔离。
15. M4 直接复用 M1 的六张表和 schema v2：SQL 只在 `MemoryStore` 内执行，`MemoryLoop` 负责把已校验的 M3 响应转换为对象、观测和决策记录。
16. 图片 SHA-256 幂等门会跳过已经 `completed` 的源图和本批次已登记的相同文件；filtered 不调用 Qwen；ignored 不污染对象库；uncertain 记录 attempt 并保持 pending；上层耗尽异常重试后转 failed。
17. new/existing 的 crop、mask、overlay 会复制到对象观测目录；关联 SQL 在单个事务中完成，事务失败时清理本次资产，避免非法对象 ID 造成部分写入。
18. M4 验证只使用临时 SQLite 和确定性假数据，不加载模型、不修改正式记忆库；真实批量模型编排仍留给 M5。
19. M4 最终服务器报告生成于 2026-07-30 18:23:30（UTC+08:00），10 项检查全部通过；非法 existing 正确回滚，uncertain 可重试，对象卡片包含两张存在的代表视图，重复图计数为 1。
20. M5 对输入目录按路径排序并递归读取受支持图片；先登记哈希，再一次加载 SAM3 处理全部新图，释放后才加载 Qwen，保持已验证的单卡顺序驻留。
21. M5 以源图为 Qwen 调用单位：处理该图前读取当时全部 active 对象卡片和最近参考图，与本图全部保留候选共同输入。较早源图新建的对象可参与后续源图归并；同一源图内的新对象要到本次批量输出写入后才进入数据库。
22. M5 只对模型异常或整批 JSON/schema 解析失败进行有限重试，默认最多 2 次；一次重试会重新判断该源图的全部候选。原始输出、token 和耗时按源图记录，成功后每个候选仍只写一条有效 decision。
23. `--validate-phase-one` 要求至少 2 个 new、1 个 existing、1 个哈希跳过、1 个 filtered/ignored 且无 pending/failed；它提供路径覆盖证据，但具体对象身份仍需结合验收图片分组复核。
24. 当前主流程采用 `automatic_point_grid`：脚本在图像上生成 `16×16` 个均匀采样点，SAM3 对点批量推理并为每点保留质量分最高的 mask；这属于内部自动几何提示，不需要用户输入类别词。
25. M5-A v1 报告生成于 2026-07-30 19:17:57（UTC+08:00）：16×16 点网格产生 256 个原始候选，过滤 244 个、保留 12 个；推理 9.23 秒、峰值显存 5390.59 MiB，全部自动化检查通过。
26. Qwen 输入不再把 SAM 来源标记当类别标签；它被明确告知当前候选没有类别提示，应只根据像素判断类别，并把背景、物体局部、粘连区域或无意义碎片判为 `ignored`。
27. 人工检查确认 3 个真实物体均有完整候选；12 个保留候选中另有 3 个阴影、2 个物体局部和 4 个近似桌面区域，因此 v1 只证明覆盖率，不满足候选纯度门槛。
28. v2 将最大 mask 面积比例从 0.8 收紧至 0.5，并过滤被更高分完整候选覆盖至少 90% 的较小 mask；不把置信度硬卡在本图真实物体与噪声之间，也不增加第二次 Qwen 调用，无法安全用几何规则区分的阴影仍由 Qwen 判为 `ignored`。
29. M5-A v2 报告生成于 2026-07-30 19:47:29（UTC+08:00）：保留候选从 12 降至 7，`mask_too_large=15`、`contained_mask=2`、`duplicate_mask=49`、`low_confidence=183`；候选推理 7.183 秒、峰值显存 5390.59 MiB，全部检查通过。
30. 用户人工确认 v2 中3个真实物体仍全部存在、桌面和局部明显减少，剩余主要为2～3个阴影，因此 M5-A 已完成；当前允许这些少量候选进入 Qwen 的原有有效性判断。
31. 阴影前置过滤保留为非阻塞优化方向：M5-B 先记录真实 `ignored` 比例和 Qwen 耗时；只有多场景数据证明成本仍高时，才基于多图标注评估阴影/反射规则或批量有效性过滤，不用单图阈值过拟合。
32. Qwen gate v1 报告生成于 2026-07-31 10:43:22（UTC+08:00）：结构状态 `passed`，7个候选各调用一次 Qwen，得到3个 new、4个 ignored、0个 pending/failed；Qwen 推理共 43.427 秒、峰值显存 11591.37 MiB。
33. 逐候选审查发现计数掩盖了语义交换错误：真实候选 `grid_point_000067` 被 ignored，而阴影候选 `grid_point_000100` 因矩形 crop 中出现邻近杯子，被标为白色红盖饮料容器并创建对象；因此 M5-B gate v1 未通过。
34. 当前修复不增加图片或模型调用：`crop.png` 改为只保留 mask 内原始像素、外部填充统一中性灰；overlay 继续提供场景位置，Qwen 提示升级至 `m3-object-identity-v3` 并明确只判断保留像素，阴影、反射和纹理应 ignored。
35. Qwen gate v2 报告生成于 2026-07-31 10:59:56（UTC+08:00）：`memory_02` 正常登记并处理1张图片，重复跳过为0；256个原始候选保留7个，得到2个 new、5个 ignored、0个 pending/failed；Qwen 推理共41.389秒、峰值显存11536.39 MiB。
36. mask 隔离修复已得到针对性证据：v1 中因矩形 crop 包含邻近杯子而误建档的阴影 `000100`，在 v2 中正确变为 `ignored`；`000208`、`000006`、`000046` 也继续保持 ignored。
37. gate v2 的剩余失败是接口边界：Qwen 明确认出 `000067` 是饮料杯，却以“不是已有鼠标”为由判 ignored。v0.13.0 曾通过两次调用隔离该问题；v0.14.0 按最新方案改回单次批量推理，因此必须重新验证提示规定的判断顺序。
38. 当前第一阶段 Qwen 调用策略：每张源图一次调用，输入本图全部保留候选的 mask 隔离 crop/overlay，以及查询时全部 active 对象卡片和每个对象最多2张最近参考图；不再逐候选调用。
39. 脚本不计算类别、属性或描述相似度，也不做 Top-K/Top-8 语义 shortlist；它只整理输入、校验候选 ID 完整性、限制 existing 只能引用本次提供的对象 ID，并把通过校验的结果持久化。
40. Qwen 对每个候选必须依次输出：有效性及理由、有效候选的临时结构化标注、全卡片比较后的 new/existing/uncertain、最终标注。ignored 的临时和最终标注必须为空；有效候选两份标注都必须存在。
41. `new` 使用最终标注建档；`existing` 追加 observation，并用最终累计标注更新已有对象卡片。临时标注和最终标注同时保存在运行报告及原始响应中，便于检查记忆比较是否不当改变候选语义。
42. 当前阶段直接发送全部卡片，适合小规模第一阶段记忆库；若未来图片数使视觉上下文接近模型上限，应采用“对象卡片显式分批 + Qwen 汇总”，不能悄悄恢复脚本相似度筛选。
43. 单图批量 gate 报告生成于 2026-07-31 13:41:34（UTC+08:00）：7个候选只触发1次 Qwen，批量 JSON 完整覆盖、全部写入终态，无 pending/failed；通用结构 checks 全部通过。
44. 业务语义 gate 未通过：`000131`、`000067`、`000155` 正确 new，`000208`、`000100`、`000046` 正确 ignored，但 `000006` 被识别为“插入杯盖中的深红色塑料吸管”并错误 new，形成4个对象而非预期3个。
45. 顶层 `status=passed` 不能替代按候选 ID 的产品验收；它只证明报告生成、阶段顺序、候选覆盖和持久化结构正常。最新报告使用空记忆库，不能证明有真实对象卡片时的 existing 归并质量；服务器单元测试结果也未进入报告。
46. 最新批量 Qwen 调用输入9712 token、输出1675 token、耗时109.945秒、峰值显存13530.58 MiB。相比 gate v2 的7次逐候选调用总计41.389秒、17757 input token、591 output token，批量方式减少输入 token 和调用次数，但生成更长且总延迟约2.66倍；本阶段保留该性能基线，不做针对单图的输出长度优化。
47. 用户于2026-07-31明确决定保留 `000006` 配件误建档问题并记录但不优化。该问题从 M5-B 阻塞项降为已接受限制；不调整提示、SAM 阈值或调用次数，不再生成单图 gate v2，直接用多视角批次评估整体发现、归并和错误率。
48. 本地验收集共28个中性文件名、14个唯一 SHA-256 和14个完全副本；按文件名排序后首次有效观测应新建3个具体实例，后续有效视角应归并，重复文件应在源图哈希门跳过。饮料杯吸管在真值中属于杯子配件；真值表仅留本地，不进入模型输入。
49. 文档信息架构已明确分层：README 只承担对外项目介绍和首次了解入口，不保存版本、里程碑、下一动作或 AI 维护规则；AGENTS 承担 AI 协作、当前管理摘要、开发规范、职责矩阵和版本/Git 规则；PROGRESS 继续作为当前执行状态的唯一详细事实源。
50. 用户决定当前小规模 Demo 的 `data/` 与 `runs/` 全量进入 Git：先把二十余张输入图、SQLite、中间处理图、原始响应和历史验证产物完整同步到本地，再依据实际内容与代码引用清洗测试遗迹。模型权重、缓存、Conda 环境和根目录隐藏真值继续忽略；目录存在仍不能代替验收报告。
51. 服务器提交 `2cda2e0` 已完整拉取：`environment/m5_end_to_end_report.json` 与验收库内部 run report 完全一致；报告、SQLite 和资产引用互相吻合，14个重复文件全部在 SAM/Qwen 前被 SHA-256 幂等门跳过。
52. `--validate-phase-one` 的当前实现只检查数量和终态覆盖，不读取真值。它要求至少2个 new、1个 existing、1个重复跳过、1个 filtered/ignored 且无 pending/failed；因此本轮 `status=passed` 只能判定执行结构通过，不能判定对象记忆产品通过。
53. 隐藏真值只有同一水瓶、同一鼠标、同一饮料杯3个实例；实际8个对象中，水瓶被拆为两个 ID，另一个水瓶 ID 又吸收6个饮料杯观测，鼠标 ID 混入11个固定装置观测，另外5个对象完全属于泡沫板、线缆或固定装置。该失败远超已接受的单个配件误建档，不能并入 `grid_point_000006` 豁免。
54. 59个 accepted observations 中，逐图裁剪核对得到29个目标/目标部件观测和30个背景/固定装置观测；同一 object-source 还存在18条重复观测，说明同图重叠候选会重复写入同一对象，后续语义修复需同时考虑有效性、同图候选合并和实例身份约束。
55. 本轮总耗时约25分48秒，其中Qwen 14次源图级调用耗时约24分11秒，是主要性能成本；SAM加载8.90秒、推理64.47秒、峰值5391.77 MiB，Qwen加载3.30秒、峰值14512.97 MiB。
56. 本次提交没有单元测试执行摘要，不能补推“测试已通过”。后续服务器复验必须把单元测试结果作为独立证据回传，不得仅凭模型运行报告代替。
57. `data/` 与 `runs/` 共715个文件、约62.20 MiB。明确可清理项为所有 `.ipynb_checkpoints/`、与 `m5_input/` 完全重复的 `data/temp/`、重复的 `runs/smoke/` 输出、旧 gate 输入/记忆库和已被 v2 替代的 `runs/m5_auto_candidates/`；当前失败验收库、固定输入、M5-A v2证据及仍被脚本引用的M2/M3资产暂保留。根目录 `m5_candidate_preview.tar.gz` 也是与已展开 M5-A v1 内容重复的历史预览包，列入待确认清理项。

## 现在执行

先冻结失败证据，再把 Demo 清洗和语义修复拆开处理：

1. 保留 `environment/m5_end_to_end_report.json` 与整个 `data/m5_validation_memory_01/`，不把它改名或当作干净 Demo 基线。
2. 由用户确认待清理清单：`.ipynb_checkpoints/`、`data/temp/`、根目录 `m5_candidate_preview.tar.gz`、重复 `runs/smoke/`、三个历史 gate 记忆库及输入、`runs/m5_auto_candidates/` v1。
3. 确认后只做目录清洗与忽略规则补充，不夹带模型策略修改；`data/m5_input/`、失败验收证据、M5-A v2及当前代码仍引用的M2/M3资产先保留。
4. 单独决定最小语义修复优先级：候选有效性过滤、同一源图重叠/新对象合并、existing 实例身份保守判定。已接受的单图吸管配件问题继续不做针对性优化。
5. 修复后使用全新 `data/m5_validation_memory_02/` 重跑同一28图集合，并同时回传单元测试摘要；验收必须比较预期3个实例、逐图对象 ID 和无效对象数，不再只看 `phase_one_coverage`。

## 第一阶段边界

本看板不加入认知完整度、缺失视角分析、主动视角规划、机械臂控制、空间地图、因果推理或完整世界模型；这些均不属于第一阶段验收。
