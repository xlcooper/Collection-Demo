# 实验看板与审计分析

> **维护规则（强制）**：本文件记录已经真实发生的正式实验、逐环节输入输出、机器证据、审计方法与结论、里程碑、当前关键判断和实验历史，不维护未来计划或下一动作。机器运行事实以原始报告、SQLite 和资产为准；产品验收以未提供给模型的真值和逐图人工核对为准。新实验完成后，应刷新“当前实验”并把被替换实验压缩进历史；不得回写原始报告或静默改写历史结论。当前协作方向由 `AGENTS.md` 的管理摘要维护。

**最后更新**：2026-07-31 18:35:46 +08:00

## 管理方式与证据原则

- **只记录已发生事实**：实验目的可以说明，但未执行的修复、复验或开发安排不写成本次实验流程。
- **区分两层结论**：`status=passed` 只证明报告声明的结构条件通过；对象是否真实、是否属于同一具体实例，必须另做产品真值审计。
- **原始证据不可回写**：外部报告、记忆库内部报告、SQLite、原始响应和图像资产保持实验生成时的内容。目录整理可以移动文件，但必须说明旧路径与新路径的对应关系。
- **当前实验与历史分开**：最新正式实验保留完整过程和分析；旧实验只保留日期、核心结果、当时结论及证据位置，避免用重复的“当前状态”覆盖历史。
- **纠错必须显式**：若后续发现计数或判断错误，应直接修正当前说明，并在实验历史中注明纠错依据。
- **文档分工**：本文负责实验和审计；`AGENTS.md` 负责协作规则与当前管理方向；`CHANGELOG.md` 只记录已经提交的版本变化。

## 当前实验看板

- **当前代码版本**：v0.16.0
- **当前正式实验版本**：v0.15.2；v0.16.0 尚无服务器实验结果
- **当前关卡**：M5-B——Qwen 接管与第一阶段端到端验收
- **正式实验**：`run_20260731T061742209430Z`
- **当前基线**：固定输入 `data/input/`、完整输出 `data/memory/`、外部报告 `environment/demo_run_report.json`
- **实验结论**：运行链、哈希幂等、报告生成、SQLite 持久化和资产引用通过；对象有效性与具体实例身份未通过
- **产品真值**：本次验收目标只有同一水瓶、同一鼠标、同一饮料杯，共3个具体实例；系统最终建立8个对象
- **已接受限制**：历史单图 gate 的 `grid_point_000006` 吸管配件误建档只记录、不针对性优化；该豁免不包含本次大规模背景污染、跨实例错并和同实例拆档
- **第一阶段范围**：只验收自动发现、结构化标注、对象建档、跨图归并、幂等和持久化；主动采集、视角规划、机械臂、认知完整度和世界模型不在本次实验内

## 本次实验：M5-B 多视角端到端实验

### 实验目的与证据

本次实验要验证：从空记忆库开始，系统能否在无人逐图确认的情况下处理一批多视角图片，自动发现候选、判断有效性、建立对象档案，并把同一具体物体的后续视角归入同一档案。

| 项目 | 真实情况 |
|---|---|
| 运行 ID | `run_20260731T061742209430Z` |
| 运行时间 | 2026-07-31 14:17:42.209603 至 14:43:30.076806（UTC+08:00） |
| 服务器回传 | 提交 `2cda2e0` |
| 外部报告 | 当前为 `environment/demo_run_report.json` |
| 内部报告 | `data/memory/run_reports/run_20260731T061742209430Z.json`；与外部报告内容及 SHA-256 完全一致 |
| 数据库 | `data/memory/memory.sqlite` |
| 图像与模型证据 | `data/input/`、`data/memory/sources/`、`data/memory/proposals/`、`data/memory/objects/`、`data/memory/raw_responses/` |
| 运行配置指纹 | `config_digest=dbcc019ec459a66c40ac820ac012378af4cb8e6e53514ea6bd6d89f24571c94e` |
| 模型与提示版本 | SAM3 `weights/sam3/sam3.pt`；Qwen `Qwen/Qwen3-VL-8B-Instruct-FP8`；72条 decision 均为 `m5-image-batch-memory-reasoning-v1` |
| 产品真值 | 本地 `temp/ground_truth.md`；只在运行后审计，未提供给服务器模型 |

实验实际运行时，输入目录和记忆根仍名为 `data/m5_input/`、`data/m5_validation_memory_01/`。v0.15.0 只把它们整理为当前的 `data/input/`、`data/memory/`，没有改写原始报告，所以报告仍保留旧绝对路径、`test=m5_end_to_end_batch` 和 `phase_one_coverage` 字段。当前代码的新名称是 `--validate-demo`、`object_memory_demo_batch`、`demo_coverage` 和提示版本 `image-batch-memory-reasoning-v1`，不能倒写成本次实验当时已经使用。

### 输入与隐藏真值

输入共有28个场景文件，文件名为 `img_0001.jpg` 至 `img_0028.jpg`。按原始字节计算 SHA-256 后，只有14个唯一文件，另14个是逐字节完全相同的副本：

`0002→0001`、`0007→0004`、`0010→0005`、`0014→0003`、`0016→0006`、`0018→0008`、`0020→0009`、`0022→0012`、`0023→0015`、`0024→0017`、`0025→0019`、`0026→0011`、`0027→0021`、`0028→0013`。

隐藏真值只包含以下3个具体实例：

| 真值实例 | 独立对象边界 |
|---|---|
| 同一个透明水瓶 | 白色瓶盖和提手属于水瓶，不另建档 |
| 同一个白色无线鼠标 | 鼠标整体为一个对象 |
| 同一个透明饮料杯 | 红色封膜和红色吸管属于杯子，不另建档 |

桌面、泡沫板、隔板、线缆、固定装置、阴影和反射都不是本次目标。真值只用于运行后的人工审计，因此不会帮助模型得到更好的结果。

### 真实执行流程与逐环节产物

数量关系如下：

```text
28个输入场景文件
→ 14个唯一文件进入模型，14个完全副本在模型前跳过
→ SAM3 产生3584个原始 mask 候选
→ 项目脚本过滤3512个，保留72组候选
→ Qwen 将13组判为 ignored，接纳59组为对象观测
→ 59条观测最终写入8个对象档案
```

| 环节 | 实际做了什么 | 本次输入 | 本次输出 |
|---|---|---|---|
| 1. 文件发现与哈希门 | 递归发现支持的图片并按路径排序；读取原始字节计算 SHA-256。相同哈希在加载模型前直接跳过 | 28个文件 | 14个 canonical source；14个 duplicate，后者的 SAM/Qwen 计数均为0 |
| 2. SAM3 候选生成 | SAM3 只加载一次；对每张唯一图生成 `16×16` 点网格，32点一批。每个网格点的多个 mask 只保留分数最高的1个 | 14张唯一图 | 每图256个、合计3584个原始候选。这里的“原始候选”是每个网格点的最佳 mask，不是3584个物体 |
| 3. 项目脚本后处理 | SAM3 输出后，由 `sam3_postprocess.py` 按置信度、面积、mask IoU、包含率和数量上限过滤；这不是 SAM3 模型内部规则 | 3584个原始候选 | 过滤3512个，保留72组候选 |
| 4. 候选资产生成 | 只为保留候选生成审计资产；被脚本过滤的候选只记录状态和原因，不生成图片、不进入 Qwen | 72组候选 | 每组1个 `crop.png`、1个 `mask.png`、1个 `overlay.jpg`，共216个候选资产 |
| 5. 模型切换 | 14张图的 SAM3 处理全部完成后释放 SAM3，再加载 Qwen，保持单卡顺序驻留 | SAM3 结果 | Qwen 阶段开始；两个模型不同时常驻 |
| 6. Qwen 源图级判断 | 每张唯一源图调用1次，共14次。一次调用包含该图全部候选的 crop 与 overlay、调用前全部 active 对象卡片，以及每个对象最多2张最近参考 crop。卡片文本包含累计类别、材质、颜色、形状和描述 | 72组候选、逐步增长的对象记忆 | 14份完整 JSON 原始响应；每个候选恰好有1个终态决定 |
| 7. 有效性、标注与身份 | Qwen 在同一次生成中依次做有效性判断、临时结构化标注、与已有卡片比较、`new/existing/uncertain` 判断和最终累计标注；这些是逻辑步骤，不是多次独立调用 | 每张图的候选和当时已有对象卡片 | 8 `new`、51 `existing`、13 `ignored`、0 `uncertain` |
| 8. 记忆写入 | `new` 新建对象并写首条 observation；`existing` 追加 observation 并更新累计标签；`ignored` 只记 decision；`uncertain` 应保持 pending，但本次没有 | 72个 Qwen 决定 | 59条 observation、8个 active object、72条 decision |
| 9. 报告与结构校验 | 汇总运行、模型、图片、候选、决定和持久化计数，并执行脚本预设的覆盖检查 | 完整运行状态 | 外部报告与内部报告各1份，顶层 `status=passed`；`phase_one_coverage` 下5项子检查全部为 `true` |

脚本后处理的本次实际过滤明细：

| 过滤原因 | 规则 | 数量 |
|---|---|---:|
| `low_confidence` | 分数低于0.88 | 1469 |
| `mask_too_large` | mask 面积超过原图的50% | 1212 |
| `duplicate_mask` | 与已保留高分 mask 的 IoU ≥ 0.90 | 814 |
| `contained_mask` | 较小 mask 至少90%落在已保留的更大高分 mask 内 | 17 |
| **合计** |  | **3512** |

`crop.png` 是候选包围框外扩8像素后的裁切图，但 mask 外部统一填成中性灰 `[127,127,127]`；`mask.png` 是生成 crop 和 overlay 时所用内存 mask 的持久化副本；`overlay.jpg` 用于显示候选在完整场景中的位置。Qwen 实际接收 `crop + overlay`，`mask.png` 不直接发送，只用于持久化和事后审计。

第一张唯一图调用时对象库为空，因此其中2个有效候选都必须为 `new`。从后续图片开始，Qwen 才能比较前面图片已经落库的对象。提示允许 Qwen 在同一源图的候选之间排除冗余或非独立局部，但 `existing` 只能引用调用前已经提供的对象 ID；同批次刚判为 `new` 的对象要在整张图响应完成后才创建，也没有第二道同图归并。

### 逐图执行明细

下表只列实际进入 SAM3/Qwen 的14张唯一图；其余14张副本已在哈希门跳过。

| 源图 | SAM保留候选 | 调用前对象卡 | 参考图 | `new` | `existing` | `ignored` |
|---|---:|---:|---:|---:|---:|---:|
| `img_0001.jpg` | 2 | 0 | 0 | 2 | 0 | 0 |
| `img_0003.jpg` | 7 | 2 | 2 | 0 | 4 | 3 |
| `img_0004.jpg` | 6 | 2 | 4 | 1 | 4 | 1 |
| `img_0005.jpg` | 7 | 3 | 5 | 1 | 4 | 2 |
| `img_0006.jpg` | 2 | 4 | 7 | 0 | 2 | 0 |
| `img_0008.jpg` | 6 | 4 | 7 | 0 | 6 | 0 |
| `img_0009.jpg` | 3 | 4 | 8 | 0 | 3 | 0 |
| `img_0011.jpg` | 8 | 4 | 8 | 4 | 4 | 0 |
| `img_0012.jpg` | 5 | 8 | 12 | 0 | 5 | 0 |
| `img_0013.jpg` | 6 | 8 | 13 | 0 | 6 | 0 |
| `img_0015.jpg` | 6 | 8 | 13 | 0 | 3 | 3 |
| `img_0017.jpg` | 4 | 8 | 13 | 0 | 2 | 2 |
| `img_0019.jpg` | 4 | 8 | 13 | 0 | 3 | 1 |
| `img_0021.jpg` | 6 | 8 | 13 | 0 | 5 | 1 |
| **合计** | **72** | — | **输入总次数118** | **8** | **51** | **13** |

14次调用全部在第一次 pipeline attempt 成功，没有模型异常重试或外部错误。候选部分共向 Qwen 发送144张图（72组 × crop/overlay），对象参考图累计输入118次；同一参考资产可以在不同源图调用中再次出现。

### 持久化输出与性能

| 输出 | 数量 | 说明 |
|---|---:|---|
| run | 1 | 状态 `completed` |
| source | 14 | 28个输入中14个唯一源图 |
| proposal | 3584 | 3512 `filtered`，72 `decided` |
| decision | 72 | 每个进入 Qwen 的候选恰好1条 |
| observation | 59 | 8 `new` + 51 `existing` |
| object | 8 | 全部为 active |
| source 文件 | 14 | 当前位于 `data/memory/sources/` |
| proposal 资产 | 216 | 72组 × crop/mask/overlay |
| observation 资产 | 177 | 59条 × crop/mask/overlay 的对象目录副本 |
| Qwen 原始响应 | 14 | 每张唯一源图1份 |
| 运行报告 | 内外各1份 | 两份内容完全一致 |

候选和对象观测共393个资产引用全部存在；14个 source 引用也全部存在。SQLite `integrity_check=ok`，没有外键违规。

| 性能项 | 本次结果 |
|---|---|
| 总耗时 | 1547.867秒，约25分48秒 |
| SAM3 | 加载8.904秒；14图推理64.468秒；峰值5391.77 MiB |
| Qwen | 加载3.302秒；14次推理1450.920秒；输入137122 token；输出22923 token；峰值14512.97 MiB |
| Qwen 部署 | GPU 0；`Qwen3-VL-8B-Instruct-FP8` snapshot `9cdc6310a8cb770ce18efaf4e9935334512aee45` |

Qwen 推理约占总耗时的93.7%，是当前主要性能成本。

## 审计分析

### 审计是如何完成的

审计分成两个独立层次：

1. **机器一致性审计**
   - 比较外部报告与记忆库内部报告的 SHA-256。
   - 对 SQLite 执行完整性和外键检查。
   - 对照报告与六张核心表的 run、source、proposal、object、observation、decision 数量。
   - 检查 source、crop、mask、overlay、raw response 和内部报告引用是否真实存在。
   - 按 SHA-256 核对14个唯一文件与14个完全副本的映射，确认副本在 SAM3/Qwen 前跳过。

2. **产品真值审计**
   - 使用未提供给模型的 `temp/ground_truth.md` 作为答案表。
   - 人工逐一核对59条已接纳 observation 的裁切内容，并结合对应 source/overlay 的场景位置；需要确认边界时再参考 mask。
   - 将每条观测标为目标、目标部件、背景或固定装置。
   - 再按 `object_id` 和 `source_id` 汇总，检查同一实例是否被拆档、不同实例是否被错并、同图候选是否产生多重写入。

### 机器一致性审计结果

| 审计项 | 结果 | 判断 |
|---|---|---|
| 输入与哈希幂等 | 14个唯一 SHA-256 登记为 source；14个副本全部在模型前跳过 | ✅ 通过 |
| 候选终态 | 3512 `filtered`、72 `decided`；无 pending/failed | ✅ 通过 |
| Qwen 输出覆盖 | 72个候选全部且仅有1个决定；无杜撰 proposal ID | ✅ 通过 |
| 报告与数据库 | 1 run、14 sources、3584 proposals、8 objects、59 observations、72 decisions 完全一致 | ✅ 通过 |
| SQLite | `integrity_check=ok`，无外键违规 | ✅ 通过 |
| 资产引用 | 所有 source、候选资产、对象观测资产、原始响应和内部报告存在 | ✅ 通过 |
| 单元测试证据 | 本次外部/内部报告和提交没有测试执行摘要 | ⬜ 无法确认服务器是否执行或通过单元测试 |

因此，本次实验的工程运行链和持久化结构是闭合的。

### 产品真值审计结果

| 审计项 | 真实结果 | 判断 |
|---|---|---|
| 对象数量 | 真值3个具体实例，系统建立8个对象 | ❌ 失败 |
| 接纳候选有效性 | 59条 observation 中，29条属于目标或目标部件，30条属于背景或固定装置 | ❌ 失败 |
| 实例分离 | 水瓶与饮料杯发生跨实例合并 | ❌ 失败 |
| 同实例归并 | 同一个水瓶被拆成两个对象 ID | ❌ 失败 |
| 档案纯度 | 鼠标档案混入11条固定装置观测 | ❌ 失败 |
| 纯无效对象 | 5个对象完全由泡沫板、线缆或固定装置组成 | ❌ 失败 |
| 同源多重观测 | 12组相同 `object_id + source_id` 组合共有30条观测；若每组保留1条，多出18条 | ❌ 失败 |
| 发现覆盖 | 每张唯一图至少有一个真实目标候选被接纳 | 🟡 只证明覆盖，不能证明对象记忆正确 |

逐对象核对如下：

| 对象 ID | 数据库最终标签 | 观测数 | 人工核对内容 | 审计结论 |
|---|---|---:|---|---|
| `obj_9a792143…` | plastic cup | 8 | 2条水瓶 + 6条饮料杯 | 水瓶与饮料杯错并，累计标签被污染 |
| `obj_04b48fc4…` | plastic bottle | 12 | 水瓶 | 与上一个对象共同造成同一水瓶拆档 |
| `obj_c78b6944…` | computer mouse | 20 | 9条鼠标 + 11条固定装置 | 鼠标档案严重污染 |
| `obj_c3ea64b7…` | container / unknown | 8 | 全部为背景或固定装置 | 纯无效对象 |
| `obj_8ff30c6f…` | foam board | 8 | 全部为泡沫板 | 纯无效对象 |
| `obj_cf0844c8…` | foam board | 1 | 泡沫板 | 纯无效对象 |
| `obj_d2662fcf…` | foam board | 1 | 泡沫板 | 纯无效对象 |
| `obj_6f416072…` | foam board | 1 | 泡沫板 | 纯无效对象 |

### 已证实的薄弱环节

- **目标有效性与验收边界未对齐**：相对于本次隐藏真值，Qwen 接纳了30条背景或固定装置观测。其中既有应由提示排除的背景、纹理或非独立区域，也有本身是物理实体、但被本次真值排除的固定装置和线缆；因此问题同时包含模型有效性误判和产品目标边界未完整编码。
- **同图候选缺少可靠闭环**：提示允许 Qwen 排除同图冗余，但落库前没有确定性的同图归并或第二次检查，最终出现18条同 `object-source` 超额观测；该分组证明多重写入，不单独证明每组 mask 都达到几何重复阈值。
- **实例身份约束不足**：程序只校验 `existing` 引用的是已提供对象 ID 且置信度不低于0.8，不能证明模型选中的确实是同一具体实例，因此出现水瓶/饮料杯错并和鼠标档案污染。
- **空库与同批新对象边界**：空记忆时有效候选必须 `new`；同一响应中新建的对象还没有可引用 ID。若同图冗余候选未被判 `ignored`，可能同时创建多个档案。
- **自动验证器存在盲区**：原始 `phase_one_coverage` 只检查至少2个 `new`、1个 `existing`、1个哈希跳过、1个 filtered/ignored 且无 pending/failed，不读取隐藏真值，也不验证 `new/existing` 的身份正确性。
- **测试证据缺失**：模型运行报告不能替代单元测试执行摘要，本次实验不能据此声明单元测试通过。

### 最终验收结论

> 本次实验的运行链、哈希幂等、候选终态、报告生成、SQLite 完整性和资产持久化通过；但目标有效性、同图多重观测控制和具体实例身份均未达到产品要求，因此 M5-B 第一阶段产品验收未通过。

## 里程碑

| 里程碑 | 状态 | 最小交付物 / 完成条件 | 已有证据与结论 |
|---|---|---|---|
| P0 项目基线 | ✅ | 第一阶段范围、开发准则、Git 与环境流程固化 | `README.md`、`AGENTS.md` 和当前环境/业务指南 |
| M0-A 服务器环境 | ✅ | Conda、CUDA、SAM3 及 Qwen 依赖可用，0个阻塞项 | `environment/server_env_report.json`：0 blocker、2 warnings |
| M0-B SAM3 单模型冒烟 | ✅ | 真实加载 checkpoint，并输出 proposal 和 mask | `cup` 得到1个 proposal，score 0.8867；推理0.462秒、峰值5087.16 MiB；详细报告仅存 Git 历史 |
| M0-C Qwen 单模型冒烟 | ✅ | 8B FP8 加载成功并输出可解析 JSON | 98个输出 token、13.32 token/s、峰值10439.92 MiB；详细报告仅存 Git 历史 |
| M0-D 双模型顺序运行 | ✅ | 同一环境先后完成 SAM3、Qwen，无显存或磁盘阻塞 | 服务器提交 `59bd3a7`；历史报告仅存 Git 历史 |
| M1 数据骨架 | ✅ | 配置、统一数据类型、SQLite schema、文件路径和最小 CLI | 六张核心表、空库查询和重复初始化通过；历史报告仅存 Git 历史 |
| M2 SAM3 流水线 | ✅ | 候选策略、mask 去重及 crop/mask/overlay 资产 | 显式 `cup/mug` 接口验证和 IoU 去重通过；显式类别策略不作为当前主流程；历史报告仅存 Git 历史 |
| M3 MLLM 流水线 | ✅ | 对象卡片、身份判断、结构化标注和 JSON 校验 | 空记忆判 `new`、同一视图判 `existing` 且 ID 正确；历史报告仅存 Git 历史 |
| M4 记忆闭环 | ✅ | 新建/归并、幂等、pending/failed、事务写入与回滚 | 确定性临时 SQLite 验证10项通过；历史报告仅存 Git 历史 |
| M5-A 无类别候选校准 | ✅ | 不提供类别词，SAM3 自动候选覆盖真实目标并降低明显噪声 | 单图从256个原始候选校准至7个，3/3真实物体仍保留；历史报告仅存 Git 历史 |
| M5-B 第一阶段验收 | 🟡 | 单命令、无人确认地完成发现—判断—标注—存储—归并，并通过结构与产品真值审计 | v0.15.2 正式实验结构通过但产品未通过；v0.16.0 两阶段定向方案已实现，尚无服务器测试或实验结果 |

## 当前关键判断

1. M5-B 仍未关闭；工程结构通过不能替代产品语义通过。
2. `data/input/`、`data/memory/` 和 `environment/demo_run_report.json` 是当前可回溯基线；原始报告中的旧路径和旧字段属于真实运行历史，不应回写。
3. “28张”是输入场景文件，“14张”是实际进入模型的唯一场景，“3584个”是网格点最佳 mask 候选，“72组”是脚本后处理保留候选，“59条”是 Qwen 接纳并写库的 observation；这些计数不能互相替代。
4. `filtered` 是项目脚本在 Qwen 前淘汰；`ignored` 是 Qwen 看过候选后判无效。几何过滤属于项目后处理，不是 SAM3 模型内部能力。
5. 在 v0.15.2 正式实验中，Qwen 每张唯一源图只调用一次；有效性、临时标注、身份比较和最终标注属于一次 JSON 生成中的逻辑顺序。v0.16.0 增加了 SAM3 前的首轮场景观察，调用关系以第15条为准。
6. 记忆按源图递增：第一张图为空库，后续图片只能比较此前已落库对象；同批新对象不能作为本次 `existing` 的引用目标。
7. `existing` 通过 schema、对象 ID 范围和置信度校验，只代表输出在接口上合法，不代表人工真值确认是同一具体实例。
8. 当前主要产品问题是目标有效性污染、跨实例错并、同实例拆档和同图多重写入；这四类问题均有本次资产与 SQLite 证据。
9. 单图 `grid_point_000006` 配件误建档的已接受限制只覆盖那一个历史样例，不能豁免本次30条背景/固定装置观测和身份错误。
10. 顶层 `status=passed`、旧 `phase_one_coverage` 和当前 `demo_coverage` 都是结构覆盖信号，不是产品验收结论。
11. Qwen 占本轮总耗时约93.7%，但当前性能可运行；在语义正确性达标前，不能用减少审计或放宽判断换取速度。
12. 当前 `data/memory/` 已登记14个唯一 SHA-256；再次输入同一28图集合会被幂等门全部跳过，因此复用该库不能产生新的全量比较证据。
13. **记录在案的存储优化判断（尚未实现）**：候选有效并归入 `existing`，不应自动等同于值得新增长期观测；还应判断它是否提供新的视角、外观、状态或身份信息。高度重复、信息增量很低的视角可以只保留 decision 和资产引用，不新增 observation。对象记忆优先保留有差异的 `crop` 作为后续参考；`overlay` 继续在 proposal 审计层保留一份并建立引用，无需再复制到对象 observation 目录。当前实现仍会为每个 `new/existing` 写 observation，并复制 `crop/mask/overlay`，因此该判断需要通过后续实验验证。
14. **v0.16.0 代码状态不等于实验结论**：现行代码已经实现“幂等门后首轮 Qwen 场景观察 → SAM3 文本概念定向分割与脚本筛选 → 第二轮 Qwen 候选/记忆判断”，但尚未在服务器执行单元测试或真实模型推理；本页前文的3584、72、59和8仍只属于 v0.15.2 点网格实验。
15. **保留默认4图批次设计**：若仍有14张唯一新图，且每张都产生候选、没有重试，首轮是4次调用（4+4+4+2），第二轮最多14次逐源图调用，正常合计最多18次；无目标或无 SAM 保留候选的源图会跳过第二轮。这里的“两次调用”是两个逻辑阶段，不是每张图固定调用两次。
16. 首轮严格位于 SHA-256 幂等门之后，逐批校验每个 `source_id` 恰好出现一次；批次重试仍失败时拆为单图救援，单图仍失败才停止该 source。默认不静默回退历史点网格，否则运行策略和实验结果不可比较。
17. SAM3 文本模式的0.5分数阈值是新的起始值，不等于历史点网格0.88质量阈值；SAM3 processor 返回前已先按文本置信度过滤，所以新报告称其为“模型阈值后检测”，再由脚本执行面积、跨概念高 IoU 去重、同概念包含过滤和候选上限。
18. 候选上限先为每个仍有检测的文本概念保留最高分 mask，再按全局分数补足；不同概念之间不执行包含删除，交给第二轮 Qwen 判断独立性。这样降低某个高频概念挤掉其他目标的风险，代价是可能保留更多低分或附属部件候选。
19. **两阶段方案的 trade-off**：首轮可减少背景与第二轮视觉输入，但漏掉的物体无法由后续恢复；概念过窄会漏异形对象，过宽会增加多实例和重复候选；4图一批减少请求数，但提高跨图混淆、长 JSON 和批次连带失败风险；两次 Qwen 驻留增加加载与首轮推理成本，但可能由更少的 SAM/第二轮候选抵消；固定物排除越严格，背景污染越少，也越可能漏掉任务需要操作的可拆部件；第二轮必须把上游名称当成可错的检索假设，以降低确认偏差。
20. 新流程的首轮 `target_id`、优先级、置信度、原因和原始响应保存在运行报告/原始响应中；proposal 持久化实际 `sam_text_prompt`，通过 `source_id + sam_text_prompt` 与首轮唯一目标对接。SQLite 尚未为首轮目标新增独立表，这是当前审计边界，后续是否升级只能依据新实验需求决定。

## 实验与决策历史

以下已删除报告的路径均指向提交 `8b53111` 及更早的 Git 历史；它们不在 v0.15.0 当前树中。

| 时间 | 实验或决策 | 真实结果 | 对当前方案的影响 | 证据 |
|---|---|---|---|---|
| 2026-07-30 | M0 环境与双模型冒烟 | SAM3、Qwen 均可在 RTX 4090 上真实推理，顺序驻留无阻塞 | 固定为单卡先 SAM3、释放后 Qwen | 当前 `environment/server_env_report.json`；历史 `environment/sam3_smoke_report.json`、`environment/qwen_smoke_report.json` |
| 2026-07-30 | M1 数据骨架 | 六张 SQLite 核心表、配置、CLI 和空库查询通过 | 建立可审计持久化地基 | 历史 `environment/m1_skeleton_report.json` |
| 2026-07-30 | M2 SAM3 接口验证 | `cup/mug` 显式提示和 IoU 去重通过，但通用文本 `object` 返回0候选 | 显式类别只证明接口，不满足无外部类别词的产品要求 | 历史 `environment/m2_sam3_pipeline_report.json` |
| 2026-07-30 | M3 Qwen 身份验证 | 空记忆输出 `new`；同一 crop 与对象卡片输出正确 `existing` | 建立 crop/overlay、对象卡片和结构化 JSON 链路 | 历史 `environment/m3_mllm_pipeline_report.json` |
| 2026-07-30 | M4 记忆闭环 | 确定性假数据验证事务、回滚、幂等和 pending/failed；未加载真实模型 | 真实批量模型编排留给 M5 | 历史 `environment/m4_memory_loop_report.json` |
| 2026-07-30 | M5-A v1 无类别候选 | 256个原始候选保留12个；3个真实物体都覆盖，但仍有桌面、局部和阴影 | 只证明覆盖，不满足候选纯度 | 历史 `environment/m5_auto_candidate_report.json` |
| 2026-07-30 | M5-A v2 几何校准 | 最大面积比收紧至0.5并加入90%包含过滤；256个保留7个，3/3目标仍在 | 当前 SAM 后处理阈值由此形成；少量阴影留给 Qwen | 历史 `environment/m5_auto_candidate_report_v2.json` |
| 2026-07-31 | Qwen gate v1 | 计数为3 `new`、4 `ignored`，但真实候选 `000067` 被忽略，阴影 `000100` 因矩形 crop 含邻近杯子而误建档 | 证明正确计数会掩盖语义交换错误 | 历史 `environment/m5_qwen_gate_report.json` |
| 2026-07-31 | Qwen gate v2 | mask 外填中性灰后，阴影 `000100` 正确 ignored；但有效杯子 `000067` 被已有鼠标卡片干扰而 ignored | 修复矩形 crop 污染，同时暴露有效性与身份判断耦合 | 历史 `environment/m5_qwen_gate_report_v2.json` |
| 2026-07-31 | 单图源图级批量 gate | 7个候选只调用1次 Qwen；3个目标正确 `new`、3个噪声正确 `ignored`，`000006` 吸管配件错误 `new` | 用户接受该单个配件问题，不做针对性过拟合 | 历史 `environment/m5_image_batch_gate_report.json` |
| 2026-07-31 | v0.16.0 两阶段定向方案决策与实现 | 代码已改为幂等门后首轮 Qwen、SAM3 文本概念分割、脚本筛选和第二轮 Qwen；默认4图一批并支持单图救援，专业提示与结构化接口已固化 | 替代现行业务代码的点网格发现策略；旧实验继续作为失败基线，新方案效果等待服务器证据 | 当前代码、配置、回归测试与 `CHANGELOG.md`；尚无新运行报告 |
