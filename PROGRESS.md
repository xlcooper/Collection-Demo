# 实验看板与审计分析

> **维护规则（强制）**：本文件记录已经真实发生的正式实验、审计结论、里程碑、仍生效的关键判断和压缩后的实验历史，不维护未来计划或下一动作。机器事实以运行报告、SQLite 和资产为准；产品验收以未提供给模型的真值和逐图人工核对为准。原始产物清理后必须明确标注，不得把历史摘要伪装成仍可直接核验的当前证据。当前协作方向由 `AGENTS.md` 维护。

- **当前版本**：v0.16.1
- **最后更新**：2026-07-31
- **当前关卡**：M5-B——Qwen 接管与第一阶段端到端验收

## 当前实验看板

| 项目 | 当前事实 |
|---|---|
| 当前正式实验 | 暂无；v0.16 两阶段流程尚未在服务器形成真实模型结果 |
| 固定输入 | `data/input/`，共28个文件，其中14张内容唯一、14张为逐字节副本 |
| 当前记忆库 | `data/memory/` 的旧运行产物已清理，当前尚未生成新记忆 |
| 当前 Demo 报告 | `environment/demo_run_report.json` 已随旧结果清理，尚无新报告 |
| 现行代码状态 | 已实现“幂等门 → Qwen 场景观察 → SAM3 文本概念分割与脚本筛选 → Qwen 候选/记忆判断 → 持久化” |
| 验收状态 | 🟡 未通过；现行代码只有本地静态检查，不能用旧实验替代服务器验收 |

本次清理是用户明确授权的数据维护操作，不是一次模型实验，也没有改变历史实验结论。旧原始产物可从清理前的 Git 提交 `3151334` 恢复。

## 现行实现（尚未形成实验结论）

1. **哈希幂等门**：先计算 SHA-256；已完成的相同图片在任何模型调用前跳过，同一批逐字节副本只处理一份。
2. **第一轮 Qwen 场景观察**：默认每4张新图一批，判断哪些独立物体值得机械臂进一步观察，并输出受约束的英文 SAM3 文本概念。整批失败时拆成单图有限救援，不回退到旧点网格。
3. **SAM3 定向分割**：每张图只编码一次，再逐概念寻找匹配实例。这里的概念是检索提示，不是最终类别或身份结论。
4. **脚本后处理**：继续执行面积、高 IoU、同概念包含关系和候选上限筛选；优先保证每个有检测的概念至少保留最高分实例。
5. **第二轮 Qwen 判断**：按源图查看候选 crop、overlay、结构信息和递增对象卡片，先复核候选有效性，再为每个候选输出唯一终态 `new/existing/ignored/uncertain`。
6. **记忆持久化**：`new/existing` 形成 observation，并更新或创建对象；所有候选都有 decision，原始响应、资产和运行报告随记忆根保存。

上述流程只说明代码设计和接口边界。首轮目标召回、SAM3 候选量、背景接纳率、身份错并/拆档以及两轮耗时，均须由下一次真实服务器实验回答。

## 历史实验摘要与审计分析

### v0.15.2 点网格基线

- **运行 ID**：`run_20260731T061742209430Z`
- **输入**：28个文件，经哈希幂等门后实际处理14张唯一源图。
- **SAM3 原始输出**：每图16×16个采样点，每点取一个最高分 mask，共3584个原始候选。
- **脚本筛选**：面积、IoU、90%包含关系和每图上限共过滤3512个，留下72个候选组；每组包含同一候选的 crop、mask 和 overlay，不是72×3个候选。
- **Qwen 调用与决定**：按源图调用14次，保存14份完整原始响应；72个候选各有一个终态决定。
- **记忆结果**：59个候选被接纳为 observation，并聚合成8个 active object；其余候选没有进入对象观测记忆。因此“72个候选”不等于“72条对象记忆”。

### 审计结论

机器一致性检查曾确认哈希跳过、候选覆盖、SQLite 引用和资产路径闭环，但隐藏真值只有3个目标实例，结果却形成8个对象，产品验收失败：

- 59条接纳观测中有30条属于背景或固定装置；
- 水瓶与饮料杯发生跨实例错误合并；
- 同一水瓶被拆成两个对象 ID；
- 鼠标对象混入固定装置观测；
- 8个对象中有5个完全由背景或固定装置构成。

这证明旧流程的主要问题不是“没有生成候选”，而是无语义点网格产生了大量难以控制的局部区域，脚本只能依据几何关系降噪，无法稳定判断物体是否值得记忆，也无法独立解决具体实例身份。

旧实验的 SQLite、源图副本、候选/对象资产、14份 Qwen 原始响应、内部运行报告和外部 Demo 报告已在 v0.16.1 按用户授权从当前工作树清理。当前仓库只保留本摘要；逐文件证据需从 Git 提交 `3151334` 或更早历史恢复后核验。

## 里程碑

| 里程碑 | 状态 | 已证实边界 |
|---|---|---|
| M0 环境与双模型冒烟 | ✅ | RTX 4090 上 SAM3、Qwen 可顺序真实推理 |
| M1 数据骨架 | ✅ | SQLite、配置、CLI 和空库查询链成立 |
| M2 SAM3 接口 | ✅ | 显式文本提示接口可用；通用 `object` 不保证召回 |
| M3 Qwen 身份接口 | ✅ | 空记忆 `new`、同 crop 对卡片 `existing` 链成立 |
| M4 记忆闭环 | ✅ | 事务、回滚、幂等和失败状态经确定性测试验证 |
| M5-A 候选与单图 gate | ✅（有限） | 覆盖和单图批量判断成立；仍有一个已接受的配件误建档限制 |
| M5-B 端到端产品验收 | 🟡 | v0.15.2 基线失败；v0.16 两阶段方案尚未真实运行和验收 |

## 当前关键判断

1. **M5-B 仍未通过**：结构闭环通过不等于对象语义、具体实例身份和记忆纯度通过。
2. **v0.16 代码状态不是实验结果**：旧的3584→72→59→8只能说明点网格基线，不能证明两阶段方案效果。
3. **第一轮必须位于幂等门之后**：重复图不能消耗 Qwen 或 SAM3 推理。
4. **第一轮关注“值得机械臂深入观察的独立物体”**：优先可操作、任务相关、可移动、有识别价值的物体；排除墙面、桌面、阴影、反光和普通装修连接件，但可拆且可能被操作的部件不能仅因位置固定而排除。
5. **首轮概念决定召回上限**：漏掉的物体后续无法恢复；概念过窄会漏异形目标，过宽会增加多实例和重复候选。
6. **保留默认4图批次设计**：它减少请求数，但增加跨图混淆、长 JSON 和批次连带失败风险；单图救援只用于失败恢复。
7. **SAM3 文本分数与旧点网格质量分不可混用**：文本模式0.5是新的起始阈值；脚本后处理只能做几何降噪，不能替代语义判断。
8. **第二轮必须独立复核**：上游概念只作为可错的检索来源，不能成为候选有效性、类别或身份的确认依据。
9. **有效候选不必都成为长期记忆**：`existing` 还应判断是否带来新视角、外观、状态或身份信息；该信息增量门尚未实现。
10. **资产优化已记录但尚未实现**：对象记忆宜优先保留差异化 crop；overlay 在 proposal 审计层保留并引用，避免每条 observation 重复复制。
11. **原始记录清理不等于历史结论消失**：当前树不再保存旧结果，摘要用于理解决策；需要逐项复核时必须恢复对应 Git 历史。

## 实验与决策历史

| 日期 | 事件 | 结论 |
|---|---|---|
| 2026-07-30 | M0–M4 分阶段验证 | 双模型、持久化骨架和确定性记忆闭环成立 |
| 2026-07-30 至 2026-07-31 | M5-A 自动候选与 Qwen gate | 几何筛选和源图级批量判断可用，但不能保证候选语义纯度 |
| 2026-07-31 | v0.15.2 多视角端到端基线 | 结构检查通过，产品语义验收失败 |
| 2026-07-31 | v0.16.0 两阶段定向方案 | 完成代码实现，等待真实服务器验证 |
| 2026-07-31 | v0.16.1 旧记录清理 | 删除当前树中的旧记忆和 Demo 报告，保留固定输入、压缩摘要与 Git 可恢复历史 |
