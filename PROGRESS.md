# 实验看板与审计分析

> 本文用大白话记录当前实验处于什么状态、最近一次真实实验每一步发生了什么，以及审计后能得出什么结论。后续优化清单见 `docs/05_后续优化清单.md`。

- **当前版本**：v0.22.5
- **最后更新**：2026-08-07
- **当前关卡**：M5-B——端到端对象记忆验收
- **当前实验状态**：最新服务器运行的自动候选新流程实际通过：5张唯一图形成3个对象和8条观测；报告标红仅因旧Web错误套用“必须有重复图”的固定Demo门槛。但白色鼠标的类内特征被误写为“对称设计”。本次只推送外部报告，缺少配套输入与完整记忆根，M5-B仍未通过

## 1. 现在仓库里有什么

- `data/input/` 仍是上一轮固定数据：28个输入文件，其中14张内容唯一，另外14个是完全相同的副本；它不是最新5图实验的输入。
- `data/memory/` 仍保存上一轮两阶段流程的完整记忆库：1次运行、14张唯一源图、15个候选与决定、3个对象和15条观测；数据库、源图、候选、对象观测、原始回答和内部报告共121个文件，全部进入Git。
- `environment/run_report.json` 已更新为自动候选新流程运行 `run_20260807T122430273165Z` 的外部报告。对应5张输入、SQLite和完整记忆根没有随本次push进入Git，因此该报告不能与当前 `data/memory/` 混作同一实验，也不能在本地验证其资产引用。
- v0.16.1、v0.17.0和v0.17.4的旧产物仍只从Git提交 `f413654` 恢复；当前 `data/memory/` 只属于2026-08-03的两阶段实验。
- 最新外部报告实际记录SAM3自动点网格、DINOv3跨图候选聚类、Qwen聚类级审查和三模型顺序驻留，报告schema为8；这是v0.22新流程的第一次真实模型运行证据。
- v0.20.1只重组展示与文档；v0.20.2只同步最新实验事实和审计；v0.20.3只增加公开 Web 展示导航，不修改算法或实验产物。
- v0.20.4只新增当时尚未实施的 `DEMO优化方案.md`；v0.21.0 随后完成单次Qwen、DINOv3视觉指纹、双证据决定、新对象摘要、SQLite schema v3与Web展示改造。首次服务器回归共执行102项测试，其中101项通过；唯一错误是Web静态契约测试用已经删除的 `annotationBox` 定位代码片段终点，实际 `renderReasoningStage` 与三段证据展示均存在。v0.21.1已把测试边界改为真实的后继函数 `renderMemory`，尚待服务器复跑。历史 `data/memory/` 和报告没有改写，不能作为新流程证据。
- v0.21.2新增Web“一键清空输入”，只删除共享输入区当前可识别的图片，不回删任何记忆库中的源图副本、对象档案或报告；没有新增正式实验结果。
- v0.22.0弃用v0.21的逐图Qwen→SAM3文本提示设计，恢复SAM3固定点网格候选，在脚本过滤后用DINOv3组织跨图候选聚类，再由Qwen按聚类批次判断完整物体、背景/零件和对象摘要；三个模型改为分阶段顺序驻留。新增聚类接触表、报告schema 8和Web解释链，并修复进度事件与正式报告使用不同图片键时同一输入重复显示的问题。
- v0.22.0首次服务器回归共运行101项测试，其中100项通过；唯一错误是测试代码引用未赋值的 `pipeline`。v0.22.1修正该测试夹具。最新真实运行进一步证明业务流程可以完成，但本次push没有附带新的完整测试输出。
- v0.22.2取消Web自动启用固定Demo内容门槛，避免自由上传因“没有重复图”被误判失败；同时将自动分割按源图折叠，并在视觉聚类摘要统一解释编号、同图约束和CLS相似度。
- v0.22.4仅重绘并嵌入当前聚类优先架构海报，补充核心文档之间的导航和海报维护职责；算法、配置、实验产物与上述验收结论均未改变。
- v0.22.5修正Web阶段证据混淆：自动分割页完整展示SAM脚本保留的候选，不再因后续Qwen把杯盖、滚轮等候选判为忽略而少显示；最终语义状态仍只在后续页面解释。算法和实验产物未改变。

## 2. 如何阅读实验记录

稳定算法流程、编号含义、运行参数和报告字段统一由[业务代码运行与验证指南](docs/04_业务代码运行与验证指南.md)维护；数据目录契约见 [`data/README.md`](data/README.md)。本文只记录真实实验发生了什么及其审计结论，不再复制实现说明。

后文常用链路为 `run → source → proposal → cluster → decision → object / observation`：一次运行处理去重后的源图，SAM3先自动提出候选，DINOv3组织视觉聚类，Qwen审查后才可能形成长期对象或观测。

## 3. 最近一次真实实验发生了什么

最近一次服务器实验运行编号为 `run_20260807T122430273165Z`，报告生成于2026-08-07 20:28:31（北京时间）。本次Git只收到 `environment/run_report.json`，没有收到对应输入图片和记忆根。

```text
5个输入文件，内容均不重复
→ SAM3每图执行16×16点网格，共产生1280个原始候选
→ 脚本保留22个候选，其余按质量分、大小或重复关系过滤
→ DINOv3为22个候选生成视觉指纹，形成18个视觉聚类
→ Qwen按聚类分3批审查，用时约181.36秒
→ 3个聚类创建新对象，1个聚类归入已有对象，13个背景/零件聚类忽略，1个身份冲突聚类保留为uncertain
→ 最终写入3个对象、8条观测和9条对象决定
```

报告中的5个source均为 `completed`，8项结构检查全部通过，没有外部错误、失败候选或未结束候选。顶层 `status=failed` 与 `pipeline_status=passed` 并不矛盾：旧Web对空白库自动添加了 `--validate-demo`，该门槛要求输入中至少出现一张完全重复图片；本次5张图都不重复，因此只有 `exact_duplicate_image_was_skipped=false`，这不是模型或存储流程失败。

从报告文字可以看到三个对象分别是白色无线鼠标、带黑色胡须标记的白色杯和黑色无线鼠标。白色鼠标另有一个单视角聚类被归入已有对象；还有一个相似白鼠标聚类虽然Qwen判断为已有对象，但历史DINO证据不支持，最终保守地标为 `uncertain`。13个被忽略聚类主要是杯盖、鼠标滚轮和桌面区域，方向符合“完整对象建档、零件与背景不建档”的设计。

类内文字描述仍有明确错误：白色鼠标摘要把已知的人体工学非对称外形写成“对称设计”，品牌/标记也为空。这说明候选发现和对象归并成功不等于细粒度描述正确，当前Qwen摘要尚未可靠抓住区分同类实例的关键特征。

当前仍不能从这份报告得出完整语义验收结论：没有配套图片、聚类接触表、SQLite与指纹资产，无法核实分割区域、对象是否漏检、同一实例是否被误合并或描述是否准确。白色鼠标三视角聚类的CLS两两相似度最低为0.675、平均0.763、最高0.846，也说明单链式聚类可能通过中间视角连接低于边阈值的成员，后续需要结合图像审计。

## 4. 最近实验之后的代码变化

本次真实运行证明v0.22自动点网格、DINOv3聚类、Qwen聚类级审查和SQLite写入能够端到端结束，且模型调用顺序与报告结构符合设计。v0.22.2不改算法，只修正Web的通用实验判定和结果解释：自由上传不再套用固定Demo内容门槛，自动分割按图片折叠展示，聚类术语集中说明。原始报告不回写；历史运行仍保留当时的 `failed` 记录，页面会解释其实际 `pipeline_status`。

## 5. 里程碑

| 里程碑 | 状态 | 结论 |
|---|---|---|
| M0 环境与模型 | 完成 | SAM3、DINOv3和Qwen已在服务器真实运行 |
| M1 数据库与文件骨架 | 完成 | 对象、候选、观测和文件可以对应保存 |
| M2 SAM3 候选接口 | 实际运行完成 | 5图产生1280个原始候选并保留22个；真实召回仍缺图片真值审计 |
| M3 Qwen 对象判断 | 实际运行完成 | 18个聚类分3批审查；13个零件/背景被忽略，语义正确性仍需资产审计 |
| M4 记忆写入闭环 | 完成 | SQLite、图片和报告可以完整对应 |
| M5-A 候选到记忆 | 完成但有限 | 新流程真实运行形成3个对象和8条观测，无流程错误 |
| M5-B 完整实验效果 | 未通过 | 最新运行缺少配套输入、资产与人工真值，不能确认召回、聚类和身份效果 |

## 6. 当前关键判断

1. 最新运行的模型、结构检查和记忆写入实际通过；页面标红是旧固定Demo覆盖门槛误用，不是SAM3、DINOv3或Qwen失败。
2. 自动点网格绕开了“Qwen先命名、SAM才看”的类别词限制，但会产生大量零件和背景；本次1280个原始候选经脚本保留22个，说明候选规模可控，不代表真实物体已经找全。
3. DINOv3把22个候选组织为18个聚类，Qwen只调用3次而不是逐图或逐候选调用；Qwen仍是本次主要耗时，约181.36秒。
4. Qwen负责完整对象/零件/背景与文字摘要，DINOv3负责视觉聚类和历史匹配；白鼠标冲突聚类最终为 `uncertain`，说明双证据保守决策实际生效。
5. 聚类相似度不是身份正确概率。单成员聚类显示1.000只是没有组内比较对象；多成员组最低值低于边阈值可能来自单链式传递，需要结合接触表审计。
6. 当前继续保存所有有效的 `new/existing` 观测；“只保留有新信息的视角”仍是后续优化，不是本轮验收项。
7. 白色鼠标的“对称设计”误述直接证明当前类内文字特征仍未通过；加上缺少输入、完整记忆根和同类不同实例真值，不能宣称召回率、聚类阈值或类内实例辨识已经通过。

## 7. 实验历史

| 版本与日期 | 实验 | 结果 |
|---|---|---|
| v0.15.2，2026-07-31 | SAM3 点网格基线 | 14张图产生3584个原始候选，脚本保留72个，Qwen接纳59条观测并形成8个档案；人工确认实际只有3个对象，且有大量背景和固定件污染。历史恢复点为 `3151334` 或更早提交 |
| v0.16.1，2026-07-31 | 第一次两阶段实验 | 14张唯一图中只有3张走完流程，产生6个候选和2个对象，饮料杯缺失 |
| v0.17.0，2026-08-02 | 第二次两阶段实验 | 第2批因旧6词限制停止；10张图完成，产生12个候选和3个对象ID，覆盖12/24 |
| v0.17.4，2026-08-02 | 第三次两阶段实验 | 第1批因旧 `and` 规则停止；10张图完成，产生11个候选和3个对象ID，覆盖11/24，并出现杯盖粒度和鼠标颜色错误 |
| v0.18.0，2026-08-02 | 清理与源头优化 | 删除三次旧记忆和报告，保留固定输入；完成首轮v4、SAM3阈值、候选上下文和对象卡语义修改 |
| v0.18.0配置，2026-08-03 | 第四次两阶段实验 | 14张唯一图全部完成，23条提示产生15个候选，形成3个正确对象档案和15条观测；结构通过但只覆盖15/24，运行编号 `run_20260803T093048019080Z` |
| v0.22.0流程，2026-08-07 | 第一次自动候选视觉聚类实验 | 5张唯一图产生1280个原始候选，脚本保留22个，DINOv3形成18个聚类，Qwen调用3次，写入3个对象和8条观测；流程通过，旧固定Demo重复图门槛造成顶层误报失败，运行编号 `run_20260807T122430273165Z` |

v0.16.1、v0.17.0 和 v0.17.4 的完整删除前快照可从 Git 提交 `f413654` 恢复。
