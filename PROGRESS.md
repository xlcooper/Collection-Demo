# 第一阶段进度看板

> **维护规则（强制）**：本文件是项目当前执行状态的唯一事实源。每个新对话在读取 README 后必须读取本文；每次收到服务器结果、推进里程碑或改变实现决策后，本地 AI 必须更新状态、证据、风险、计划和下一动作。用户明确确认可以判定功能完成；报告用于固化指标和技术事实，二者冲突时以最新报告为准。过时状态必须替换，不以追加说明掩盖。

**最后更新**：2026-07-31 12:44:25 +08:00

## 管理方式

- 初始里程碑来自本地 `docs/00_项目纲要.md`、`docs/01_第一阶段项目设计规划书.md`，并结合最新服务器环境形成。
- 纲要负责目标和边界，规划书负责初始架构参考；它们不是日常状态源，也不要求每轮重复读取。
- 本文件沿初始主线推进，并依据真实开发结果调整顺序、门槛和风险；调整只修改本文件和受影响的职责文件，不把过程信息回填堆积到规划书。
- CHANGELOG 记录已经提交的历史版本；环境重建与模型冒烟复现命令由 `docs/03_服务器环境与冒烟测试指南.md` 维护。

## 当前快照

- **版本**：v0.13.0
- **当前关卡**：M5-B——Qwen 接管与第一阶段端到端验收
- **端到端总入口**：`python scripts/run_object_memory.py`；`scripts/verify_m*.py` 仅用于里程碑验证和问题定位
- **第一阶段目标**：给系统一批不断增加的场景图片，它能自动发现物体、生成标签、建立对象档案，并把同一具体物体的不同观测持续归并到同一档案中；暂不负责主动选择拍摄视角、控制机器人或判断物体信息是否已经完整
- **最近完成**：本地主流程已重构为两阶段 Qwen：先独立完成候选有效性和临时标注，再用文字相关性从全部对象卡片中选出最多8张，结合参考图确认身份
- **当前问题**：新接口、文字 shortlist 和调用计数已补充确定性测试，但尚未由服务器执行；真实 Qwen 的输出 schema、三个物体建档及四个无效候选提前停止也仍待验证
- **下一动作**：服务器运行单元测试，并用全新 `data/m5_two_stage_gate_memory_01` 对同一场景执行两阶段 gate，回传 `environment/m5_two_stage_gate_report.json`
- **M5-B 当前门槛**：`000131`、`000067`、`000155` 三个真实候选均为合理 `new`；`000208`、`000006`、`000100`、`000046` 均为 `ignored`；无 pending/failed

状态说明：✅ 已完成；🟡 正在进行；⬜ 尚未开始；⛔ 被外部条件阻塞。

## 里程碑

| 里程碑 | 状态 | 最小交付物 / 完成条件 | 当前证据 | 下一动作 |
|---|---|---|---|---|
| P0 项目基线 | ✅ | 第一阶段范围、开发准则、Git 与环境流程固化 | `README.md`、环境声明和执行指南 | 持续维护，不扩展阶段范围 |
| M0-A 服务器环境 | ✅ | Conda、CUDA、SAM3 及 Qwen 依赖可用，0 个阻塞项 | 最新报告：0 blocker、2 warnings；数据盘剩余 23.45 GiB | M1 控制数据规模，不下载备用模型 |
| M0-B SAM3 单模型冒烟 | ✅ | 真实加载 checkpoint，并输出至少一个 proposal 和 mask | `cup` 得到 1 个 proposal，score 0.8867；加载 10.196 秒、推理 0.462 秒、峰值显存 5087.16 MiB | 单提示链路完成；全量策略留到 M2 |
| M0-C Qwen 单模型冒烟 | ✅ | 8B FP8 加载成功，输出可解析的约定 JSON，并记录耗时/显存 | 98 token，13.32 token/s；加载 3.518 秒、推理 7.356 秒、模型全部在 GPU 0、峰值显存 10439.92 MiB | 结构链路完成；语义质量留到 M3 验证 |
| M0-D 双模型顺序运行 | ✅ | 同一环境中先后完成 SAM3、Qwen，且无显存或磁盘阻塞 | 统一入口生成三份通过/无阻塞报告，回传提交 `59bd3a7` | M0 关闭，进入 M1 |
| M1 数据骨架 | ✅ | 配置、统一数据类型、SQLite schema、文件路径和最小 CLI | M1 报告 `passed`：6 项检查全真，六张表计数均为 0 | M1 关闭，进入 M2 |
| M2 SAM3 流水线 | ✅ | 候选策略、mask 去重、crop/mask/overlay 和源图关联 | M2 报告 `passed`：2 个原始候选跨提示去重为 1 个保留候选，10 项检查全真 | M2 关闭，进入 M3 |
| M3 MLLM 流水线 | ✅ | 对象卡片、身份判断、自动标注和 JSON 校验 | v2 服务器报告 `passed`：空记忆为 `new`；同一视图为 `existing` 且 ID 正确；全部检查通过 | M3 关闭，进入 M4 |
| M4 记忆闭环 | ✅ | 新建/归并、幂等、pending/failed、事务写入和运行摘要 | 服务器报告 `passed`，10 项检查全真；4 runs、3 sources、6 proposals、1 object、3 observations、5 decisions | M4 关闭，进入 M5 |
| M5-A 无提示候选校准 | ✅ | 不提供物体类别，由 SAM3 自动提出足够可用的场景候选 | v2：256 个原始候选保留 7 个，3/3 真实物体均保留；桌面和局部明显减少，全部检查通过 | 阶段关闭；阴影优化转为非阻塞后续项 |
| M5-B 第一阶段验收 | 🟡 | 单命令、无人确认地完成发现—判断—标注—存储—归并，并可追踪失败 | 两阶段分析、文字 shortlist 和参考图确认已实现，真实 Qwen 尚待复验 | 先完成同图两阶段 gate，再扩展多视角批量 |

## 当前关键判断

1. M0 已完成：统一入口顺序运行两个模型并完成环境自检，三份报告均已进入 Git。
2. SAM3 的 BF16 链路已跑通：`cup` 得到 1 个 proposal 和 mask，score 0.8867；推理约 0.46 秒、峰值显存约 5.0 GiB。
3. Qwen3-VL-8B-Instruct-FP8 全部位于 GPU 0；98 个输出 token 用时 7.356 秒，即 13.32 token/s。此前约 10 秒属于正常波动，不是 CPU offload。
4. Qwen 整图曾输出 `coffee/latte`，没有严格聚焦物理容器；M3 已改为输入 SAM crop 与 overlay 并明确只标注被分割的物理对象。M5 自动候选不再附带类别提示，类别由 Qwen 仅依据 crop 像素生成。
5. 通用文本提示 `object` 返回 0 proposals，不能代表“全量发现物体”；此前 M2 用显式类别列表完成了接口验证，但该策略不满足当前“无需外界类别提示”的产品要求，不能继续作为主流程候选策略。
6. 数据盘剩余 23.45 GiB，足够进行小规模 M1，但不再下载备用模型；每个里程碑继续监控空间。
7. nvcc 11.8 与 generation flags 均为已接受的非阻塞警告。
8. M1 已通过服务器验证：默认配置、六类数据模型、重复初始化、六张核心表、空库查询和 CLI 状态均通过；报告生成于 2026-07-30 16:03:59（UTC+08:00）。
9. M1 只建立持久化地基；M2 已完成候选与图像资产，M3 只做 Qwen 判断而不写数据库，实际新建/归并和事务写入仍属于 M4。
10. M2 报告已通过：`cup`、`mug` 各得到 1 个候选，IoU 去重后保留 `cup` 候选；模型加载 9.243 秒、全部提示推理 0.515 秒、峰值显存 5098.53 MiB。
11. 历史 M3 验收曾按固定批次遍历全部对象卡片；v0.13.0 起主流程已停止全库分批输入，改为先用临时标注对全部卡片文字排序，再只比较固定大小的 shortlist。
12. M3 提示 v2 最终报告生成于 2026-07-30 17:52:37（UTC+08:00）：全部检查通过，模型仍完全位于 GPU 0；加载 3.562 秒、两次推理共 20.09 秒、峰值显存 10667.93 MiB。
13. M3 空记忆输出 `new` 和完整的白色陶瓷咖啡杯标注；同一 crop 对象卡片输出 `existing`、置信度 1.0，并正确返回 `obj_m3_same_view_reference`。旧提示 v1 的同视图误判问题已由 v2 修复。
14. 历史 M3 提示 v2 曾在同一调用中混合有效性、身份和标注；v0.13.0 起当前实现明确拆成候选分析和身份确认两个 schema，避免对象卡片影响候选是否有效。
15. M4 直接复用 M1 的六张表和 schema v2：SQL 只在 `MemoryStore` 内执行，`MemoryLoop` 负责把已校验的 M3 响应转换为对象、观测和决策记录。
16. 图片 SHA-256 幂等门会跳过已经 `completed` 的源图和本批次已登记的相同文件；filtered 不调用 Qwen；ignored 不污染对象库；uncertain 记录 attempt 并保持 pending；上层耗尽异常重试后转 failed。
17. new/existing 的 crop、mask、overlay 会复制到对象观测目录；关联 SQL 在单个事务中完成，事务失败时清理本次资产，避免非法对象 ID 造成部分写入。
18. M4 验证只使用临时 SQLite 和确定性假数据，不加载模型、不修改正式记忆库；真实批量模型编排仍留给 M5。
19. M4 最终服务器报告生成于 2026-07-30 18:23:30（UTC+08:00），10 项检查全部通过；非法 existing 正确回滚，uncertain 可重试，对象卡片包含两张存在的代表视图，重复图计数为 1。
20. M5 对输入目录按路径排序并递归读取受支持图片；先登记哈希，再一次加载 SAM3 处理全部新图，释放后才加载 Qwen，保持已验证的单卡顺序驻留。
21. M5 对每个候选先运行不含记忆卡片的有效性与临时标注调用；只有有效候选才重新查询 SQLite，因此同一批次较早创建的对象仍能参与后续归并，无效候选不会读取或输入记忆。
22. M5 只对模型异常或 JSON/schema 解析失败进行有限重试，默认最多 2 次；每次原始输出、token 和耗时进入对象记忆目录，成功决策仍只写一条有效 attempt。
23. `--validate-phase-one` 要求至少 2 个 new、1 个 existing、1 个哈希跳过、1 个 filtered/ignored 且无 pending/failed；它提供路径覆盖证据，但具体对象身份仍需结合验收图片分组复核。
24. 当前主流程采用 `automatic_point_grid`：脚本在图像上生成 `16×16` 个均匀采样点，SAM3 对点批量推理并为每点保留质量分最高的 mask；这属于内部自动几何提示，不需要用户输入类别词。
25. M5-A v1 报告生成于 2026-07-30 19:17:57（UTC+08:00）：16×16 点网格产生 256 个原始候选，过滤 244 个、保留 12 个；推理 9.23 秒、峰值显存 5390.59 MiB，全部自动化检查通过。
26. Qwen 输入不再把 SAM 来源标记当类别标签；它被明确告知当前候选没有类别提示，应只根据像素判断类别，并把背景、物体局部、粘连区域或无意义碎片判为 `ignored`。
27. 人工检查确认 3 个真实物体均有完整候选；12 个保留候选中另有 3 个阴影、2 个物体局部和 4 个近似桌面区域，因此 v1 只证明覆盖率，不满足候选纯度门槛。
28. v2 将最大 mask 面积比例从 0.8 收紧至 0.5，并过滤被更高分完整候选覆盖至少 90% 的较小 mask；不把置信度硬卡在本图真实物体与噪声之间，也不增加第二次 Qwen 调用，无法安全用几何规则区分的阴影仍由 Qwen 判为 `ignored`。
29. M5-A v2 报告生成于 2026-07-30 19:47:29（UTC+08:00）：保留候选从 12 降至 7，`mask_too_large=15`、`contained_mask=2`、`duplicate_mask=49`、`low_confidence=183`；候选推理 7.183 秒、峰值显存 5390.59 MiB，全部检查通过。
30. 用户人工确认 v2 中3个真实物体仍全部存在、桌面和局部明显减少，剩余主要为2～3个阴影，因此 M5-A 已完成；当前允许这些少量候选进入 Qwen 的原有有效性判断。
31. 阴影前置过滤保留为非阻塞优化方向：M5-B 先记录真实 `ignored` 比例和 Qwen 耗时；只有多场景数据证明成本仍高时，才基于多图标注评估阴影/反射规则或批量有效性过滤，不用单图阈值过拟合。
32. Qwen gate v1 报告生成于 2026-07-31 10:43:22（UTC+08:00）：结构状态 `passed`，7个候选各调用一次 Qwen，得到3个 new、4个 ignored、0个 pending/failed；Qwen 推理共 43.427 秒、峰值显存 11591.37 MiB。
33. 逐候选审查发现计数掩盖了语义交换错误：真实候选 `grid_point_000067` 被 ignored，而阴影候选 `grid_point_000100` 因矩形 crop 中出现邻近杯子，被标为白色红盖饮料容器并创建对象；因此 M5-B gate v1 未通过。
34. 当前修复不增加图片或模型调用：`crop.png` 改为只保留 mask 内原始像素、外部填充统一中性灰；overlay 继续提供场景位置，Qwen 提示升级至 `m3-object-identity-v3` 并明确只判断保留像素，阴影、反射和纹理应 ignored。
35. Qwen gate v2 报告生成于 2026-07-31 10:59:56（UTC+08:00）：`memory_02` 正常登记并处理1张图片，重复跳过为0；256个原始候选保留7个，得到2个 new、5个 ignored、0个 pending/failed；Qwen 推理共41.389秒、峰值显存11536.39 MiB。
36. mask 隔离修复已得到针对性证据：v1 中因矩形 crop 包含邻近杯子而误建档的阴影 `000100`，在 v2 中正确变为 `ignored`；`000208`、`000006`、`000046` 也继续保持 ignored。
37. gate v2 的剩余失败是接口边界：Qwen 明确认出 `000067` 是饮料杯，却以“不是已有鼠标”为由判 ignored；仅继续堆叠单次提示约束不再作为当前方案，v0.13.0 从输入接口上隔离有效性与身份判断。
38. 当前第一阶段 Qwen 调用策略：每个脚本保留候选先调用一次候选分析；ignored 到此结束；有效候选在记忆为空时直接 new，有对象卡片时才增加一次身份确认调用。
39. 当前文字检索不引入新模型或依赖：脚本按粗/细类别、材质、颜色、形状和描述计算确定性相关分数，默认取前8张对象卡片；Qwen 在第二阶段同时获得临时标注、这些卡片文字、候选 crop 和参考图。
40. 文字 shortlist 只负责缩小范围，不能单独证明同一具体实例；参考图仍是最终身份确认的重要证据。当前词法排序适合小规模第一阶段，未来若卡片量和同类实例显著增加，再用多场景数据评估文本/视觉 embedding 索引。

## 现在执行

M5-B 先验证 Qwen 接管，再进入完整批量验收：

1. 服务器拉取代码并运行单元测试；本轮依赖未变化，不安装项目或新库。
2. 按 `docs/04_业务代码运行与验证指南.md` 使用现有隔离输入目录和全新 `data/m5_two_stage_gate_memory_01` 运行总入口；本轮不使用 `--validate-phase-one`。
3. 核对三个真实物体在 candidate analysis 中为 valid 并最终 new；四个阴影/局部在第一阶段直接 ignored、不查询对象卡片；确认无 pending/failed，回传 `environment/m5_two_stage_gate_report.json`。
4. 小规模接管通过后，再准备 10～20 张多视角验收图，包含同一物体不同角度、同类不同实例、完全相同副本和无效场景。
5. 使用全新编号的验收记忆库运行总入口并增加 `--validate-phase-one`，复核同物体是否归并、不同实例是否分开。

## 第一阶段边界

本看板不加入认知完整度、缺失视角分析、主动视角规划、机械臂控制、空间地图、因果推理或完整世界模型；这些均不属于第一阶段验收。
