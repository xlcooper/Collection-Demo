# 实验看板与审计分析

> **维护规则（强制）**：本文件记录已经真实发生的正式实验、审计结论、里程碑、仍生效的关键判断和压缩后的实验历史，不维护未来计划或下一动作。机器事实以运行报告、SQLite 和资产为准；产品验收以未提供给模型的真值和逐图人工核对为准。原始产物清理后必须明确标注，不得把历史摘要伪装成仍可直接核验的当前证据。当前协作方向由 `AGENTS.md` 维护。

- **当前版本**：v0.17.0
- **最后更新**：2026-07-31
- **当前关卡**：M5-B——Qwen 接管与第一阶段端到端验收

## 当前实验看板

| 项目 | 当前事实 |
|---|---|
| 正式实验 | v0.16.1 两阶段定向流程首次真实服务器运行 |
| 运行 ID | `run_20260731T110624049759Z` |
| 证据 | `environment/demo_run_report.json`、`data/memory/memory.sqlite`、内部运行报告、36份 Qwen 原始响应及全部图像资产 |
| 输入 | 28个文件；14张内容唯一，14张逐字节副本在模型调用前跳过 |
| 源图结果 | 3张完成，11张在首轮 Qwen→SAM3 文本协议校验处失败 |
| 候选与记忆 | 6个 SAM3 候选全部决定并接纳；2个 `new`、4个 `existing`、6条 observation、2个 active object |
| 运行状态 | `completed_with_errors`；外部 Demo 报告 `failed` |
| 产品验收 | ❌ 未通过：真值3个对象只建档水瓶和鼠标，饮料杯完全缺失；仅3/14张唯一图进入 SAM3 |

本轮不是环境或模型加载失败。两种模型均完成真实推理，结构化记账与资产闭环正常；主故障发生在首轮 Qwen 输出与本地 schema 之间的文本契约。

## 本次实验真实流程

### 1. 哈希幂等门

- 扫描 `data/input/` 的28个文件。
- 14张唯一图片注册为 source；14张逐字节副本在任何 Qwen/SAM3 调用前跳过。
- 数据库最终记录14个 source，与报告一致。

### 2. 第一轮 Qwen 场景观察

- 14张新图按 `4 + 4 + 4 + 2` 组成4个主批次。
- 4个主批次均调用2次，8次全部 schema 失败；其中既有带 `with` 的自然英文短语，也有一次中文 `sam_text_prompt`。
- 主批次失败后，系统把14张图全部拆成单图救援：3张首试成功，11张各重试2次仍失败。
- 首轮因此实际调用33次，而不是理想的4次；只有 `img_0001.jpg`、`img_0005.jpg`、`img_0015.jpg` 的6个目标提示被接受。

11张最终失败图并不是 Qwen 判定“没有目标”。Qwen 已识别水瓶、鼠标或饮料杯，但自然短语如 `plastic cup with red straw`、`transparent water bottle with handle` 被 `SceneTarget` 校验器拒绝。校验器把 `with` 列为禁词，却统一报告“too generic”，导致具体、可用的概念也无法进入 SAM3。

按隐藏真值，14张唯一图共有24个“图像×目标对象”可见视图。人工核对首次主批原始响应，Qwen 对其中22个给出了正确的完整对象概念；主要偏差是一个遮挡目标遗漏和把整杯收窄为杯盖。接口最终只接受6/24，因此本轮主要召回损失发生在 schema 与整批失败策略，而不是首轮视觉理解本身。

### 3. SAM3 定向分割与脚本后处理

- 只有3张成功源图进入 SAM3，共6个文本提示。
- SAM3 每个提示返回1个过模型阈值实例，共6个原始候选。
- 面积、IoU、同概念包含关系和候选上限过滤均未淘汰候选，最终仍为6个。
- 人工检查6组 crop/mask/overlay：3个水瓶视角、3个鼠标视角，均为真实目标，背景或固定装置污染为0/6。

这只能证明已进入 SAM3 的小样本候选很干净，不能证明两阶段方案已达到完整召回或广泛场景精度；11张图及全部饮料杯视图没有进入该环节，过滤分支也未被覆盖。

### 4. 第二轮 Qwen 候选与记忆判断

- 按3张成功源图各调用1次，共3次，均首试成功。
- 6个候选全部先判为有效，再得到唯一终态：首图的鼠标、水瓶为2个 `new`；后两图为4个 `existing`。
- 水瓶的3个视角正确聚合为1个对象，鼠标的3个视角正确聚合为1个对象；在这一受限子集内没有错并、拆档或背景接纳。
- 身份理由并不都可靠：部分 `existing` 只依据同类、形状和材质相似，尚不足以一般化证明同一具体实例。
- 第三张成功图中的白/灰鼠标被 Qwen 写成“粉色”，并覆盖了对象卡片的颜色与描述。结果 ID 恰好正确，但稳定属性累计发生污染；红色 overlay 干扰是合理怀疑，单次证据不足以单独证明因果。
- 一个被水瓶大幅遮挡、信息量很低的鼠标局部仍被保存为长期 observation，说明“有效且属于已有对象”与“值得新增长期记忆”尚未拆开。

### 5. 持久化输出

- 六张核心表计数为 `1 run / 14 source / 6 proposal / 6 decision / 2 object / 6 observation`。
- 每个候选恰有1个 decision；每个 `new/existing` 恰有1条 observation；没有 pending、failed proposal 或悬空对象。
- 两个对象分别为鼠标和水瓶，各有3条 observation；饮料杯没有对象档案。
- 当前实现把每条 observation 的 crop、mask、overlay 从 proposal 再复制一份。18份文件逐字节相同，重复占用1,239,065字节，占该记忆根31.33%；其中 overlay 占838,691字节。

## 审计分析

### 机器一致性

- SQLite `integrity_check=ok`，外键异常0；状态、计数和关系闭环与报告一致。
- 14份规范 source 均存在，SHA-256、尺寸和输入原字节一致；28个输入恰为14对重复。
- 数据库引用的53个唯一资产路径、报告引用的36份原始响应及全部 source/proposal 资产均存在，无越界或错链。
- 外部 `environment/demo_run_report.json` 与内部运行报告逐字节一致。
- 报告7项结构检查均为真，但这里只表示输入被记为“完成/失败/重复”且已产生候选闭环；不能把 `every_processed_source_has_scene_guidance=true` 误读为14张图均分析成功，真实成功数是3。

### 根因与批次 trade-off

1. **词法契约冲突**：提示要求简短具体英文名词短语，却没有明确说明禁用 `with`；本地校验又把 `with` 错误归为“过于泛化”。11个最终失败 source 全部因此被挡在 SAM3 之前。
2. **整批原子失败**：一张图中的一个非法提示会让同批其余合法图片一起作废。4图批次有吞吐收益，但当前错误隔离方式把局部格式问题放大成批次召回损失。
3. **无反馈确定性重试**：4个主批次与11个失败救援的第二次响应均与第一次逐字相同。15次盲重试额外消耗38,810 input token、5,420 output token和352.486秒，没有修复任何结果。
4. **失败根不可直接开启新 run**：当前记忆库有11个属于本 run 的 failed hash。存储规则只允许它们在原 run 内恢复；新 run 不能直接接管，因此该根应作为失败证据整体保留，不能当作干净增量库继续实验。

### 性能与资源

| 阶段 | 调用 | 推理耗时 | Input / Output token | 峰值显存 |
|---|---:|---:|---:|---:|
| Qwen 首轮场景观察 | 33 | 755.628 s | 83,342 / 11,609 | 11,540.56 MiB |
| SAM3 分割 | 6个文本提示 | 0.521 s | — | 5,100.71 MiB |
| Qwen 第二轮判断 | 3 | 140.139 s | 12,458 / 2,170 | 11,440.24 MiB |
| Qwen 合计 | 36 | 895.767 s | 95,800 / 13,779 | 11,540.56 MiB |

运行墙钟为918.172秒（15分18.17秒），Qwen 推理占97.56%。第二轮随记忆上下文从0张卡片/0张参考图增长到2张卡片/4张参考图，单次耗时由42.270秒增至51.356秒，说明对象记忆增长会继续推高视觉上下文成本。

本次服务器提交没有单元测试日志或测试摘要，因此只能确认真实 Demo 与产物审计，不能据此宣称现行回归测试已通过。

## 里程碑

| 里程碑 | 状态 | 已证实边界 |
|---|---|---|
| M0 环境与双模型冒烟 | ✅ | RTX 4090 上 SAM3、Qwen 可顺序真实推理 |
| M1 数据骨架 | ✅ | SQLite、配置、CLI 和空库查询链成立 |
| M2 SAM3 接口 | ✅ | 显式文本提示接口可用；通用 `object` 不保证召回 |
| M3 Qwen 身份接口 | ✅ | 空记忆 `new`、同 crop 对卡片 `existing` 链成立 |
| M4 记忆闭环 | ✅ | 事务、回滚、幂等和失败状态经确定性测试验证 |
| M5-A 候选与单图 gate | ✅（有限） | 覆盖和单图批量判断成立；仍有一个已接受的配件误建档限制 |
| M5-B 端到端产品验收 | ❌ | v0.15.2 点网格基线失败；v0.16.1 两阶段首次运行又因接口契约只完成3/14张唯一图 |

## 当前关键判断

1. **M5-B 仍未通过**：结构闭环、局部候选纯度或正确聚合2个对象，均不能替代3个真值对象的完整端到端验收。
2. **首要问题是 Qwen→SAM3 协议，不是 SAM3 分割质量**：应区分“模型看到了目标”“输出可解析”和“提示被接口接受”三层召回。
3. **4图批次继续作为一次实验调用单位**：批处理确有吞吐收益；批次响应不合规时直接让该批失败并保留证据，不再自动拆成单图改变实验条件。批次大小是否调整只能依据后续真实实验。
4. **实验阶段不设置自动恢复路径**：本轮15次重试没有修复任何结果，反而掩盖调用成本。两轮 Qwen 均只调用一次；失败不自动重试、救援、规范化、降级或回退，优先修复已确认根因。
5. **`with` 应在源头成为合法契约**：允许它描述同一完整物体的可见特征，不做删除或改写；提示同时禁止用它拼接不同对象，并继续要求附属部件优先归到完整对象概念。
6. **第二轮身份判断仍需保守**：同类、颜色、材质或外形相似不能单独证明同一实例；本轮 ID 正确不代表理由已满足长期准则。
7. **对象卡片不能由最后一次可疑属性直接覆盖**：白色鼠标被改成粉色说明稳定属性需要累计、冲突或置信管理；overlay 应主要承担位置审计，不能无防护地影响颜色判断。
8. **有效视角不等于值得长期保存**：低信息、重遮挡或与已有视角近似的 `existing` 应经过信息增量门；该能力仍未实现。
9. **资产去重已有量化证据**：对象 observation 宜引用 proposal 资产，至少不再重复复制 overlay；本轮可减少31.33%的记忆根占用。
10. **当前失败记忆库是证据，不是下一轮起点**：新的效果比较必须使用独立空记忆根，避免失败 source 所属 run、已有对象卡片和已完成哈希影响结论。

## 历史实验摘要

### v0.15.2 点网格基线

- 28个输入经哈希门处理14张唯一图；SAM3 以16×16点网格生成3584个原始候选。
- 脚本过滤3512个，留下72组候选；Qwen 按源图调用14次，为72个候选各给1个终态。
- 59个候选被接纳为 observation，聚合成8个对象；真值只有3个对象。
- 审计发现30条背景/固定装置观测、5个纯背景/固定物对象，以及水瓶/饮料杯错并、水瓶拆档和鼠标档案污染，产品验收失败。
- 原始产物已在 v0.16.1 清理；逐文件证据需从 Git 提交 `3151334` 或更早历史恢复。

### 实验与决策时间线

| 日期 | 事件 | 结论 |
|---|---|---|
| 2026-07-30 | M0–M4 分阶段验证 | 双模型、持久化骨架和确定性记忆闭环成立 |
| 2026-07-30 至 2026-07-31 | M5-A 自动候选与 Qwen gate | 几何筛选和源图级批量判断可用，但不能保证候选语义纯度 |
| 2026-07-31 | v0.15.2 多视角点网格基线 | 结构检查通过，产品语义验收失败 |
| 2026-07-31 | v0.16.0 两阶段定向实现 | 完成“Qwen观察→SAM3定向分割→Qwen细判”代码 |
| 2026-07-31 | v0.16.1 旧记录清理 | 清空默认记忆根并保留固定输入、历史摘要和 Git 恢复点 |
| 2026-07-31 | v0.16.1 首次真实服务器实验 | 3/14张唯一图完成；6个候选纯净，但协议故障使饮料杯缺失，M5-B 未通过 |
| 2026-07-31 | v0.16.2 本地审计 | 确认词法契约、整批失败、无反馈重试、属性覆盖和资产重复问题 |
| 2026-07-31 | v0.17.0 根因修复 | 允许同一物体的 `with` 描述，移除两轮自动重试与首轮单图救援；代码等待服务器验证，未改写当前实验结论 |
