# 实验看板与审计分析

> 本文档只记录真实完成的实验：每一步收到什么、实际做了什么、输出了什么，以及最后如何评价。当前开发方向见 `AGENTS.md`，暂不处理的优化见 `docs/05_后续优化清单.md`。

- **当前版本**：v0.17.4
- **最后更新**：2026-08-02
- **当前关卡**：M5-B——端到端对象记忆验收

## 1. 本次结论与数量变化

本次实验未通过最终验收。系统正确建立了水瓶、鼠标和饮料杯3个对象档案，但人工逐图核对后应该保存24次“某张图中出现的某个物体”，最终只保存12次。

运行编号：`run_20260801T161221190913Z`

当前代码说明：v0.17.4 已删除6词上限并升级首轮提示版本，但还没有新的服务器实验。下面仍按 v0.17.0 本次运行真实发生的结果记录，不改写历史数据。

```text
28个输入文件
→ 跳过14个内容完全相同的副本，剩14张唯一图片
→ Qwen第一次分4批看图，共写出23条找物体建议
→ 第2批的5条建议全部没有进入SAM3，剩18条
→ SAM3在10张图中按18条文字指令找物体，找到12个区域
→ 脚本检查后仍保留12个候选
→ Qwen第二次给12个候选分别作出处理决定
→ 最终形成3个对象档案和12条观测记录
```

人工逐图核对的结果是：水瓶出现10次、鼠标出现10次、饮料杯出现4次，共24次。这里的24不是24个不同物体，而是3个物体在多张图片中合计出现24次。

## 2. 名词和编号说明

| 名称 | 大白话解释 |
|---|---|
| `run_id` | 一次完整实验的编号 |
| `source_id` | 一张去重后原图的编号 |
| `target_id` | 第一次Qwen在单张图中临时列出的“第几个物体”；每张图都从 `target_001` 重新开始 |
| `sam_text_prompt` | 真正交给SAM3的英文找物体指令，例如 `wireless mouse` |
| `raw_candidate_id` | SAM3找到的临时区域编号 |
| `proposal_id` | 脚本检查并正式保存后的候选编号 |
| `object_id` | 现实中某个具体物体的长期档案编号 |
| `observation_id` | 某个对象在一张图片中的一次观测记录编号 |

本次运行产物中没有直接保存 `target_id → proposal_id` 的对应字段。SAM3实际收到的是英文提示词，不是 `target_id`；需要通过相同的 `source_id`、提示词和顺序还原关系。`raw_candidate_id` 中的 `text_000` 表示第一条提示，`text_001` 表示第二条提示。

## 3. 本次实验逐步实录

本次记忆库根目录是 `data/memory_v017_01/`。下表中的文件位置都从这个目录开始计算，只有最后一份外部报告位于仓库的 `environment/`。

| 环节 | 输入 | 本次实际发生的事 | 输出和文件位置 |
|---|---|---|---|
| 1. 读取与去重 | `data/input/` 中28个文件 | 按图片内容比较，确认其中14个是另外14张图的完全相同副本。副本到此停止，14张唯一图继续处理 | 14个 `source_id`；处理用原图保存在 `sources/` |
| 2. Qwen第一次看图 | 14张唯一图 | 按 `4+4+4+2` 分成4批，共调用Qwen 4次。Qwen列出23个值得继续找的物体。程序按整批检查回答；第2批5条建议中有2条英文超过6个单词，所以这一批5条都没有交给SAM3 | 4份原始回答：`raw_responses/<run_id>/scene_batch_0001/response.json` 至 `raw_responses/<run_id>/scene_batch_0004/response.json`；其余3批留下18条可用指令 |
| 3. SAM3按文字找物体 | 10张图、18条英文指令 | SAM3模型只加载1次。每张图先转换成供SAM3搜索的内部特征，这一步叫“编码”，10张图各做1次；然后执行18次文字查找。12次找到了分数达到要求的区域，6次没找到：5次是鼠标，1次是水瓶。原始区域、方框和分数此时先在内存中 | 12个临时候选；“没找到”表示SAM3没有产生候选，不是后来被脚本删除 |
| 4. 脚本检查候选 | 12个SAM3临时候选 | 检查区域大小、两个区域是否几乎重复、一个区域是否大幅包含另一个，以及每张图最多保留多少个。本次12个全部通过，没有删除任何候选 | 12个 `proposal_id`，保存在 `proposals/<run_id>/<proposal_id>/`。`crop.png` 是局部裁图，`mask.png` 是标出物体区域的黑白图，`overlay.jpg` 是在原图上高亮该区域 |
| 5. Qwen第二次看候选 | 12组候选图，以及处理到当时已经建立的对象档案和参考图 | 按原图分10次调用Qwen。它先判断候选是不是有效物体，再决定创建新对象、归入已有对象，或丢弃。处理第一张图时记忆库为空，后面会看到前面已经建立的档案 | 12个最终处理结果：3个 `new`（新建对象）、9个 `existing`（归入已有对象），没有丢弃。回答位于 `raw_responses/<run_id>/<source_id>/response.json` |
| 6. 写入对象记忆 | 12个处理结果 | 创建3个对象档案，并把12个候选分别保存为对应对象的一次观测 | 对应关系写入 `memory.sqlite`；图片写入 `objects/<object_id>/observations/<observation_id>/` |
| 7. 生成实验报告 | 整次运行的计数、错误和文件引用 | 汇总本次实验，记忆库内保存1份，仓库中另存1份供本地审计 | `run_reports/<run_id>.json`；`environment/run_report_v017_01.json` |

第2批的两条超长指令分别是：

- `img_0006.jpg / target_001`：`red plastic drink cup lid with straw`，7个单词。
- `img_0011.jpg / target_001`：`transparent plastic drink cup with red text`，7个单词。

同批另外3条建议本身没有超长，但因为程序检查的是整批回答，所以也没有进入SAM3。

## 4. 14张唯一图逐图结果

| 原图 | `source_id` | 批次 | 人工确认图中物体 | Qwen第一次、SAM3和最终记忆结果 |
|---|---|---:|---|---|
| `img_0001.jpg` | `src_c5a2b20d9299071e3915298423480189` | 1 | 鼠标、水瓶 | `target_001`鼠标、`target_002`水瓶；都找到；新建鼠标和水瓶档案 |
| `img_0003.jpg` | `src_4cf691844b8baeee414cff6a9d6e441e` | 1 | 鼠标、水瓶 | `target_001`水瓶找到并归入已有档案；`target_002`鼠标未找到 |
| `img_0004.jpg` | `src_38f13969e802dc30e46f45461efef2ac` | 1 | 饮料杯 | `target_001`饮料杯找到；新建饮料杯档案 |
| `img_0005.jpg` | `src_76482d84fc76d1c7e2061993bfa67a79` | 1 | 鼠标、水瓶 | `target_001`水瓶找到并归入已有档案；`target_002`鼠标未找到 |
| `img_0006.jpg` | `src_ebd9a7980e4eebecde06d3ab3317d51c` | 2 | 饮料杯 | `target_001`被写成“杯盖和吸管”，且英文有7词；整批停止，未进SAM3，无记忆 |
| `img_0008.jpg` | `src_11b171cb7b25e04e37560c8a76630baf` | 2 | 鼠标、水瓶 | 只列出 `target_001`水瓶，鼠标被漏掉；整批停止，未进SAM3，无记忆 |
| `img_0009.jpg` | `src_cd11b8f53eee5bf00b87e637aa6c481f` | 2 | 鼠标、水瓶 | `target_001`水瓶、`target_002`鼠标；整批停止，未进SAM3，无记忆 |
| `img_0011.jpg` | `src_01577d5285c9315307e807b77e802c81` | 2 | 饮料杯 | `target_001`饮料杯的英文有7词；整批停止，未进SAM3，无记忆 |
| `img_0012.jpg` | `src_3cea6b6ee098e81455d929f6f2ca3aa2` | 3 | 鼠标、水瓶 | `target_001`水瓶未找到；`target_002`鼠标找到并归入已有档案 |
| `img_0013.jpg` | `src_f37cbe405bc095713b4c5096c9b3bafa` | 3 | 饮料杯 | `target_001`饮料杯找到并归入已有档案 |
| `img_0015.jpg` | `src_54ac1b5d01f7f5f4123518653092ee1b` | 3 | 鼠标、水瓶 | `target_001`水瓶、`target_002`鼠标都找到并归入已有档案 |
| `img_0017.jpg` | `src_3c29806ddef097e7d2dff530f49d18e4` | 3 | 鼠标、水瓶 | `target_001`水瓶找到并归入已有档案；`target_002`鼠标未找到 |
| `img_0019.jpg` | `src_5cf926f0d4c1c65b71d162b3e47ae947` | 4 | 鼠标、水瓶 | `target_001`水瓶找到并归入已有档案；`target_002`鼠标未找到 |
| `img_0021.jpg` | `src_46cdc95f1f72f513c8dfe5228aa5e0f6` | 4 | 鼠标、水瓶 | `target_001`水瓶找到并归入已有档案；`target_002`鼠标未找到 |

## 5. 一条真实编号链路

以 `img_0001.jpg` 中的鼠标为例：

```text
原图 img_0001.jpg
→ source_id: src_c5a2b20d9299071e3915298423480189
→ target_id: target_001
→ 交给SAM3的指令: wireless mouse
→ SAM3临时候选: text_000_candidate_000
→ 正式候选: prop_e2ffcb3776fc46b1bd10b685999d55da
→ Qwen第二次结果: new
→ 鼠标档案: obj_677e64f351e445cdb2f6d71b6adb63a9
→ 本次鼠标观测: obs_da182de3dd5a4154b942d9e335faa062
```

对应文件依次位于：

- 第一次Qwen：`raw_responses/<run_id>/scene_batch_0001/response.json`
- 候选图片：`proposals/<run_id>/prop_e2ffcb3776fc46b1bd10b685999d55da/`
- 第二次Qwen：`raw_responses/<run_id>/src_c5a2b20d9299071e3915298423480189/response.json`
- 最终观测：`objects/obj_677e64f351e445cdb2f6d71b6adb63a9/observations/obs_da182de3dd5a4154b942d9e335faa062/`

## 6. 最终记忆与审计结论

| 真实对象 | `object_id` | 最终保存 | 人工确认应有 |
|---|---|---:|---:|
| 水瓶 | `obj_d19c9d7d699243c88ae5f0eebd0660b9` | 7次观测 | 10次出现 |
| 鼠标 | `obj_677e64f351e445cdb2f6d71b6adb63a9` | 3次观测 | 10次出现 |
| 饮料杯 | `obj_d7d79a4e58cf48ee801c7908912b58e9` | 2次观测 | 4次出现 |
| **合计** | **3个对象档案** | **12次观测** | **24次出现** |

- 12个候选都是真实水瓶、鼠标或饮料杯，没有桌面、阴影或固定件。
- 12个候选都归入了正确的具体对象，没有错误合并，也没有把同一对象拆成多个档案。
- 第2批4张图中实际有6次物体出现；Qwen只列出5条建议，随后5条全部没有进入SAM3，因此损失6次。其余10张图中的18次物体出现都被Qwen列出，但SAM3又漏掉6次，最终保存12/24。
- SQLite数据库、图片文件和两份报告可以完整对应，没有缺失文件或断开的引用。
- 当前规则会保存所有有效并被判断为 `new` 或 `existing` 的观测。是否值得保留每个新视角属于已延后的优化，不算本轮问题。

## 7. 运行耗时

| 环节 | 实际处理量 | 模型推理时间 |
|---|---:|---:|
| Qwen第一次看图 | 4次批量调用 | 209.019秒 |
| SAM3 | 模型加载1次、10张图各转换1次内部特征、18次文字查找 | 1.495秒 |
| Qwen第二次判断 | 10次调用、处理12个候选 | 322.773秒 |
| **整次运行** | 14张唯一图 | **554.746秒，约9分15秒** |

两轮Qwen合计调用14次，推理用时531.792秒。Qwen峰值显存11789.87 MiB，SAM3峰值显存5100.71 MiB。

## 8. 里程碑

| 里程碑 | 状态 | 大白话结论 |
|---|---|---|
| M0 环境与模型 | ✅ | SAM3和Qwen都能在服务器真实运行 |
| M1 数据库与文件骨架 | ✅ | 对象、候选、观测和文件可以完整保存 |
| M2 SAM3候选接口 | ✅（有限） | 可以按英文物体名称找区域，但不同视角下仍会漏检 |
| M3 Qwen对象判断 | ✅ | 可以创建新对象，也可以把新视角归入已有对象 |
| M4 记忆写入闭环 | ✅ | 数据库、图片和运行报告可以完整对应 |
| M5-A 候选到记忆 | ✅（有限） | 批量候选可以完成判断和写入 |
| M5-B 完整实验效果 | ❌ | 3个对象身份正确，但只保存12/24次物体出现 |

## 9. 当前关键判断

1. 本次正确识别并归档了3个真实对象，但可见物体只保存50%，所以M5-B仍未通过。
2. 造成第2批停止的6词上限已在 v0.17.4 删除；新的服务器实验尚未验证结果。SAM3对鼠标只找到3/8次的问题仍然存在于当前实验记录中。
3. Qwen第二次的对象身份判断在本次12个候选上全部正确。
4. 当前继续保存所有有效的 `new/existing` 观测；视角信息增量不是当前验收项。
5. 后续报告必须分开统计：Qwen列出多少目标、程序采用多少指令、SAM3找到多少候选、最终保存多少观测。
6. `data/memory/` 和 `data/memory_v017_01/` 都是已结束实验的证据；下一次对比实验使用新的空记忆库。

## 10. 历史记录

| 版本与日期 | 实验或修改 | 结果与证据 |
|---|---|---|
| v0.15.2，2026-07-31 | SAM3点网格基线实验 | 14张图产生3584个原始候选，脚本留下72个，Qwen接纳59个观测并形成8个档案；人工确认实际只有3个对象，且有30条背景或固定件观测。原始文件已清理，需要从Git提交 `3151334` 或更早版本恢复 |
| v0.16.1，2026-07-31 | 第一次两阶段实验 | 14张唯一图中只有3张走完流程，产生6个候选和2个对象，饮料杯缺失；证据在 `data/memory/` 和 `environment/demo_run_report.json` |
| v0.17.0，2026-08-02 | 第二次两阶段实验 | 14张唯一图中10张进入SAM3，产生12个候选和3个正确对象，最终覆盖12/24；证据在 `data/memory_v017_01/` 和 `environment/run_report_v017_01.json` |
