# 实验看板与审计分析

> **维护规则（强制）**：本文件记录已经真实发生的正式实验、审计结论、里程碑、仍生效的关键判断和压缩后的实验历史，不维护未来计划或下一动作。机器事实以运行报告、SQLite 和资产为准；产品验收以未提供给模型的真值和逐图人工核对为准。原始产物清理后必须明确标注，不得把历史摘要伪装成仍可直接核验的当前证据。当前协作方向由 `AGENTS.md` 维护。

- **当前版本**：v0.17.1
- **最后更新**：2026-08-02
- **当前关卡**：M5-B——Qwen 接管与第一阶段端到端验收

## 当前实验看板

| 项目 | 当前事实 |
|---|---|
| 正式实验 | v0.17.0 两阶段定向流程第二次真实服务器运行 |
| 运行 ID | `run_20260801T161221190913Z` |
| 证据 | `environment/run_report_v017_01.json`、`data/memory_v017_01/memory.sqlite`、内部同版报告、14份 Qwen 原始响应、全部图像资产和本地隐藏真值 |
| 输入 | 28个文件；14张内容唯一，14张逐字节副本在模型调用前跳过 |
| 源图结果 | 10张完成，4张因同一首轮批次的文本词数校验失败 |
| 候选与记忆 | 18个合法文本提示进入 SAM3，产生并保留12个候选；3个 `new`、9个 `existing`、12条 observation、3个 active object |
| 运行状态 | `completed_with_errors`；外部 Demo 报告 `failed` |
| 产品验收 | ❌ 未通过：3个对象身份均正确，但只保留12/24个真值视图，4张唯一图未进入 SAM3，另有6个合法提示未产生候选 |
| 回归测试 | 未得到有效结果；用户提供的命令运行在 `(base)` Python 3.10，7个测试模块均因环境或源码导入失败，不能视为代码测试结论 |

本轮确认 `with` 契约修复和“每个逻辑范围只调用一次、失败直接暴露”均真实生效。对象级结果从上一轮的2个提升到真值3个，候选纯度为12/12；但新的硬词数限制、SAM3 提示召回和低信息观测继续阻止 M5-B 通过。

## 本次实验真实流程

### 1. 哈希幂等门

- 扫描 `data/input/` 的28个文件。
- 14张唯一图片注册为 source；14张逐字节副本在任何 Qwen/SAM3 调用前跳过。
- SQLite 中14个 source 的哈希、尺寸和规范源图与输入完全一致，每个唯一哈希在输入中恰有2份。

### 2. 第一轮 Qwen 场景观察

- 14张新图按 `4 + 4 + 4 + 2` 组成4批，严格调用4次；没有重试、拆单救援、规范化或回退。
- 4份原始响应覆盖全部14个 `source_id`，共提出23个目标。按隐藏真值人工核对：22个是完整正确的对象概念；`img_0006` 把完整饮料杯收窄成 `drink cup lid with straw`，`img_0008` 漏掉被水瓶部分遮挡的鼠标。
- `with` 已真实放行。成功批次中7条含 `with` 的提示原样进入 SAM3，其中6条产生候选；合法例子包括 `transparent water bottle with white cap`、`plastic drink cup with straw` 和 `transparent water bottle with lid`。
- 第二批一次响应中的 `red plastic drink cup lid with straw` 与 `transparent plastic drink cup with red text` 各有7个英文词，被本地“最多6词”规则拒绝。该批4张图、5个模型目标整体失效，其中另外3条提示本来合法。
- 失败没有被自动掩盖：第二批只调用1次，原始文本和精确校验错误均保存到单份 `response.json`。

### 3. SAM3 定向分割与脚本后处理

- 只有首轮校验成功的10张图进入 SAM3，共18个文本提示。
- SAM3 产生12个模型阈值以上候选；面积、IoU、同概念包含关系和数量上限均未再过滤，最终仍为12个。
- 对已进入 SAM3 的提示，水瓶检出7/8、鼠标3/8、饮料杯2/2。6个零候选提示分别是 `img_0003/0005/0017/0019/0021` 的鼠标和 `img_0012` 的水瓶，不是被脚本后处理淘汰。
- 人工检查12组 crop/mask/overlay：全部属于真实水瓶、鼠标或饮料杯，背景、阴影和固定装置污染为0/12。
- 质量并非全部理想：`img_0012` 鼠标只剩被水瓶遮挡后的一块弯月形局部；`img_0013` 饮料杯 mask 覆盖杯身和杯盖，但漏掉大段吸管。

### 4. 第二轮 Qwen 候选与记忆判断

- 按10张有候选的源图各调用1次，共10次，均首试完成；两轮 Qwen 合计14次，对应14份单一 `response.json`。
- 12个候选全部先判为有效，再得到唯一终态：3个 `new`、9个 `existing`，没有 `ignored` 或 `uncertain`。
- 最终正确形成3个对象，无错并或拆档：水瓶7条 observation、鼠标3条、饮料杯2条。
- 按24个真值“图像×对象”视图计算，最终覆盖为水瓶7/10、鼠标3/10、饮料杯2/4，合计12/24。
- `img_0012` 的低信息鼠标局部仍以 `0.98 valid / 0.92 existing` 保存，说明“是真实对象且属于已有实例”仍未与“值得长期保存”分开。
- 本轮未再出现鼠标被写成粉色；但对象卡片仍把“反光表面、旁边有白色鼠标/透明容器”等场景关系写入稳定描述，水瓶“刻度标记”也可能是对白色图形的误读。

### 5. 持久化输出

- 六张核心表计数为 `1 run / 14 source / 12 proposal / 12 decision / 3 object / 12 observation`。
- 每个 proposal 恰有1个 attempt=1 的 decision；每个 `new/existing` 恰有1条 observation；对象、候选、源图和原始响应关系一致。
- 当前实现继续把每条 observation 的 crop、mask、overlay 从 proposal 复制一份。36组副本逐字节相同，额外占用2,955,257字节，占7,356,464字节记忆根的40.17%。

## 审计分析

### 机器一致性

- SQLite `integrity_check=ok`、`quick_check=ok`，外键异常0，数据库 `user_version=2` 与代码一致。
- 记忆根共102个文件；数据库和报告引用路径无缺失、越界或孤儿，86个图像资产均可解码。
- 外部报告与内部报告逐字节一致，大小78,299字节，SHA-256 为 `6b8942978780cbd9681a6e6be5cfff54f8911e54e813485dde94c8224a15e14d`。
- 4份首轮与10份第二轮原始响应均可解析为审计封套，scope、预期 ID、token、耗时和错误与报告一致。

### 产品效果

- **对象级身份正确**：真值3个对象恰好形成3个对象档案，12个已接纳候选全部归入正确实例。
- **候选纯度明显改善**：12/12是真实目标，没有旧点网格实验中的桌面、阴影、固定件和装修连接件污染。
- **视图级召回仍不足**：最终只覆盖12/24。6个真值视图随第二批 schema 失败而丢失，另6个正确文本提示在 SAM3 阶段返回0候选。
- **长期记忆质量仍未建模**：低信息遮挡视图被无条件追加，多个相近水瓶视角也全部保存；对象卡混入场景上下文。

### 当前根因与 trade-off

1. **`with` 已修复，不是本轮故障**：含 `with` 的具体短语能够通过 schema 并被 SAM3 使用。
2. **6词硬上限成为新的源头契约冲突**：两个语义具体的7词短语触发拒绝；这是本地预设约束造成的失败，不能靠重试、拆单或静默改写掩盖。
3. **完整对象命名仍有一次偏差**：`img_0006` 被命名为杯盖而不是整杯；即使取消词数冲突，这个语义偏差仍需单独评价。
4. **4图批次放大单项 schema 错误**：2个非法字段让同批4图和3条原本合法提示一起失效。当前无救援策略正确保留了实验条件和错误证据，但批次耦合仍是明确 trade-off。
5. **SAM3 召回成为可见瓶颈**：对合法提示只检出12/18，主要损失为鼠标3/8。现有证据尚不能区分遮挡、概念措辞和阈值各自影响，不能直接归因给脚本过滤。
6. **旧 Demo coverage 门槛不再代表效果**：本轮候选纯净导致 `invalid_or_duplicate_mask_was_filtered=false`；即使没有其他错误，该固定条件也会把“没有坏候选可过滤”判为失败。
7. **报告部分字段易误读**：`images_analyzed=10` 实际是成功解析图数，而 Qwen 看过14张；`every_processed_source_has_scene_guidance=true` 会把 failed source 视为已记账；`external_errors=0` 也不包含图内 schema 错误。
8. **回归测试仍未验证**：附件中的7个导入错误来自 `(base)` Python 3.10 缺少项目依赖和源码路径，不是7个业务测试失败，也不能证明现行测试通过。

### 性能与资源

| 阶段 | 调用 | 推理耗时 | Input / Output token | 峰值显存 |
|---|---:|---:|---:|---:|
| Qwen 首轮场景观察 | 4 | 209.019 s | 18,192 / 3,071 | 11,565.40 MiB |
| SAM3 分割 | 18个文本提示 | 1.495 s | — | 5,100.71 MiB |
| Qwen 第二轮判断 | 10 | 322.773 s | 42,240 / 4,718 | 11,789.87 MiB |
| Qwen 合计 | 14 | 531.792 s | 60,432 / 7,789 | 11,789.87 MiB |

运行墙钟为554.746秒（约9分15秒）。相比 v0.16.1，Qwen 总调用从36次降到14次，墙钟从918.172秒降到554.746秒；与此同时成功源图从3张增至10张、候选从6个增至12个、对象从2个增至正确的3个。速度和对象级效果均改善，但不能抵消视图覆盖仅50%的产品失败。

## 里程碑

| 里程碑 | 状态 | 已证实边界 |
|---|---|---|
| M0 环境与双模型冒烟 | ✅ | RTX 4090 上 SAM3、Qwen 可顺序真实推理 |
| M1 数据骨架 | ✅ | SQLite、配置、CLI 和空库查询链成立 |
| M2 SAM3 接口 | ✅ | 显式文本提示接口可用；不同概念和遮挡下的召回不稳定 |
| M3 Qwen 身份接口 | ✅ | 空记忆 `new`、对象卡片 `existing` 链成立 |
| M4 记忆闭环 | ✅ | 事务、回滚、幂等和失败状态经确定性测试验证 |
| M5-A 候选与单图 gate | ✅（有限） | 覆盖和源图级批量判断成立；长期视角价值尚未判断 |
| M5-B 端到端产品验收 | ❌ | v0.17.0 已找齐3个对象，但4/14唯一图失败且只保留12/24真值视图 |

## 当前关键判断

1. **M5-B 仍未通过**：对象数量和身份正确不等于完整端到端验收；当前视图覆盖只有50%。
2. **源头修复原则已得到正反两方面证据**：允许 `with` 使饮料杯首次建档；保留6词硬上限又制造了新的同类协议失败。
3. **失败透明策略应继续保持**：本轮4次首轮调用准确暴露故障，不应重新增加重试或拆单救援来追求表面完成率。
4. **SAM3 提示召回必须与脚本过滤分开统计**：本轮6个损失发生在模型候选产生前，后处理过滤数为0。
5. **3个对象档案的身份聚合可以保留为正面证据**：12个候选无背景污染、无错并和拆档；但只适用于当前3实例数据集。
6. **有效视角不等于值得长期保存**：重遮挡鼠标局部和相近水瓶视角再次证明需要独立的信息增量判断。
7. **对象稳定属性不得包含偶然场景关系**：旁边的其他物体和桌面反光不应进入对象本体描述，末次响应直接覆盖仍有污染风险。
8. **结构 coverage 不应要求实验必须制造坏候选**：没有 filtered/ignored 只能说明该分支本轮未覆盖，不能单独判定效果失败。
9. **报告指标必须区分“模型看过”“schema 接受”“进入 SAM3”“产生候选”**：否则10、14、18、12等不同口径会被混为一谈。
10. **两次失败记忆根都是证据，不是下一轮起点**：`data/memory/` 与 `data/memory_v017_01/` 均应整体保留，不能复用其中 failed source 开启新实验。

## 历史实验摘要

### v0.16.1 两阶段首次真实实验

- 28个输入正确去重为14张唯一图；4个主批次全部 schema 失败，拆成单图后只有3张完成、11张失败。
- 首轮实际调用33次，15次无反馈重试没有改变输出；最终只有6个提示进入 SAM3。
- 6个候选均为真实水瓶或鼠标，形成2个对象和6条观测；饮料杯完全缺失，鼠标卡片被错误更新为粉色。
- 运行墙钟918.172秒，Qwen 共36次调用。完整证据仍保存在 `data/memory/` 与 `environment/demo_run_report.json`。

### v0.15.2 点网格基线

- 28个输入经哈希门处理14张唯一图；SAM3 以16×16点网格生成3584个原始候选。
- 脚本过滤3512个，留下72组候选；Qwen 按源图调用14次，为72个候选各给1个终态。
- 59个候选被接纳为 observation，聚合成8个对象；真值只有3个对象。
- 审计发现30条背景/固定装置观测、5个纯背景/固定物对象，以及水瓶/饮料杯错并、水瓶拆档和鼠标档案污染，产品验收失败。
- 原始产物已在 v0.16.1 清理；逐文件证据需从 Git 提交 `3151334` 或更早历史恢复。

### 实验与决策时间线

| 日期 | 事件 | 结论 |
|---|---|---|
| 2026-07-30 | M0–M4 分阶段验证 | 双模型、持久化骨架和确定性记忆闭环成立 |
| 2026-07-30 至 2026-07-31 | M5-A 自动候选与 Qwen gate | 几何筛选和源图级批量判断可用，但不能保证候选语义纯度 |
| 2026-07-31 | v0.15.2 多视角点网格基线 | 结构检查通过，产品语义验收失败 |
| 2026-07-31 | v0.16.0 两阶段定向实现 | 完成“Qwen观察→SAM3定向分割→Qwen细判”代码 |
| 2026-07-31 | v0.16.1 两阶段首次实验 | 3/14张唯一图完成；6个候选纯净，但协议故障使饮料杯缺失 |
| 2026-07-31 | v0.16.2 本地审计 | 确认 `with` 契约、整批失败、无反馈重试、属性覆盖和资产重复问题 |
| 2026-07-31 | v0.17.0 根因修复 | 允许同一物体的 `with` 描述，移除两轮自动重试与首轮单图救援 |
| 2026-08-02 | v0.17.0 第二次真实实验 | 3个对象聚合正确，但6词限制、批次失败和 SAM3 漏检使视图覆盖仅12/24 |
| 2026-08-02 | v0.17.1 本地审计 | 完成报告、SQLite、资产和逐图真值审计；未修改业务算法 |
