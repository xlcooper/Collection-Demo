# Collection-Demo

> **项目管理入口（强制）**：每个新对话必须先读本文件，再读 `PROGRESS.md`。README 只保存项目摘要、长期准则和文件导航；`PROGRESS.md` 是当前阶段、完成状态、执行计划和下一动作的唯一事实源。不得从 README、CHANGELOG 或初始规划书推测当前进度。每次收到服务器结果、调整依赖或推进阶段后都要同步维护 PROGRESS、相关职责文件和 CHANGELOG；用户说明服务器已 push 时，本地 AI 必须先 `pull`。

**第一阶段摘要目标**：给系统一批不断增加的场景图片，它能自动发现物体、生成标签、建立对象档案，并把同一具体物体的不同观测持续归并到同一档案中。第一阶段暂不负责主动选择拍摄视角、控制机器人或判断物体信息是否已经完整。

- **当前版本**：v0.6.0
- **当前阶段**：M2——SAM3 流水线（待服务器验证）
- **当前状态摘要**：显式提示列表适配器、候选过滤去重和 crop/mask/overlay 生成已实现；等待 AutoDL 真实模型报告
- **权威进度与下一步**：查看 `PROGRESS.md`

## 当前范围

第一阶段计划实现以下无人值守 workflow：

```text
输入场景图片目录
→ 计算图片 SHA-256，跳过已经处理的图片
→ SAM3 发现并分割候选物体
→ 后处理过滤无效/重复 mask，并生成 crop、mask、overlay
→ 释放 SAM3 显存
→ 从 SQLite 读取已有对象卡片
→ Qwen 比较当前候选与已有对象，并输出受约束 JSON
→ 校验 JSON
    ├─ new：生成标签，创建对象并保存首次观测
    ├─ existing：归入已有对象并追加观测
    ├─ ignored：记录无效原因
    └─ uncertain：进入 pending 并按规则重试
→ SQLite 事务写入
→ 输出本次运行摘要
```

本阶段不实现认知完整度、主动视角规划、机械臂控制、空间地图或完整世界模型。

## 当前技术决策

- SAM3 官方图像 checkpoint；Qwen3-VL-8B-Instruct-FP8 首选，4B 仅作实测失败后的回退。
- 单卡顺序加载两个模型；Transformers 直接推理，不引入推理服务。
- 文件系统保存图像资产，SQLite 保存结构化索引。

完整环境事实、模型选择和风险只在 `docs/02_服务器环境分析.md` 维护。

## 服务器环境摘要

最新已回传环境报告结论为 0 个阻塞项、2 个警告项：nvcc 11.8 和数据盘剩余 23.45 GiB。原始事实见 `environment/server_env_report.json`，解释与模型决策见 `docs/02_服务器环境分析.md`；README 不复制完整环境表。

## 导航

| 路径 | 用途 | Git 状态 |
|---|---|---|
| `README.md` | 新对话入口、项目摘要、长期准则和职责导航；不管理详细进度 | 跟踪 |
| `PROGRESS.md` | **唯一当前执行事实源**：里程碑、状态、证据、风险、计划和下一动作 | 跟踪 |
| `CHANGELOG.md` | 已提交版本的时间线；不承担当前计划 | 跟踪 |
| `environment/*.json` | 服务器生成的机器可读证据；由 PROGRESS 引用 | 跟踪 |
| `docs/02_服务器环境分析.md` | 当前服务器事实解释、模型部署决策与环境风险 | 跟踪 |
| `docs/03_服务器环境与冒烟测试指南.md` | 服务器环境重建与 SAM3/Qwen 冒烟复现命令；不管理业务开发任务 | 跟踪 |
| `docs/04_业务代码运行与验证指南.md` | 业务包安装、CLI 使用和各开发里程碑的服务器验证命令 | 跟踪 |
| `environment.yml`、`requirements*.txt` | 可复现依赖的事实源 | 跟踪 |
| `config/`、`src/object_memory/` | 业务配置和第一阶段业务代码 | 跟踪 |
| `scripts/`、`tests/` | 环境检查、里程碑验证和回归测试入口 | 跟踪 |
| `docs/00_项目纲要.md` | 原始项目背景与第一阶段边界；仅在范围疑问时参考 | 仅本地，忽略 |
| `docs/01_第一阶段项目设计规划书.md` | 初始架构与实施指南；不是当前状态文件 | 仅本地，忽略 |

## 下一步

下一动作只在 `PROGRESS.md` 维护；`docs/03_服务器环境与冒烟测试指南.md` 只维护环境重建与模型冒烟复现命令。README 仅提供摘要和导航，避免多处计划逐渐不一致。

## 长期开发准则

本节是所有新对话和后续开发必须优先读取并持续遵守的规则。用户可以明确扩展准则；新增规则应写入本节或相应职责文件，并记录到 CHANGELOG。

1. **PROGRESS 权威**：每个新对话按 README → PROGRESS → 当前任务相关文件的顺序读取。初始纲要和规划书提供设计依据，但真实进度、计划修正和下一步只写入 PROGRESS。
2. **MVP 与奥卡姆剃刀**：优先完成满足当前目标的最小方案，避免额外扩展、过早抽象和过度设计。
3. **聚焦第一阶段**：当前只开发自动化对象记忆 Demo；主动采集、视角规划、机械臂和世界模型均不提前实施。
4. **Friendly 代码**：模块、脚本和逻辑必须职责清楚、命名直接、流程可读；优先短函数、显式类型、结构化模型输出和可操作错误信息。
5. **本地/服务器分工**：本地只用于人与 AI 协作、修改、审查、提交和 push；不在本地安装深度学习环境或运行脚本/测试。所有脚本、测试与 Demo 只在 AutoDL 服务器执行。
6. **Conda 与依赖管理**：项目运行环境统一由 Conda 管理；Conda 基础环境以 `environment.yml` 为事实源，Python 依赖以 `requirements*.txt` 为事实源，不把临时安装命令当作最终配置，不长期使用 `base` 环境运行项目。环境配置按可独立重跑的步骤拆分，不使用单个脚本串联全量安装。
7. **Git 与服务器先验**：用户通知服务器已 push 后，本地 AI 必须首先 pull，并只依据拉取到的结果文件分析。用户可以只提交服务器生成的报告，不负责维护 CHANGELOG。
8. **职责化维护**：每类信息只在其职责文件详细维护，其他文件只做摘要和链接。每次服务器结果、依赖变化或阶段推进后，必须更新 PROGRESS 及受影响文件；过时内容应替换，不能继续产生误导。
9. **Push 规则**：每个完成的本地 AI 修改批次都必须更新 CHANGELOG、提交并 push；若 push 失败必须明确报告。

## 版本规则

- CHANGELOG 由本地 AI 统一维护；用户的服务器结果提交允许暂不更新 CHANGELOG，由 AI 在拉取和分析后的维护提交中补记。
- 每个 AI push 在 CHANGELOG 顶部增加语义化版本号、北京时间时间戳和简短摘要。
- 默认递增补丁版本；阶段目标或兼容性明显变化时递增次版本或主版本。
- 同一对话回合中的关联修改作为一个版本批次提交。
